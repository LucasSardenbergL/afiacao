# nucleo-ci.txt — as provas SQL EXECUTADAS que rodam no caminho obrigatório do merge.
# =====================================================================================
# Consumido por `db/roda-nucleo-ci.sh` (job `provas-sql` do ci.yml). Uma linha por
# script; `#` comenta; linha vazia ignorada. O runner EXIGE que todo caminho aqui
# exista e que o total executado bata com o total lido — lista vazia é ERRO, não
# "nada a fazer".
#
# ## Por que uma allowlist, e não `db/test-*.sh`
#
# São 289 provas (medido 2026-09-07). Um glob traria junto as que dependem de prod
# (`psql-ro`), de `deno`/`bun` ou de dados fora do repo — e a primeira vermelha por
# AMBIENTE ensinaria a tratar este job como flaky, que é como um gate morre. A lista
# cresce por adição deliberada: rode o candidato local, confirme que é autocontido
# (só initdb + SQL do próprio script), e acrescente na seção do eixo.
#
# ## A ordem dos eixos (parecer Codex, 2026-09-07)
#
# autorização > finitude monetária > atomicidade > concorrência. É a ordem de dano:
# um furo de autorização vale por todos os outros somados.

# Formato: <caminho> <mínimo de asserts> [falsificar=<n> | falsificar=fora-do-ci]
# O segundo campo é CONTRATO, não enfeite: o fechamento das provas é
# `[ "$FAIL" -eq 0 ]`, que **também aceita PASS=0** — uma prova truncada sai 0 e
# passaria. O runner exige que o nº de asserts EXECUTADOS seja ≥ este número.
# Subir asserts não quebra nada; encolher a prova reprova até alguém baixar o
# número aqui — e aí a perda de cobertura fica no diff, que é o ponto.
#
# O terceiro é OBRIGATÓRIO quando a prova tem modo `--falsificar` (o runner procura
# a flag no arquivo e reprova a linha que não declara). `falsificar=<n>` roda o modo
# aqui e exige ≥n sabotagens com o vermelho CERTO — mesma lógica de ratchet do 2º
# campo. `falsificar=fora-do-ci` é exceção declarada, com o motivo em comentário na
# MESMA linha; o runner a imprime em toda execução. Detalhe no cabeçalho do runner.

# ── Eixo 1 — AUTORIZAÇÃO ──────────────────────────────────────────────────────────
# RLS sob `SET ROLE`, REVOKE nas DUAS pontas (PUBLIC e anon) e o `security_invoker`
# das views. Esta última é a falha ABERTA que nenhum gate textual enxerga: omitir
# `WITH (security_invoker=on)` num CREATE OR REPLACE RESETA a opção e a view passa a
# ler como OWNER, bypassando RLS.
db/test-security-invoker-views.sh          27
db/test-views-invoker-off-p0.sh            25
db/test-authz-revoke-anon-rpc.sh           19
db/test-authz-is-not-true-escritas.sh      24

# ── Eixo 2 — FINITUDE MONETÁRIA ───────────────────────────────────────────────────
# `ausente ≠ zero`: preço que o Omie não mandou não vira R$ 0,00; custo sem cmc
# degrada para NULL; e a finitude é CHECK no banco, porque guard no writer protege
# só aquele writer.
db/test-preco-ausente-nao-e-zero.sh        54
db/test-check-finitude-money-path.sh       27
db/test-caca-custo-producao.sh              6

# ── Eixo 3 — ATOMICIDADE ──────────────────────────────────────────────────────────
# "Guard fora da escrita não é guard": o predicado tem de estar DENTRO do `WHERE`
# que grava (ou no trigger), nunca num SELECT anterior.
db/test-cancelar-pedido-guard-atomico.sh   48
db/test-apply-score-updates.sh             32
db/test-sales_orders_omie_hash_unique.sh    5
# O write-back da EDIÇÃO: cabeçalho E linhas do pedido na MESMA transação. O mínimo é 54 de
# propósito — 51 é o que a prova roda quando a migration da trigger irmã (20260907220000) some do
# repo, e nesse caso a INTERAÇÃO entre as duas deixa de ser verificada em silêncio. O número aqui
# é o que faz esse silêncio virar vermelho.
db/test-pedido-edicao-atomica.sh           54
# O caminho pelo qual TODA mudança de banco entra em produção: `db:aplicar` + `aplicar_sql()`.
# Tentativa gravada FORA da transação, recibo DENTRO; marcador positivo de fim; sha reconferido
# pelo banco; e as duas classes de SQL que não cabem na transação do script (envelope e
# `CREATE INDEX CONCURRENTLY`) recusadas antes de tocar no banco.
# Entrou aqui porque a ausência já custou: o #2421 quebrou TODA migration com envelope e passou
# verde no CI. Tinha duas camadas — as fixtures não cobriam a classe com `BEGIN;` (#2441), e a
# prova rodava só no laptop, presa a um PGBIN de Homebrew. Portada para o pg-harness aqui.
# O `--falsificar` dela (9 sabotagens) segue FORA: o 2º locale tem de ser pt_BR, porque o
# `db-aplicar.sh` separa falha-limpa de desconhecido casando ERRO×ERROR do psql, e o runner
# ubuntu não tem pt_BR — rodar em (C, C.UTF-8) seria a falsificação-em-um-ambiente do #1483.
db/test-db-aplicar.sh                      36  falsificar=fora-do-ci  # sem pt_BR no runner, ERRO×ERROR do psql ficaria sem prova — provisionar locale e provar numa falha real

# ── Eixo 4 — CONCORRÊNCIA / TOCTOU ────────────────────────────────────────────────
# A janela entre dois round-trips PostgREST, com uma chamada HTTP que cria pedido de
# compra REAL no meio. Nenhum lock atravessa uma transação que já commitou; o que
# atravessa é uma marca durável na linha.
db/test-claim-disparo-cenario-b.sh         76
db/test-disparado-simulado-pos-disparo.sh  59
db/test-fin-sync-lease.sh                  22
db/test-pedido-venda-coerencia.sh          31

# ── Eixo 5 — VEREDITO DE DEPLOY ──────────────────────────
# O SQL que responde "a edge está REALMENTE no ar?". O dano aqui é acreditar num
# veredito: `CANARIA VERMELHA` que nunca dispara, 401 que vira fail-open, resposta
# de ontem valendo como veredito de hoje. Esta prova é a única do núcleo que GERA o
# SQL pelo próprio gerador (`db/lib/gerar-canaria-fixture.ts`, sobre o gerador do
# disco) em vez de conferir um retrato commitado — por isso o job carrega o runtime
# bun, e por isso ela envelhece junto com o gerador em vez de virar retrato velho.
# Entrou no núcleo em 2026-09-08 porque o gate de mutação NÃO cobre o bloco canária
# (medido: 10 de 12 mutações sobrevivem lá). Mas o que entrou foi o modo NORMAL: as
# sabotagens — a rede que dava sentido à entrada — seguiram fora do CI até 2026-09-10,
# quando ganharam o 3º campo abaixo (e, antes de entrar, a exigência de vermelho pelo
# motivo CERTO: duas delas ficavam vermelhas por SQL que nem compilava).
db/test-canaria-veredito.sh                24  falsificar=17

# ── Eixo 6 — DUPLA CONTAGEM (o mesmo fato sob N óticas) ──────────────────────────
# `fin_movimentacoes` guarda o MESMO pagamento do Omie duas vezes (lançamento do título e
# lançamento no banco) e ainda previsões. Somar sem escolher a ótica dobra o caixa sem erro
# nenhum. Esta prova executa o bloco (c) do SQL da skill CFO EXTRAÍDO DO ARQUIVO — é a única
# rede de um SQL que não é migration: a skill é texto, e quem a editar reintroduzindo a dobra
# só fica vermelho aqui. Entrou em 2026-09-10, quando a dobra já tinha inflado a tendência de
# caixa publicada no fechamento de abril (docs/cfo/2026-04-fechamento.md, errata).
db/test-cfo-caixa-90d-otica.sh             30
