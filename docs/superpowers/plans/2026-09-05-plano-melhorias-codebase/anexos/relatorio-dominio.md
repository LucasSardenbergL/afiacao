# Auditoria read-only — domínio `src/lib` · `src/services` · `src/queries` · `src/utils` · `src/types` · `src/integrations`

Worktree: `.claude/worktrees/intelligent-yalow-39d4e7` @ `9fe4e8880` (branch `claude/code-improvements-plan-2bc5f9`) · 2026-09-05.
Método: só grep/sed/git (leitura). Health em background: typecheck 0 · lint 0 · knip 0 (0 exports órfãos). Nada rodado por mim.
Convenção de nota: **I**=impacto · **R**=risco de não fazer · **E**=esforço (1 baixo … 5 alto). "⚠ incerteza" = não medi em prod (psql fora das regras deste subagente).

---

## Achados (ordenados por I×R)

### A1 | Casts `as never` cuja justificativa EXPIROU (89% apontam para nomes já tipados) | código
- **Evidência:** `rg "\.(from|rpc)\('x' as never"` em prod = **89 sites / 69 nomes / 46 arquivos**; cruzado com `types.ts` (Tables+Views+Functions): **79 sites (89%) já estão tipados**. Comentários que afirmam "ainda não está nos tipos gerados" (14 ocorrências) para tabelas presentes: `src/hooks/useEndividamento.ts:3-5` (`fin_dividas*` — estão), `src/queries/useClienteGrupos.ts:8-16` ("regen é a Task 4" — `cliente_grupos` está), `src/hooks/useTarefas.ts:10-13` (`v_tarefas_estado` etc. — estão, e ainda empilha `as never) as any`), `src/lib/call-log/record.ts:6-10` (`(supabase as any).from('call_log')`), `src/types/prime.ts:3-4` (3 `prime_*` já tipadas, tipos manuais paralelos), reposição money-path: `src/components/reposicao/embalagem/useEmbalagemConsulta.ts:49,62` (`sku_embalagem_equivalencia`, `sku_preco_fornecedor_capturado`), `src/hooks/useClienteTier.ts:22,85` (`cliente_tier_preco`).
- **Por que importa:** `.from('x' as never)` desliga a checagem de colunas do `select/update/eq` inteiro — renomear/dropar coluna no banco passa no typecheck e só falha em runtime (PostgREST 42703). O gerador (Lovable, 301 commits "Work in progress") resolve a dívida sozinho, mas ninguém retira o cast: o motivo expirou e a proteção continua desligada.
- **Proposta:** (1) gate `src/__tests__/casts-stale-gate.test.ts` que lê `types.ts` e reprova `.from/.rpc('<nome>' as never)` quando `<nome>` já existe (denominador = 89, baseline = os 10 legítimos); (2) codemod por módulo (46 arquivos) trocando por `supabase.from('x')` + `Tables<'x'>`; apagar `src/types/prime.ts` manual e os tipos locais de `useClienteGrupos`.
- **I=4 · R=4 · E=2 (gate) / 3 (codemod)**

### A2 | Tabela e RPC FANTASMA escondidas pelo `as never` — feature roteada sobre objeto que não existe em prod | código/infra
- **Evidência:** `fila_aplicacao_omie` (3 arquivos: `src/components/reposicao/aplicacao/useAplicacaoFila.ts:87,…`, edge `supabase/functions/omie-aplicar-parametros/index.ts:129,163`) e RPC `gerar_fila_aplicacao_omie` (`useAplicacaoFila.ts:127`) e `quality_checklists` (`src/pages/QualityChecklist.tsx`, `src/pages/OrderDetail.tsx`): **0 ocorrências** em `supabase/schema-snapshot.sql` (qualquer schema; dump de 2026-09-05), **0** em `supabase/migrations`, **0** em `db/`, **0** no `types.ts` (que lista as mesmas 336 tabelas do snapshot). Rotas ATIVAS: `src/App.tsx:374` (`admin/reposicao/aplicacao` → `sessao/aplicacao`, página `AdminReposicaoSessaoAplicacao` consome o hook) e `src/App.tsx:304` + link em `src/components/admin-order/OrderActionButtons.tsx:34` (`/admin/orders/:id/quality`). Origem: commits "Changes" do bot Lovable (2026-02-21 e 2026-04-19).
- **Por que importa:** dois geradores independentes (pg_dump + gerador de tipos) concordam que os objetos não existem → ou a tela quebra em prod (42P01 no primeiro `.from`) ou é feature morta que o knip não vê (page roteada = entry). O `as never` é exatamente o que impediu o typecheck de acusar. ⚠ incerteza: não confirmei via psql-ro; confirme com `\d fila_aplicacao_omie` antes de agir.
- **Proposta:** confirmar em prod; se ausente, decisão de produto: aposentar (regra money-path "quem chama? é a pergunta errada" → medir uso via PostHog `reposicao.aplicacao.*`/rota) ou criar a tabela+RPC por migration via Lovable. Enquanto isso, o gate do A1 deve ter um 2º ramo: nome ausente do `types.ts` E do snapshot = vermelho (não baseline).
- **I=4 · R=3 · E=2**

### A3 | Money-path: `accumulateMarginFromItems` fabrica preço 0 em item sem `unit_price` (assimétrico com o custo) | código
- **Evidência:** `src/lib/scoring/margin.ts:214-215` — `qty = Number(item.quantity || item.quantidade || 1)`, `price = Number(item.unit_price || item.valor_unitario || 0)`; o item sem custo é PULADO (`c == null → continue`, l.213) mas o item sem preço entra com receita 0 e custo cheio. Cabeçalho do mesmo arquivo (l.5-10, 34-43) condena `|| 0`. `src/lib/scoring/__tests__/margin.test.ts` cobre "sem custo" (l.20) e "string" (l.38), **não** "sem preço". Na origem, `supabase/functions/omie-vendas-sync/index.ts:1296,1355` e `omie-analytics-sync/index.ts:1343` gravam `unit_price: prod.valor_unitario || 0` — a ausência chega ao TS já como 0.
- **Por que importa:** margem do pedido/cliente fica NEGATIVA fabricada (custo sem receita) → health score do farmer rebaixado e fila de ações distorcida — o oposto de "ausente ≠ zero". Como a edge já converte em 0, corrigir só o TS é INERTE (money-path §2: "MEÇA se a ausência chega como null").
- **Proposta:** na edge, gravar `valor_unitario` ausente/≤0 como `null`; em `margin.ts`, `if (!(Number.isFinite(price) && price > 0)) { semPreco++; continue; }` e expor `semPreco` na cobertura (`coberturaCustoCliente`) — teste de falsificação com item `unit_price: null`. Espelho MIRROR já existe (`edge-money-path-invariants`), então o pin acompanha. ⚠ incerteza: volume real de `unit_price` 0/null em `sales_orders.items` não medido.
- **I=4 · R=3 · E=1 (TS) + 1 (edge)**

### A4 | "Hoje" com 2 semânticas (UTC × São Paulo) e 5 implementações | código
- **Evidência:** UTC: `src/lib/visitas/today.ts:2` (`hojeISO`, comentado "UTC, consistente com o route planner"), `src/queries/usePropostaPreview.ts:19` (`hojeIso` privado), `src/pages/RotaPropostas.tsx:29` (`todayIso`), + **26 sites** `new Date().toISOString().slice(0,10)|split('T')[0]` em **23 arquivos**. SP: `src/lib/dashboard/sp-date.ts:8` e `src/lib/prime/competencia.ts:12` (`hojeSP` — duas cópias funcionalmente idênticas), `src/lib/time/sp-day.ts:38` (`spBusinessDate`, canônico, 12 callers). Sites money-path em UTC: `src/components/financeiro/cashflow/EventosManager.tsx:200,214` (default de `inicio`/`data_prevista` de evento de caixa — engine A1), `src/pages/AdminReposicaoAlertas.tsx:46`, `src/hooks/useRoutePlanner.ts:424,619`, `src/hooks/useVisitasHoje.ts:25` (`.eq('scheduled_date', hojeISO())`). Três `addDays` (aging-helpers:100, sp-date:13, usePropostaPreview:20) e dois `daysBetween` (aging-helpers:93, valor-cockpit-helpers:35) — todos corretos, todos duplicados.
- **Por que importa:** entre 21h e 0h BRT o "hoje" UTC é amanhã: evento de caixa cai no dia errado da projeção; a lista "visitas de hoje" mostra as de amanhã; o filtro de alertas pula um dia. É a classe "fuso" que `sp-day.ts` existe para fechar — mas não é a regra.
- **Proposta:** consolidar `hojeSP()`/`addDias`/`diasEntre` em `src/lib/time/` (mover as 2 cópias, reexportar); regra ESLint `no-restricted-syntax` para `toISOString().slice(0, 10)`/`split('T')[0]` fora de `src/lib/time`; DECISÃO explícita em `docs/agent/mapa-do-app.md` se rota/visitas seguem UTC (hoje é comentário de código, não doc). ⚠ o UTC do route planner parece deliberado (comentário), mas sem doc — tratar como decisão pendente, não bug.
- **I=3 · R=3 · E=2**

### A5 | Formatador de moeda: 39 definições locais, zero canônico, semânticas divergentes (uma fabrica R$ 0,00) | código
- **Evidência:** 39 definições `formatBRL|fmtBRL|brl|formatCurrency` (36 em `src`, 2 em edges), **78** sites `Intl … currency: 'BRL'`; `src/lib/format.ts` exporta só `maskDocument/decodeHtmlEntities/formatMargemPct`. Divergências: `src/lib/grupos/format.ts:13` → `BRL.format(Number(v ?? 0))` (null → "R$ 0,00", 6 callers em `GrupoFinanceiroTab` mostrando saldo/vencido do grupo); `src/lib/prime/format.ts:5`, `src/lib/reposicao.ts:5`, `src/lib/reposicao/sku-param.ts:125` → null → "—"; `src/lib/fila/critica/montar.ts:9` → `Math.round(v).toLocaleString` (sem símbolo, sem centavos); `src/pages/RotaPainelLigacoes.tsx:15` → `maximumFractionDigits: 0`.
- **Por que importa:** "ausente ≠ zero" vale na UI: o saldo do grupo indisponível vira "R$ 0,00" — o mesmo defeito que as engines erradicaram, reintroduzido na camada de formatação. E cada tela decide sozinha se mostra centavos.
- **Proposta:** `formatBRL(v, { casas?: 0|2 })` em `src/lib/format.ts` com `null|undefined|NaN → '—'`; codemod nas 36 definições (reexportar onde o nome local for importado por muitos); ESLint `no-restricted-syntax` para `style: 'currency'` fora de `src/lib/format.ts`.
- **I=3 · R=2 · E=2**

### A6 | Três camadas de acesso a dados sem critério: `src/queries` ≡ `src/hooks`; 73 arquivos de UI chamam o Supabase direto | arquitetura
- **Evidência:** `src/queries/` (27): 23 são hooks react-query, 8 chamam `supabase.from/rpc` direto, 7 usam `as never/RpcFn`, **0** importam `services/` — indistinguível de `src/hooks/` (178: 104 react-query, 62 supabase direto, 11 importam services, 3 importam queries). `src/services/` (45): **0/45** usam react-query (I/O puro, 7 testes em `src/services/__tests__/`) — único critério REAL. UI com I/O direto: 37 `pages` + 36 `components`; `src/lib` com I/O: 10 arquivos (`analytics-ledger`, `farmer/registrar-geracao`, `push/device`, `reposicao.ts`…). Docs: **0** menções a `src/queries`/critério de camada em `CLAUDE.md` e `docs/agent/*`. `queries/` nasceu em 2026-03-06 ("Move queries to TanStack hooks"); nos últimos 60 dias entraram 4 arquivos novos em queries, 5 em hooks, 4 em services.
- **Por que importa:** sem regra, cada PR escolhe a pasta por hábito; o manifesto de módulos (F1) classifica por dono, não por camada, então a ambiguidade não fica vermelha. A camada que tem critério bom (services = puro, testável sem React) é a menos usada.
- **Proposta:** regra em `docs/agent/mapa-do-app.md`: `services/` = I/O puro sem React (testável); `hooks/` = react-query sobre services ou supabase; `src/queries/` **congelado** (gate no `manifesto.gate`: arquivo novo em `src/queries/` = vermelho; migração oportunista para `hooks/`); ratchet "UI não chama `supabase.from/rpc`" com baseline 73 (mesmo idioma do `fronteiras.gate`).
- **I=3 · R=2 · E=2 (regra+gate) / 4 (migração)**

### A7 | Ratchet de fronteiras estagnou há 2 meses e aceita aresta nova por regeneração | arquitetura
- **Evidência:** `src/lib/modulos/fronteiras-baseline.ts` — histórico de arestas: 284 → 239 → 226 (PRs cirúrgicos, 2026-07-09) → 225 → 226 → 229 → 231 → 230 → 229 → 227 → 229 → **230** (hoje: 194 runtime + 36 type, 57 pares). Desde `0008eeaf3`: **+10 adicionadas / −6 removidas**. Pares bidirecionais (critério Codex: "módulo mal cortado ou contrato ausente"): farmer↔telefonia **29+11 = 40** (17% da baseline), vendas↔farmer 9+8, vendas↔tintometrico 8+6. Arestas novas típicas: `src/services/whatsappProposta/enviarProposta.ts → src/lib/pricing/mergeCustomerPrices.ts` e `src/lib/whatsapp/proposta-cotacao.ts → …mergeCustomerPrices.ts` (o único consumidor externo do helper é telefonia); `DesovaMissaoDialog → useCoverage/useTarefas/categoria-map` (×3). A mensagem do gate (`fronteiras.gate.test.ts:26`) instrui a rodar `gerar-baseline` para a aresta nova "ficar visível no diff".
- **Por que importa:** o ratchet só é ratchet se a adição custar algo; hoje custa um comando. O PR-3 planejado (sino do AppShell, 7 inversões + ~30 KB eager) segue pendente desde 2026-07-08.
- **Proposta:** (a) campo `motivo:` obrigatório na entrada da baseline para aresta ADICIONADA depois do commit-âncora (gate reprova entrada nova sem motivo); (b) mover `mergeCustomerPrices` para contrato de `vendas` explicitamente exportado (ou para telefonia, dono do único consumidor) — −2 arestas; (c) executar o PR-3 já quantificado.
- **I=3 · R=2 · E=2**

### A8 | Validação de fronteira: 201 chamadas rpc/invoke, 169 casts `data as X`, zod em 0 arquivos de services/queries/hooks | código
- **Evidência:** `.rpc(` **103** sites, `functions.invoke|invokeFunction` **98**; casts pós-resposta **169** em 123 arquivos; arquivos com `from 'zod'`: lib 3 · services 0 · queries 0 · hooks 0 · components 11 · pages 1. Três críticos money-path: (1) `src/components/reposicao/aplicacao/useAplicacaoFila.ts:131` — `data as unknown as GerarFilaResult | GerarFilaResult[]` (contrato ambíguo resolvido por `Array.isArray`; o toast fabrica "0 prontos" via `r?.prontos ?? 0` se o shape mudar — e a RPC nem existe em prod, cf. A2); (2) `src/pages/AdminReposicaoPedidos.tsx:502` → `interpretarRespostaDisparo(data as RespostaDisparo)` (`src/components/reposicao/pedidos/shared.ts`: `disparados ?? 0`, `falhas ?? 0`, `aguardando_portal ?? 0` → resposta com chave renomeada vira "nada a disparar" (info) num pedido que pode ter sido enviado ao fornecedor); (3) `src/services/financeiroV2Service.ts:674-679` — `rows as unknown as Array<Record<string, unknown>>` + `valor_documento ?? 0` no orçamento por entidade. Contra-exemplo correto no mesmo domínio: `parseSnapshotSemanas` (`cockpit-consolida-helpers.ts`) valida à mão e degrada para `null`.
- **Por que importa:** o cast afirma o shape que a edge devolvia no dia do PR; a edge é deployada à parte (Lovable, 3 deploys manuais) — a deriva não tem sensor no cliente.
- **Proposta:** parser fail-closed (zod ou à mão, padrão `parseSnapshotSemanas`) nos 3 sites; `interpretarRespostaDisparo` deve distinguir "chave ausente" (→ `error` + mensagem "resposta inesperada — confira o pedido") de "0"; gate textual com lista FECHADA das edges money-path exigindo parser no consumidor.
- **I=3 · R=3 · E=2**

### A9 | Arredondamento: 16 `roundN` locais sem canônico; `Math.round(x*100)/100` sem EPSILON | código
- **Evidência:** `round2/round4/round6` definidos localmente **16×** (8 em `src`: `src/lib/custos/auditoria-margem.ts:26`, `src/lib/tint/sync-promote.ts:23`, `src/lib/venda-assistida/preco-preparado.ts:80`, `src/lib/financeiro/valor-cockpit-helpers.ts:214`…; 8 em edges: `enviar-pedido-portal-sayerlack/qtde-portal.ts:22` `round6`, `scoring-recalc-client:325`…), + 5 `Math.round(x*100)/100` inline; **0** helper exportado (`rg '^export (const|function) round'` = vazio); 32 `toFixed` (quase todos em texto de motivo — ok); 0 comparação `===` entre floats money-path encontrada (bom); `Number.EPSILON` aparece 21× mas nunca dentro de um `roundN`.
- **Por que importa:** `Math.round(1.005 * 100) / 100 === 1` (não 1.01) — o idioma falha em meio-centavo por representação binária; cada cópia decide sozinha e a paridade TS×edge×SQL (`round(numeric, 2)` do Postgres arredonda half-away-from-zero) não é provada para nenhum deles. Risco baixo por site, sistêmico por quantidade.
- **Proposta:** `src/lib/money/arredondar.ts` (`round2 = (x) => Math.round((x + Number.EPSILON) * 100) / 100`, `round4`, `round6`) espelhado em `_shared/` com teste de paridade contra casos `1.005`, `2.675`, negativos; migrar os 8 de `src` (E=1) e os 8 das edges quando tocadas.
- **I=2 · R=2 · E=1**

### A10 | Paginação: 11 helpers (5 canônicos pinados + 6 locais fora do pin G2) | código
- **Evidência:** definições: `fetchAllPages` (`src/lib/postgrest.ts:152`, 11 callers), `coletarPaginado` (`src/lib/scoring/rpcPaginada.ts:44`), `paginateAll` (`src/hooks/unifiedOrder/catalog-helpers.ts:27`), `buscarTodasPaginas` (**privado** em `src/services/financeiroService.ts:204` — enquanto o V2 importa `fetchAllPages`), `paginarTudo` (`src/lib/carteira/escopo-clientes.ts:46`), `paginarLog` (`src/queries/useRouteContactList.ts:93`), `lerTudo` (`src/queries/useRoutePanel.ts:10`), `paginarView` (`src/hooks/useCaca.ts:54`), `fetchAll` (`src/hooks/useUtiContas.ts:69`) + 2 nas edges. `.range(` = 78 sites em 38 arquivos. Os 6 locais cumprem o contrato hoje (throw em `error`, `data == null` → throw, `.order` no caller) — mas o pin G2 do `paginacao-artesanal-gate` nomeia só os 5 canônicos.
- **Por que importa:** o gate G1/G3 cobre a FORMA do laço, não o contrato de cada cópia: uma "simplificação" futura num local (`?? []`) fica verde. Cinco reimplementações de 15 linhas do mesmo helper que o repo já erradicou uma vez (#1557/#1563/#1564).
- **Proposta:** trocar os 6 locais por `fetchAllPages` (assinatura idêntica `(de, ate) => PromiseLike<{data,error}>`); mover `buscarTodasPaginas` para `lib/postgrest` ou apagar. Sem risco funcional; fecha a superfície que o G2 não pina.
- **I=2 · R=2 · E=2**

### A11 | Idioma `.range(de, ate) as unknown as PromiseLike<{ data: X[] | null; error: unknown }>` — 24 sites em 9 arquivos | código
- **Evidência:** `src/hooks/useFarmerScoring.ts:177,203,231,334,363`, `src/hooks/useBundleEngine.ts:381,389,417,513`, `src/hooks/useCrossSellEngine.ts:358,381,461`, `src/services/financeiroV2Service.ts:523,535`, `src/hooks/useTacticalPlan.ts` (+5 `as unknown as {data…}`). Causa: `fetchAllPages<T>(buscarPagina: (de, ate) => PromiseLike<{ data: T[] | null; error: unknown }>)` (`src/lib/postgrest.ts:152-153`) não infere `T` do builder tipado quando o `.from` é `as never` ou o select tem string composta.
- **Por que importa:** cast triplo = o `select('a, b')` deixa de ser conferido contra `T` — mesma cegueira do A1, em engines money-path (scoring/bundle/cross-sell).
- **Proposta:** overload em `fetchAllPages` aceitando `PromiseLike<PostgrestResponse<T>>`/`PostgrestFilterBuilder<…, T[]>` para o `T` sair do select; remove os 24 casts junto com o A1 (mesmo codemod).
- **I=2 · R=2 · E=2**

### A12 | `financeiroService` × `financeiroV2Service`: "V2" é outro DOMÍNIO, não versão — nome induz a migração inexistente | documentação/arquitetura
- **Evidência:** V1 (`src/services/financeiroService.ts`, 1.211 l, **39** importadores) = sync + resumo + CP/CR + aging + DRE + fluxo + capital de giro + mapping + CSV + giro executivo; V2 (785 l, **10** importadores) = fechamento, eliminações intercompany, orçamento, permissões, análise dimensional, snapshots do cockpit, balanço. **0** colisão de nomes exportados; `financeiroTypes.ts` já é compartilhado; nenhum doc/TODO de convergência (`docs/agent/financeiro.md` não cita os arquivos).
- **Por que importa:** "V2" comunica "V1 é legado" — agente novo tende a portar leitura para o V2 ou a evitar tocar o V1; o custo é decisão errada, não bug.
- **Proposta:** mínimo (E=1): JSDoc de cabeçalho nos dois ("leitura operacional" × "fechamento/orçamento/dimensional; não há migração pendente") + linha em `financeiro.md`. Ideal (E=3): `src/services/financeiro/{leitura,fechamento,orcamento,dimensional}.ts` com barrel de compat e split do V1 por responsabilidade.
- **I=2 · R=1 · E=1 / 3**

### A13 | `decodeHtmlEntities` duplicado com implementações diferentes | código
- **Evidência:** `src/lib/format.ts:45` (textarea + guard `typeof document === 'undefined'`, 2 callers) vs `src/lib/utils.ts:12` (`DOMParser`, sem guard SSR, fallback `|| text`, 10 callers).
- **Por que importa:** dois comportamentos para a mesma pergunta (entidade HTML no nome do produto Omie); um deles lança fora do DOM.
- **Proposta:** manter a de `utils.ts` com o guard da outra; `format.ts` reexporta. Quick win.
- **I=1 · R=1 · E=1**

### A14 | Normalizador de documento em 4 cópias (+ CEP ×2, acento ×3 e 12 sites `normalize('NFD')`) | código
- **Evidência:** `replace(/\D/g, '')` como função nomeada em `src/queries/useClienteGrupos.ts:44` (`normalizarDocumento`), `src/lib/caca/documento.ts:13` (`normalizarDocumento`), `src/lib/clientes-nao-vinculados/snapshot.ts:41` (`normalizeDoc`), `src/hooks/useUtiContas.ts:93` (`normDoc`, com regra ≥11 dígitos própria); `normalizarCep` em `src/lib/route/cep.ts:5` e edge `cep-geo-resolver`; `semAcento` (`src/lib/route/field-targets.ts:61`), `normalizarTexto` (`src/lib/radar/normalizar.ts:54`) + 12 sites soltos de `normalize('NFD')`. Total de definições `normaliz*|normalize*|slugify|semAcento`: 61 (a maioria legítima, domínio-específica). `normalizeCityKey` tem harness de paridade SQL (`db/test-city-norm-paridade.sh`) — não tocar.
- **Por que importa:** documento é chave de junção Omie↔profile (CNPJ com máscara vs sem); 4 regras próximas-mas-não-iguais (`useUtiContas` devolve `null` <11 dígitos, as outras devolvem string curta) produzem matches diferentes por tela.
- **Proposta:** `src/lib/documento.ts` (`apenasDigitos`, `normalizarDocumento`, `documentoValido`) com os 4 callers; `src/lib/texto.ts` (`semAcento`) para os 12 NFD. Sem tocar city-norm.
- **I=1 · R=1 · E=1**

### A15 | Tipos manuais paralelos ao `types.ts` já resolvidos pelo gerador | código
- **Evidência:** `src/integrations/supabase/types-departments.ts` (39 l) e `visitasAgendadas.ts` (18 l), `src/types/prime.ts` ("prime_* ainda NÃO estão nos tipos gerados" — 3 estão), tipos locais em `useClienteGrupos`/`useEndividamento`/`usePedidosProgramados` — todos com comentário "apagar quando o types.ts for regenerado". O `types.ts` (20.729 l) É regenerado pelo Lovable a cada "Work in progress" (5 últimos commits) — 336 tabelas = 336 do snapshot, 83 views (81 + 2 matviews), 275 funções (as 150 do snapshot fora do types são triggers/`private`/não-PostgREST).
- **Por que importa:** o `types.ts` não deriva (medido) — quem deriva são os tipos manuais, que ninguém aposenta porque a promessa "quando regenerar" não tem gatilho.
- **Proposta:** incluir no gate do A1 um 2º assert: arquivo com comentário `ainda não est(á|ão) nos tipos` cujo nome já está no `types.ts` = vermelho (14 ocorrências hoje).
- **I=2 · R=2 · E=1**

---

## Medições (grep, prod = sem `*.test.*` e `__tests__/`)

| Eixo | Número |
|---|---|
| Inventário | `src/lib` 677 arq · `services` 45 · `queries` 27 · `utils` 3 · `types` 5 · `integrations` 4 · `hooks` 178 (não-teste) |
| Money-path (lib financeiro/reposicao/custos/regua-preco/carteira*/preco/pricing/pedido*/positivacao/scoring + services fin* + utils) | `Number(` 86 · `?? 0` 126 · `\|\| 0` 12 · `parseFloat(` 1 · `toFixed(` 32 · `Math.round(` 30 · `isFinite` 123 · `EPSILON` 21 · `Infinity` 11 (todos em comentário/sentinela) — 206 sites de coalescência-para-zero; top: `orcamento-forecast-helpers` 22, `financeiroService` 18, `scoring/margin` 15, `compras-otimizador-helpers` 11, `valor-cockpit-helpers` 11, `antecipacao-helpers` 11 |
| Arredondamento | `roundN` locais 16 (8 src + 8 edge) · `Math.round(x*100)/100` inline 5 · helper canônico exportado 0 · `===` entre floats money-path 0 |
| Formatadores | moeda: 39 definições (36 src), 78 sites Intl BRL · data: 34 definições · "hoje": 5 implementações (3 UTC, 2 SP + `spBusinessDate`) · `new Date().toISOString().slice/split` 26 sites/23 arq |
| Parsers | `parseDecimalBR` 2 (src + edge tint-import, paridade testada) · `parseValorBR` 1 (prime, guardado por regex) |
| Normalizadores | 61 definições · documento 4 cópias · CEP 2 · `normalize('NFD')` 12 sites |
| Paginação | 11 helpers (9 src + 2 edge) · `.range(` 78 sites/38 arq · `fetchAllPages` 11 callers |
| V1/V2 | 1.211 × 785 linhas · 39 × 10 importadores · 0 colisões · 7 testes em `services/__tests__` |
| `types.ts` | 20.729 l · 301 commits (últimos 5 = bot Lovable) · tabelas 336 = 336 snapshot · views 83 vs 81 (2 matviews) · fns 275 vs 346 |
| `as never` | 229 sites/77 arq · em `.from/.rpc('x' as never)`: 89 sites/69 nomes/46 arq · **79 (89%) já tipados** · 3 nomes inexistentes em prod+types (`quality_checklists` 4, `fila_aplicacao_omie` 3, `gerar_fila_aplicacao_omie` 1) · 2 dinâmicos |
| Outros escapes | `as any` 33/18 arq (useTarefas 6, lib/transcription 4) · `: any` 14/3 (lib/sip 11 — JsSIP) · `@ts-expect-error` 3 · `@ts-ignore` 0 · `as unknown as` 310/147 (components/reposicao 32, queries 23, useTacticalPlan 14) · `as unknown as PromiseLike<` 24/9 · `as RpcFn` 9/4 |
| Fronteira rpc/invoke | `.rpc(` 103 · invoke 98 · casts `data as X` 169/123 arq · zod: lib 3 · services 0 · queries 0 · hooks 0 · components 11 · pages 1 |
| Camadas | queries: 23/27 react-query, 8 supabase direto, 0 importam services · hooks: 104/178 react-query, 62 supabase direto, 11 importam services · services: 0/45 react-query · UI com supabase direto: pages 37 + components 36 · lib com I/O 10 · docs com critério: 0 · arquivos novos 60d: queries 4 / hooks 5 / services 4 |
| Fronteiras de módulo | baseline 230 (194 runtime + 36 type, 57 pares) · histórico 284→239→226→225→226→229→231→230→229→227→229→230 · +10/−6 desde 2026-07-09 · farmer↔telefonia 40 · vendas↔farmer 17 · vendas↔tintometrico 14 |
| Espelhos src/lib ↔ `_shared` | 7 arquivos homônimos, todos em paridade lógica (diff só em comentários) · 15 arquivos com `MIRROR-START` em src · 2 testes de paridade |
| Datas (money-path dirs) | `new Date(` 47 · `toISOString().slice/split` 5 (4 com `T00:00:00Z` = seguros; 1 UTC-shift: `usePropostaPreview:19`) · date-fns só em `lib/pedido` (5 arq) |

## Descartei porque…

1. **`saldo ?? 0` nos `reduce` de `financeiroService.ts:602-638,877`** — `fin_contas_receber.saldo`/`fin_contas_pagar.saldo` são `GENERATED ALWAYS AS (valor_documento NOT NULL − COALESCE(...))` (snapshot): nunca NULL; o `?? 0` é ruído de tipo, não fabricação. (`fin_contas_correntes.saldo_atual` é `DEFAULT 0` nullable — `c.saldo_atual ?? 0` em l.250/604 fabricaria só se houver linha com NULL em prod; não medido, e o doc financeiro já trata a lista vazia com `exigirLinhas`.)
2. **`demanda_diaria ?? 0` em `compras-otimizador-helpers.ts:86-107`** — guardado em l.189 (`<= 0` → não força, flag). Desenho documentado em `reposicao.md`.
3. **Views `fin_analise_cp/cr_dimensoes` "no types mas não no snapshot"** — são `MATERIALIZED VIEW` (snapshot l.8500); o gerador lista matviews sob `Views`. Não é deriva.
4. **`parseDecimalBR` duplicado (src/lib/preco + edge tint-import)** — espelho deliberado (Deno não importa de `src/`) com `src/lib/tint/__tests__/edge-parse-parity.test.ts`. Desenho (money-path §Helper espelhado).
5. **Scoring farmer TS × SQL/edge** — `calculate-scores` importa `_shared/score-ponderado.ts` e `_shared/paginate.ts`; `margin.ts` está pinado por `MIRROR` em `edge-money-path-invariants.test.ts`; paridade TS×SQL provada por `db/test-fu4f-fase3-paridade-ts-sql.sh`. Desenho.
6. **Os 7 helpers homônimos src/lib ↔ `_shared` "divergem" (erro-mensagem 35 l, universo-pedidos 7 l)** — diff normalizado mostra só JSDoc; lógica idêntica; `STATUS_NAO_VENDA` pinado pelo gate. Descartado.
7. **Divisão por `receitaTTM` em `ponto-equilibrio-helpers.ts:239,277`** — `if (!(mcPct > 0)) return degradar(...)` engole NaN/Infinity; `mediaPonderada` (aging) guarda `somaPeso <= 0`. Descartado.
8. **`orcamento-forecast-helpers.ts:52-56` soma `receita_bruta ?? 0` de meses fechados** — o arquivo carrega `confianca` por linha e `confianca_geral` (17 menções) e `fechados` vem de `fin_fechamentos` (mês fechado sem DRE é anômalo). Fraco demais para listar; ⚠ não provei que a confiança rebaixa por mês faltante.
9. **`Infinity` (11 sites)** — todos em comentário de guard ou sentinela deliberada (`payback_meses ?? Infinity` no sort, `piorSaldo = Infinity` no mínimo). Descartado.
10. **Lint `no-explicit-any`** — `eslint.config.js:10` só comenta; `: any` prod = 14, 11 deles em `src/lib/sip` (JsSIP sem tipos). Não é achado.
