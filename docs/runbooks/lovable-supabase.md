# Runbook — Banco & Deploy no Lovable (Supabase, migrations, edge, frontend)

> Detalhe operacional movido do CLAUDE.md (§5) em 2026-06-14. As REGRAS-CHAVE ficaram destiladas no CLAUDE.md §5 "Banco & deploy (Lovable)". Leia ISTO ao tocar banco/migration/edge/deploy. Inclui o histórico de migrations aplicadas.

### ⛔ Acesso ao banco — SOMENTE via Lovable (sem terminal, sem curl, sem CLI)

**O Lucas NÃO tem acesso a terminal/curl/Supabase CLI pro backend.** Todo acesso ao banco e edge functions é feito **exclusivamente pela UI do Lovable** (o SQL Editor do Lovable em `lovable.dev/projects/.../view=cloud&section=sql`, ou o chat AI do Lovable). Confirmado em 2026-05-19.

**NUNCA sugerir ao usuário:**
- ❌ comandos `curl` (ele não tem `$SUPABASE_URL`/`$CRON_SECRET` no shell — falha com "No host part in the URL")
- ❌ `supabase` CLI, `psql`, ou qualquer ferramenta de terminal
- ❌ acessar o Supabase Dashboard direto em `supabase.com/dashboard` — o projeto real (`fzvklzpomgnyikkfkzai`) é gerenciado pela org do Lovable e o Lucas recebe "You do not have access". O projeto `lkotrsfdvnwxqyevhffh` que aparece na conta dele é um projeto-teste vazio, NÃO o de produção.

**Como o usuário roda QUALQUER coisa no backend:**
- **SQL (DDL/DML/migrations/queries)** → cola no SQL Editor dentro do Lovable → Run
- **Invocar edge function** → pedir pro chat do Lovable invocar, OU via `net.http_post` no SQL Editor (se pg_net + auth configurados), OU acontece automático via triggers/cron
- Pra dar instrução de SQL, sempre rotular: "🟣 Lovable → SQL Editor → cola → Run"

### Migrations Supabase — ⚠️ aplicação manual obrigatória

**Lovable Cloud NÃO aplica automaticamente** migrations que você commita em `supabase/migrations/`. Confirmado experimentalmente em 2026-05-17:

- Migrations geradas pelo Lovable (formato `_UUID.sql`, ex: `_868822bb-e38c-4fcf-8879-c64e48bd7630.sql`) rodam quando você usa o builder visual dele
- Migrations com nome custom (`_user_departments.sql`, `_dashboard_visits.sql`, `_enable_realtime_dashboard_v3.sql`) commitadas via PR **ficam só no repo** e não tocam o banco

**Workflow obrigatório quando criar migration custom** (automatizado pela skill `lovable-db-operator` — ver §12; use-a sempre que for mexer no banco, ela empacota todo este ritual):

1. Cria o arquivo em `supabase/migrations/YYYYMMDDHHMMSS_<nome>.sql`
2. Mergeia o PR normal (commit fica no histórico do código)
3. **Entrega o SQL inline na conversa** em blocos separados (1 bloco por migration), prontos pra colar no **SQL Editor** (não fica enviando o founder pro arquivo no GitHub Raw ou pedindo `bunx supabase db push`)
4. Founder cola cada bloco no SQL Editor → Run → confirma "Success"
5. **Valida no final** com uma query de checagem (count de tabelas/triggers/funções criados) também pra colar no SQL Editor

**Formatação de blocos SQL na conversa** (preferência do founder, registrada 2026-05-19):

- Sempre usar fenced code block com ```` ```sql ```` — assim o app de chat renderiza o botão de copiar no canto superior do bloco.
- **Cada bloco SQL deve terminar com a tag de fechamento ```` ``` ```` numa linha sozinha**, sem texto ou explicação encostada nela. Texto após `\`\`\`` fora do bloco. Isso garante que o botão "Copy" do app cubra a área visível inteira e funcione mesmo quando o bloco é longo (founder rola até o fim e quer poder copiar dali também sem voltar pro topo).
- Quando entregar múltiplos blocos sequenciais (A→B→C→D), **um bloco SQL por mensagem**, com label "BLOCO A/B/C/D" antes e a query de validação (`SELECT 'BLOCO X OK' AS status, ...`) no final do bloco. Founder cola, vê "Success" + a contagem esperada, confirma, e aí mando o próximo.

**Migrations já entregues por este workflow** (referência):
- Fundação Tier 1 (15 migrations, 2026-05-17 → 18) — todas coladas via SQL Editor
- A1 Inteligência de Caixa (4 migrations, 2026-05-19) — idem
- `20260517100000_enable_realtime_dashboard_v3.sql`, `20260517120000_user_departments.sql`, `20260517140000_dashboard_visits.sql`
- **Carteira-Omie Sub-PR B (2026-05-24, PR #263)**: `20260524170000_scores_unique_por_cliente.sql` (BLOCO A) + `20260524180000_carteira_scores_owner_e_filas.sql` (BLOCO B) — aplicadas via SQL Editor. Rollout coordenado concluído e validado em produção: BLOCO A→B → re-deploy das 5 functions (`calculate-scores`, `scoring/visit-recalc-client`, `scoring/visit-recalc-batch`) → invocar `calculate-scores` (6908) → BLOCO C (enfileira carteira) → drain concorrente do `visit-score-recalc-client` (fila 6908→0) → `scoring-recalc-batch` → BLOCO D. **Estado final: `farmer_client_scores` e `customer_visit_scores` = 6908 linhas, 1 por cliente, `farmer_id` = dono (Regina 1890); 3 donos; filas com UNIQUE(customer_user_id); 3 triggers de enfileiramento resolvendo o dono via `carteira_assignments`.** Crons noturnos `scoring-recalc-batch-nightly` (`0 6 * * *`) e `visit-score-recalc-batch-nightly` (`0 7 * * *`) ativos rodando o código novo (enumeram só ativos 30d mapeados pro dono). **Módulo de scoring agora cobre TODA a carteira do Omie (não só "quem teve atividade"); cobertura de férias na UI (/settings → Permissões).**
- **Carteira-Omie Sub-PR D — Positivação & KPIs (2026-05-25, PR #279)**: `20260525120000_positivacao_kpis.sql` (BLOCO A) — aplicada via SQL Editor. Adiciona `sales_orders.order_date_kpi` (data do pedido pra KPI, de `infoCadastro.dInc` no sync; antigos = `created_at`/previsão), tabela `carteira_positivacao_snapshot` (+RLS) e RPC **`get_minha_positivacao()`** (`SECURITY DEFINER`, `auth.uid()`, anti-IDOR — dona da verdade: elegíveis × pedido válido MTD + lista "a positivar"). Functions deployadas: `omie-vendas-sync` (EDIT, grava `order_date_kpi`) + `carteira-positivacao-snapshot` (CREATE, cron mensal idempotente). Cron `carteira-positivacao-snapshot-mensal` (`0 8 1 * *`) ativo. Validado em prod (carteira elegível da Regina ~1890). UI: tela `FarmerCalls` lidera com Positivação MTD + lista "Clientes a Positivar" (heros Farmer/Hunter), atividade do dia rebaixada. **Pedido válido = não-cancelado/não-draft; KPIs por POSSE própria (cobertura é só visibilidade); cortados da v1: Mix/Gap, UI histórica, Receita vs Meta.** **Programa Carteira-Omie (Sub-PRs A+B+D) 100% em produção.**
- **Hardening de RLS dos scores de carteira (2026-05-25, PR #329)**: `20260526020000_rls_score_carteira_hardening.sql` — aplicada e validada via SQL Editor (`✅`). **Furo:** `farmer_client_scores` tinha policy `FOR ALL` ampla (`master OR employee`) → qualquer vendedor lia E **gerenciava** (UPDATE/DELETE/roubo de posse via `onConflict`) a carteira de TODOS via PostgREST; filtro por `farmer_id` no front era só display. Novo helper **`pode_ver_carteira_completa(uid)`** = `master OR (employee AND commercial_role IN gerencial/estrategico/super_admin)` (exige `employee` p/ não vazar pra `customer` com role sujo). `farmer_client_scores`: split do `FOR ALL` → SELECT = gestor/master OU própria carteira+cobertura (reusa `carteira_visivel_para`); IUD = `farmer_id=auth.uid()` OU gestor/master. `customer_visit_scores` (já own-scoped): SELECT ganha cobertura+gestor (corrige quebra silenciosa de `useMyVisitSuggestions` p/ carteiras cobertas); write por carteira. Chamadas em `(select …)` → initPlan. `service_role` bypassa (engines intactos); as RPCs view-as `_for(target)` da main são `SECURITY DEFINER` → não afetadas. 2 consults codex no desenho. ⚠️ **Timestamp realocado de `20260525220000`→`20260526020000`** (colidia com `20260525220000_viewas_access_targets.sql` de sessão paralela). **Follow-up de segurança das 5 tabelas-irmãs ENTREGUE (PR #340)** — ver bullet abaixo.
- **Hardening de RLS das 5 tabelas de relacionamento da carteira (2026-05-26, PR #340)**: `20260526040000_rls_carteira_relacionamento_hardening.sql` — aplicada e validada via SQL Editor (`✅`). Fecha a mesma exposição `FOR ALL master-OR-employee` (BFLA) do #329 em **`farmer_recommendations`, `farmer_bundle_recommendations`, `farmer_calls` (transcrição de ligações), `route_visits` (geo; posse via `visited_by`, NÃO `farmer_id`), `farmer_copilot_sessions`**. Split do `FOR ALL` → SELECT = gestor/master OR **own** (`farmer_id`/`visited_by`) OR carteira/cobertura (`carteira_visivel_para`); IUD = gestor/master OR own (assimetria leitura>escrita: cobertura lê, não muta). **Branch own no SELECT (≠ scores)**: aqui a coluna de posse é o criador *client-side* e o cliente vem de `sales_orders`, não de `carteira_assignments` (o `INSERT WITH CHECK` força `own=auth.uid()` → o branch own não vaza linha alheia); `calls`/`copilot` têm `customer_user_id` **nullable** → `carteira_visivel_para(NULL)=só master`, então o branch own garante leitura das próprias ligações/sessões sem cliente vinculado (subsume a velha "Farmers can view their own calls"). `pode_ver_carteira_completa` redeclarado verbatim (idempotente, bate 1:1 com o #329) p/ paste standalone; `TO authenticated`; `(select …)` → initPlan; `service_role` bypassa (engines de scoring intactas). 1 consult codex. Consumidores mapeados POR tabela antes de apertar (escritas client-side own-scoped; leituras amplas das tabs Intelligence são gestor/master-gated = `pode_ver_carteira_completa`; route planner `lastVisits` degrada honestamente fora-da-carteira). ⚠️ **Timestamp realocado `20260526020000`→`20260526040000`** (colidia com #329/scores + watchdog mergeados em paralelo).
- **"Ver como pessoa" — impersonação read-only master (2026-05-25, PRs #323 + #331)**: 3 migrations via SQL Editor — `20260525210000_viewas_rpcs_for.sql` (internals `_carteira_{mixgap,positivacao}_for_owner` + RPCs de vendedor refatoradas pra delegar + irmãs **master-only** `get_meu_mixgap_for`/`get_minha_positivacao_for` — **Pattern B** do Codex: gate `RAISE` no forbidden, internals `REVOKE`d de `authenticated`, contrato das RPCs de vendedor preservado); `20260525220000_viewas_access_targets.sql` (`get_user_access_profile_for` + `list_impersonation_targets` = donos de carteira, master-only); `20260525230000_impersonation_audit.sql` (tabela + `log_impersonation_start`/`end_impersonation`, `actor=auth.uid()` server-side, LGPD). **Sem edge function.** Client: `ImpersonationContext` (master-only, `sessionStorage`, `effectiveUserId` SÓ em leitura — a sessão continua a do master, todo write é do master; guard de CI `no-write-leak` por allowlist), hooks `useMy{Positivacao,MixGap,VisitSuggestions,CarteiraScores}` impersonation-aware (na impersonação escopam só à carteira do alvo, ignoram a cobertura do master), `ViewAsPicker` em `/meu-dia` (MasterDashboard) + **banner persistente** "Vendo como X — somente leitura · Sair" + CTA "Nova ligação" desabilitado. **#331:** `useEffect` restaura a impersonação do `sessionStorage` quando `isMaster` confirma (sem isso, F5 perdia — `isMaster` é false no mount). **Escopo data-only:** o **#221 (acesso por persona) foi FECHADO**, então a **camada de MENU está adiada** (impersona troca os DADOS; o menu segue o do master); `get_user_access_profile_for` + `useImpersonatedAccessProfile` ficam prontos pra quando uma fundação de acesso existir. **Codex review do gate: limpo.** Smoke em prod OK (picker/banner/audit; SPA preserva a sessão; RLS #329 não afeta — master lê tudo). **Não-objetivos v1:** impersonar cliente, gestor impersonar, agir como o alvo. Specs/planos em `docs/superpowers/{specs,plans}/2026-05-25-ver-como-persona-*`.
- **Lente "Ver como pessoa" — camada de EXIBIÇÃO: menu + rotas + telas internas (2026-06-06, PRs #653 Fase 1 + #659 Fase 2, na main; ⚠️ pendente Publish no Lovable):** estende a impersonação data-only acima pro que o **#221 adiou** — agora a lente reflete o **menu lateral, as rotas e as telas internas** como o ALVO vê (não só os dados). Pedido do founder: ver o app como cada vendedora pra **reproduzir bugs que elas reportam**. Fonte única **`useDisplayAccess()`** (`src/hooks/useDisplayAccess.ts`): sem lente espelha o real (`useAuth` + sales-only + commercial-role + dept); na lente deriva do perfil do ALVO (`get_user_access_profile_for` via `useImpersonatedAccessProfile`). ⚠️ **`useAuth()` continua SEMPRE real** (sessão/escrita/identidade/RLS do master); `display*` é SÓ exibição/navegação — **nunca** decidir escrita/identidade com `display*` (use `useAuth` real; ver guardrail). **Write-guard** (Proxy no client supabase, `src/lib/impersonation/lens-write-guard.ts`): na lente bloqueia mutação — PostgREST insert/update/upsert/delete + storage + `functions.invoke` + **RPC mutante** (libera só `get_`/`list_` + allowlist explícita p/ leitura PT, ex. `listar_pedidos_a_separar`); é *thenable rejeitado encadeável* (telas de leitura que mutam no mount degradam sem crashar). **NÃO é barreira de segurança** (o servidor ainda autoriza o master) — é lente de inspeção + defesa em profundidade; a lente é **master-only** (`isImpersonating` só é true pro master). **Fase 1 (#653):** AppShell (menu/badges suprimidos), `RequireStaff`/`RequireFinanceiroAccess`, persona/ordem de cards lêem `display*`; banner honesto; chip "Ver como: pessoa" (aposenta o seletor manual de papel); placeholder "Carregando visão…"; auto-saída + toast se o perfil do alvo falhar; auditoria de callsites (write-identity fica real — `CoveragePanel`/`AdminStandardProcessDetail`). **Fase 2 (#659):** `useMyCommercialRole` lente-aware → `/meu-dia` mostra o dashboard da Farmer + `FarmerCalls` reflete o alvo (2 consumidores display herdam, não mudam); CTAs de outcome (`AcaoOutcomeMenu`/`OutcomeMenu`) `disabled` na lente; **Dialer bloqueado na FONTE** (`WebRTCCallContext.makeCall` + `acceptIncoming` com `isLensActive()` — WebRTC fura o write-guard, **P1 achado pelo review opus**: gatear só o `WebRTCDialer` deixava ≥3 callsites — `AgendaTodayList`/`Telefonia`/`FarmerCalls` — disparando ligação real). **Bookkeeping da lente** (`log_impersonation_start`/`end_impersonation`, RPCs mutantes) roda no **`supabaseUnguarded`** (client SEM guard, em `client.ts`, **contido por guardrail de CI** — só importável no client + ImpersonationContext). **Guardrails de CI** (`src/lib/impersonation/__tests__/`): `no-write-leak`, `display-access-no-write`, `unguarded-client-contained`, `lens-write-guard`. **Limitações conhecidas (não-bugs):** `RequireCaca` (`/caca`) alcançável por deep-link na lente — autoriza por `isMaster` REAL (benigno, não escala); `ViewAsPicker` só no MasterDashboard (some quando a lente mostra dashboard não-master — sair pelo banner). Specs/planos em `docs/superpowers/{specs,plans}/2026-06-0{5,6}-lente-*`.
- **Lente "Ver como" nas telas de ATIVIDADE do vendedor + KPIs de nível mundial da farmer (2026-06-06, em prod):** complemento da camada de exibição (#653/#659) — o founder, na lente como vendedora, viu **dados do MASTER** vazando e KPIs que não eram do papel dela. **(A) Leitura escopada ao ALVO:** telas que liam atividade por `useAuth().user.id` (SEMPRE o master) vazavam na lente. Padrão de fix: **leitura→`effectiveUserId`** (entra na allowlist do guardrail `no-write-leak`); **escrita/geração→`disabled={isImpersonating}` + write-guard** (o RLS das tabelas de carteira #329/#340 tem branch gestor/master → o master LÊ os dados do alvo, então o fix de front basta; fora da lente `effectiveUserId === user.id` = byte-equivalente). ✅ **Feitos:** **Telefonia** ([#671](https://github.com/LucasSardenbergL/afiacao/pull/671): `call_log` histórico + badge de perdidas + aba "Time" via `useDisplayAccess`/`useIsTelefoniaManager`), **Meu Dia** ([#678](https://github.com/LucasSardenbergL/afiacao/pull/678): `useMyKpis`/`useKpisVisita`/`useFollowupsVisita`/`useMinhasVisitasResultado`/`FarmerCalls.loadCallLogs`/`useFarmerScoring` [este é engine: leitura segue o alvo mas o **upsert de scores é PULADO na lente** por `isImpersonating` — o master inspeciona, não recalcula a carteira do alvo]), **lista de ligação** (`useRouteContactList`) e **Clientes** (`useClientesScope`/`escopo-clientes`/`useAdminCustomers`) — estas duas por sessões paralelas. ✅ **Engines de IA — FEITO (2026-06-13, [PR #796](https://github.com/LucasSardenbergL/afiacao/pull/796) parte 1 + [PR #799](https://github.com/LucasSardenbergL/afiacao/pull/799) parte 2, na main; ⚠️ requer Publish):** as 7 engines mapeadas viraram lente-aware pelo mesmo rito (leitura-de-exibição→`effectiveUserId` na allowlist + ação-de-gerar→`disabled={isImpersonating}`). **#796:** `useCrossSellEngine` (lê os scores do ALVO p/ inspeção, NÃO cai no fallback super-admin e **PULA o upsert** na lente, igual `useFarmerScoring`), `useFarmerExperiments` (loadExperiments→alvo; criar/iniciar/medir/cancelar disabled), `useDiagnosticQuestions` (getEffectivenessStats→alvo), `FarmerCallsPendingLink` (lista pendente→alvo; "Vincular" disabled). **#799:** `useTacticalPlan` (loadPlans/getActivePlan/getEffectivenessStats→alvo; gerar/recordResult disabled), `useFarmerTacticalPlan` (dropdown loadCustomers→alvo, recarrega na troca de lente), `useFarmerCopilot` (dropdown→alvo; "Iniciar sessão" disabled via `isImpersonating` exposto pro `SessionStartCard`). Off-lens `effectiveUserId === user.id` → byte-idêntico, zero regressão; o write-guard já bloqueia toda mutação na lente (o disabled é só UX honesta). Teste-padrão: `lens-atividade-vendedor.test.tsx` + os novos `lens-engines-ia.test.tsx`/`lens-tactical-plan.test.tsx` (stub captura o `.eq()` de cada hook → o filtro segue o alvo na lente, o próprio usuário fora); guardrail `no-write-leak` estendido. ⏳ **Pendente (follow-up): fluxo de BUNDLES** (`useFarmerBundles`/`useBundleEngine`/`useBundleArguments`) ainda exibe dados do master na lente — engine fora dos 7 acima; tornar lente-aware (incl. disabled dos botões de gerar argumento/pergunta no `BundleCardFull`) é PR próprio. **(B) KPIs de nível mundial da farmer** ([#690](https://github.com/LucasSardenbergL/afiacao/pull/690)): o `/meu-dia` roteia por `CommercialDashboard`→`useMyCommercialRole` (lente-aware); o `FarmerDashboardV2` montava cards ad-hoc (o "Visitas de hoje" vazava p/ farmer só-ligação; positivação não estava em dashboard nenhum). Agora **lidera com o placar de positivação** (`PositivacaoHero`, lente-aware via `useMyPositivacao`) + **REMOVEU `VisitasHojeCard`**; `useMyPositivacao` passou a expor `receitaMtd` (o dado já vinha da RPC); `PositivacaoHero` (farmer) ganhou **Receita MTD** + **Win-back/Recuperados**. **KPIs classificados pro OTE futuro:** comissionáveis (output) = **Positivação MTD %** [⭐NORTE] · Receita MTD · Win-back; higiene (atividade/leading, medido mas NÃO pago direto) = Cobertura MTD % · Ligações hoje; acionáveis (guiam a fila) = clientes a positivar/recência crítica/mix-gap. Anti-gaming: positivação em **%** (carteira inchada não infla) + atividade não-comissionada (não premiar volume vazio). Spec `docs/superpowers/specs/2026-06-06-kpis-farmer-meu-dia-design.md`. ⏳ **Pendente: expandir hunter/closer/master** (mesmo rito — a frente "começar pela farmer e expandir"). ⏳ **Sessão de OTE (remuneração variável)** — 3 achados da auto-revisão adversária a incorporar quando modelar a comissão: (1) **piso de valor na positivação** (hoje "positivado"=comprou qualquer valor → pedido de R$1 não deve contar p/ comissão; usar ticket mínimo ou ponderar por valor); (2) **NRR-like / retenção de valor** (positivação mede SE comprou, não SE caiu — cliente que despencou de R$10k→R$1k conta como "positivado"; medir receita da carteira vs período anterior); (3) **win-back comissionável com salvaguarda** (peso baixo + a **retenção contínua** deve valer mais que recuperar — senão incentiva "deixar o cliente cair pra recuperar depois"). ⚠️ **Codex adversarial NÃO rodou nesta entrega** (usage limit do Plus + flaky na janela: 5 tentativas = 1 timeout + 2 usage-limit + 2 prompt-vazio — "Caminho B"; fiz auto-revisão adversária no lugar → os 3 achados acima). KPIs são por-farmer e auditáveis (base de OTE). 100% frontend (sem migration/edge); cada uma das 3 entregas exigiu Publish no Lovable.
- **Clientes não-vinculados — relatório master/gestor (2026-05-27, PRs #372 + #383 + #402 + #413, em produção)**: tela `/admin/clientes-nao-vinculados` (seção Gestão, gated `gestorComercialOuMaster`) que lista os clientes que existem no **Omie** mas **não têm conta no app** — alvos pra convidar/criar conta. **Definição correta de "não-vinculado"** (o que adiou o B antes): código Omie **∉** `omie_clientes` (vínculo) **E** documento **∉** `profiles` (checar os DOIS conjuntos; senão um cliente que TEM profile mas ainda não ganhou linha em `omie_clientes` viraria falso não-vinculado = relatório enganoso). Migration `20260527120000_clientes_nao_vinculados.sql` (BLOCO A): tabelas `omie_clientes_nao_vinculados` (snapshot) + `omie_nao_vinculados_state` (1 linha/empresa: status/last_complete_synced_at/total/error); RLS read = `pode_ver_carteira_completa(auth.uid())`, escrita só `service_role`; view **`v_clientes_nao_vinculados_atual`** (`security_invoker`, JOIN `synced_at = last_complete_synced_at` → UI lê só o último run COMPLETO) + RPC **`finalize_nao_vinculados_snapshot`** (transacional: delete-stale + state=complete, idempotente). Edge `omie-analytics-sync`: action async **`start_nao_vinculados`** (`EdgeRuntime.waitUntil` + 202; gate master/gestor **server-side** via `pode_ver_carteira_completa` quando `auth.via='staff'`; valida `account='vendas'` p/ não cair no fallback de credenciais; guard de 15 min "already_running"). **Refresh:** botão Atualizar (on-demand) **+ cron noturno** `nao-vinculados-refresh-diario` (`30 8 * * *`, `net.http_post` do `start_nao_vinculados` com `timeout_milliseconds:=60000` — o 202 do waitUntil volta rápido, o run segue em background; migration `20260527250000`, #413). **Export CSV** (botão → `useExportNaoVinculados` pagina TODAS as ~10.485 linhas via `.range` ordenado por `omie_codigo_cliente` p/ paginação estável + helper puro TDD `toCsv` em `src/lib/clientes-nao-vinculados/csv.ts`, delim `;` + BOM p/ Excel pt-BR; evento `carteira.nao_vinculados_export`). **Cabeçalho mostra `state.total` (10.485), não a lista capada em 1000** (#402 — a query da view bate o cap do PostgREST; rótulo "mostrando os primeiros 1.000"). Helper puro TDD `classifyClienteForSnapshot` + `buildNaoVinculadoRow`/`accountToEmpresa` em `src/lib/clientes-nao-vinculados/snapshot.ts`, espelhados verbatim no edge. ✅ **Validado em prod (2026-05-27): `status=complete`, `total=10485` não-vinculados do Oben, view batendo 10485.** ⚠️ **LIÇÃO-CHAVE (o smoke de prod pegou):** a 1ª versão (#372) pegava **carona no `syncCustomers`** (laço de linking com ~2 queries PostgREST POR cliente × ~10k + ~6900 updates de vendedor) → **estourou o budget do edge** (run preso em `status=running`, nunca `complete`; o `error_message=NULL` + `running` provou que o runtime MATOU a função antes do `catch` = timeout, não erro). Degradação foi honesta (a UI nunca mostrou run incompleto — lê só `last_complete_synced_at`). Fix (#383): **rotina DEDICADA e desacoplada `syncNaoVinculados`** que NÃO toca no linking (`syncCustomers` revertido ao original → zero risco no money-path): **2 leituras em massa** (`fetchAllOmieClienteCodigos` + `fetchAllProfileDocs`, paginadas via `.range(from, from+999)` p/ furar o cap de 1000 do PostgREST → ~13 reads, não ~20k) → classifica em memória → bulk insert → finalize. **Padrão:** quando precisar diffar uma lista grande do ERP contra o banco, NÃO faça query-por-item num sync quente; carregue os conjuntos em massa e classifique em memória. ⚠️ **Lições de rollout (multi-sessão):** (a) **deployar o edge SÓ depois do PR mergear** — deployar "da main" antes do merge pegou a main velha → o trigger respondeu `400 "Ação desconhecida"` (action inexistente no binário); (b) **treadmill dos arquivos de audit** (`docs/migrations-audit.md` + `scripts/audit-custom-migrations.sql` conflitam a CADA PR de migration que mergeia) → quando o PR **não** adiciona migration, resolver com `git checkout --theirs` nesses 2 (a main é autoritativa), sem regenerar; (c) **o gstack `/browse` (headless E headed) NÃO renderiza esta SPA** (Chromium controlado + script injetado `~flock.js` → React não monta, só o spinner do `index.html`; assets servem 200, zero erro no console) — smoke de UI fica no Chrome real do founder; verificação de maior sinal é o **backend** (a view é a fonte de dados da tela). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-{26,27}-clientes-nao-vinculados*`.
- **Tarefas — cobrança de atividades das vendedoras (Fase 1) (2026-05-31, PR #545, em produção)**: founder atribui tarefas a vendedoras tied a um cliente (categorias ligar/oferecer/preco/whatsapp/outro); a vendedora vê de forma **saliente** na Meu Dia (card no topo + badge na sidebar), o app dá **baixa automática** quando há prova determinística e **propõe + 1 toque** quando só detecta menção na transcrição; vencendo + passada a tolerância sem cumprir/confirmar → **e-mail de cobrança** ao founder (via `fornecedor_alerta` → `dispatch-notifications`). **Escada de certeza** (decisão central, codex): determinístico fecha / inferido propõe / invisível manual — sugestão da IA sozinha **nunca** apaga nem dispara a cobrança. **6 migrations via SQL Editor** (`20260528130000`..`20260528134000` = BLOCO A–E: tabelas `tarefas`/`tarefa_satisfacao_candidatos`/`tarefa_eventos` + view **`v_tarefas_estado`** fuso `America/Sao_Paulo` + RLS 7 policies reusando `pode_ver_carteira_completa`/`carteira_coverage` + funções cron + CHECK de `fornecedor_alerta` estendido c/ `tarefa_atrasada` + seed `company_config` backstop 7d/tolerância 1d) + **`20260528135000_tarefas_matcher_created_at_floor.sql`** (FIX da revisão adversária codex: matcher só satisfaz tarefa com interação **posterior à criação** — senão ligação/visita de rotina anterior dava auto-baixa falsa). **Motor 100% SQL puro + pg_cron, SEM edge function nova**: `tarefas_matcher_tick()` (cron `tarefas-matcher-15min`, casa interação→fecha/cria candidato, idempotente via UNIQUE+`status='aberta'`) + `tarefas_escalonamento_tick()` (cron `tarefas-escalonamento-diario` `0 21 * * *` = 18h BRT, agrupa por responsável efetivo×empresa→`fornecedor_alerta`, `escalado_em` fire-once). Crons = chamadas SQL **locais** (sem `net.http_post` → sem armadilha de timeout). **Modos de prazo:** data fixa OU próxima interação (ligação/visita/entrega) c/ **backstop** automático (7d, fixo na criação). **Adiar com motivo** (snooze anti-cobrança-injusta) + **ciente de férias/cobertura** (`carteira_coverage` → responsável efetivo). WhatsApp = botão dentro da tarefa (toque = baixa, origem `whatsapp`; `wa.me` só abre, não há envio rastreável → honesto sobre o nível de evidência). Frontend: `src/lib/tarefas/` (helper WhatsApp + categoria→auto_satisfy, TDD) + `src/hooks/useTarefas.ts` + `src/components/tarefas/` (card Meu Dia + dialog criação em lote) + `src/pages/Tarefas.tsx` (lista do founder "tarefas que criei" + picker de cliente Omie, gated `isMaster||isGestorComercial`) + badge AppShell + rota `/tarefas`. Execução por subagentes (subagent-driven), CI verde (typecheck+strict+test+build+lint). **3 consults de design + 1 revisão adversária de implementação (codex).** ⚠️ **Lição de merge (multi-sessão):** o auto-merge da main **engoliu silenciosamente** 2 coisas (import `ClipboardList` no AppShell + um card no `FarmerDashboardV2`) — pegas só pelo `tsc -p tsconfig.app.json` **ANTES do push** (typecheckar o tree mergeado é obrigatório, não opcional). **Fast-follows (não bloqueiam):** (1) **texto do e-mail** — conferir/ajustar o template do `dispatch-notifications` pro tipo `tarefa_atrasada` (edge function, via chat do Lovable); (2) **editar tarefa** (cancelar já existe) reusa o dialog em modo edição. **Fase 2 (schema preparado, NÃO construída):** tarefas recorrentes + trava de comprovação por foto (`template_id`/`requer_comprovacao`/`comprovacao_url` já na tabela). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-28-tarefas-cobranca-vendedoras*`.
- **Afiação OS — sincronização de andamento → etapa da OS no Omie (Colacor SC) (2026-06-05, PR #630)**: `20260605140000_afiacao_os_status_sync.sql` — aplicada e **validada via SQL Editor** (`tem_fila=1, colunas_4=4, deve_ser_20=20, deve_ser_null=NULL, tem_trigger=1, tem_cron=1`). ⚠️ **CORREÇÃO DE PREMISSA (pra não re-investigar):** a integração afiação→Omie **JÁ EXISTIA** — a edge `omie-sync` action `sync_order` (staff-only, chamada pelo wizard de pedido unificado em `src/services/orderSubmission/submitOrder.ts`) **cria a OS na Colacor SC** (`OMIE_COLACOR_SC_APP_KEY`, `servicos/os/IncluirOS`, grava em `omie_ordens_servico`, insere a linha em `orders`). O gap era só que a **ETAPA** (`cEtapa`) da OS nunca sincronizava depois (o `alterarOrdemServicoOmie`/action `update_order` existiam mas DORMENTES, sem caller). Esta entrega liga o sync de **andamento** (abordagem B): trigger `AFTER UPDATE OF status ON orders` (`afiacao_os_enqueue`) → enfileira **só se a etapa-alvo mudou** em `afiacao_os_sync_fila` (upsert por `order_id`) → cron `afiacao-os-sync */5` (`afiacao_os_sync_kick`, Vault + `net.http_post` + `timeout_milliseconds`, idle-cheap) → action nova **`sync_os_status`** na `omie-sync` (gate `via='cron'/'service_role'`) drena a fila e chama **`alterarEtapaOS` status-only** (`Cabecalho.cEtapa` SÓ; NÃO reenvia serviços → preserva ajustes manuais na OS dentro do Omie; `alterarOrdemServicoOmie`/`update_order` **intactos**). **Mapa** `mapear_status_etapa` (helper puro `src/lib/afiacao/os-etapa.ts` TDD, espelhado **verbatim** em SQL e na edge): `pedido_recebido/aguardando_coleta/orcamento_enviado/aprovado`→`10` Aberta, `em_triagem/em_afiacao/controle_qualidade`→`20` Em andamento, `pronto_entrega/em_rota`→`30` Aguardando faturamento, **`entregue`→NULL = mantém** (decisão do founder: NÃO marca "Faturada"/`50` — faturamento/fechamento são manuais no Omie; o app só empurra o andamento operacional e nunca sobrescreve etapa pós-entrega; aprovação do cliente é `10`→`10` = não enfileira). Idempotência via `omie_ordens_servico.last_etapa_sincronizada` (+ `last_status_sincronizado/last_sync_at/last_sync_error`); backoff exponencial (teto 30min, 6 tent→`erro_atualizacao`). Review de qualidade (subagente) fechou 2 falhas silenciosas: erro transitório de leitura do `orders` apagava o item da fila (→ vira backoff recuperável) e `.maybeSingle()` na busca da OS era poison-pill com `order_id` duplicado (→ `.order(created_at desc).limit(1)`). 1 consult codex (mecanismo B + os 2 fixes). ✅ **Edge `omie-sync` redeployada verbatim via Lovable (2026-06-05, commit `76cf9927`)** — `omie-sync/index.ts` idêntica ao merge (diff vazio; o Lovable só regenerou `types.ts` com a tabela/colunas novas). **Feature 100% no ar** (migration + edge + cron `afiacao-os-sync */5`); resta só o QA opcional (mover um pedido de teste no kanban e conferir a etapa da OS no Omie da Colacor SC, cruzando com `omie_ordens_servico.last_etapa_sincronizada`). **Probe pós-deploy:** `alterarEtapaOS` manda só `Cabecalho` (update parcial); se o Omie exigir payload completo, contingência = `ConsultarOS` → patch só `cEtapa` preservando os serviços do Omie. **Fast-follow opcional NÃO feito:** watchdog check `afiacao_os_sync` no `data_health_watchdog` (Task 5 do plano — alerta se uma OS ficar dessincronizada). Specs/plano em `docs/superpowers/{specs,plans}/2026-06-05-afiacao-os-status-sync*`.

- **Canal "Melhorias" — sugestões/problemas dos funcionários com triagem por IA (2026-06-10/11, [PR #750](https://github.com/LucasSardenbergL/afiacao/pull/750), em prod; ⚠️ requer Publish)**: qualquer staff reporta problema/sugestão/pergunta pelo botão 💡 no **topbar** (dialog captura rota de origem + empresa ativa sozinho); a edge **`melhoria-triagem`** (Anthropic direto `claude-sonnet-4-6`, forced tool-use + caching, gate `authorizeCronOrStaff` só-staff) **tria NA HORA** (tipo problema/sugestao/pergunta · urgência · módulo · título · resposta cordial · `avaliacao_founder` técnica) e, quando a pergunta casa, responde com DADOS REAIS via 2 RPCs determinísticas `SECURITY DEFINER` JWT-scoped: **`melhoria_clientes_por_produto`** (quem comprou em 12m — pedido válido padrão #279 + `deleted_at is null`; visibilidade por carteira: `pode_ver_carteira_completa` OR `carteira_visivel_para`) e **`melhoria_produtos_relacionados`** (mesma família + `farmer_association_rules` com confidence/lift — cobre "tirar produto X de linha: quem compra + o que oferecer"). **Princípios**: captação NUNCA depende da IA (INSERT antes do invoke; falha → `triagem_status='pendente'` + botão re-triar na fila); IA nunca fabrica número (a UI renderiza a TABELA CRUA do jsonb `dados`; prosa instruída a não repetir); **anti-vazamento de carteira**: tools de dados SÓ quando caller==autor (re-triagem por master de item alheio classifica SEM tools — senão a tabela nasceria com a visão completa do master visível pro autor restrito); mapa explícito tool→RPC (nome alucinado nunca executa); `disable_parallel_tool_use` (senão o caso canônico "quem compra + substituto" dava 400). Tabelas `melhoria_itens`/`melhoria_mensagens` (migration `20260610130000`, aplicada+validada): RLS own-OR-master; INSERT staff não pré-popula campos da IA; **`dados` jsonb é EXCLUSIVO do service_role** (humano nunca grava a "tabela de evidência"); réplicas cap 6 msgs (app-level + re-validação na edge); sem DELETE (descartar é status). Front: `/melhorias` (meus itens + thread + réplica; **"meus" filtra autor EXPLÍCITO** — RLS sozinha mostraria tudo pro master) e `/gestao/melhorias` (fila master: filtros `useUrlState`, urgência+data, **"Copiar prompt pro Claude Code"** montado client-side por `montarPromptClaudeCode` — determinístico, com o relato delimitado como dado não-confiável e fence escapado anti prompt-injection) + badge sidebar master. Helpers TDD `src/lib/melhorias/` (22 testes) espelhados verbatim na edge. ⭐ **PG17 com FALSIFICAÇÃO** (`db/test-melhorias-rpcs.sh`): além dos asserts, sabotei o gate staff de propósito → o teste TINHA QUE CAIR e não caía (nenhum assert cobria customer-INSERT; catch-alls `WHEN OTHERS→OK` engoliam tudo) → assert A6c2 + re-raise adicionados; agora gate sabotado derruba o teste. **LIÇÃO: teste SQL negativo com `WHEN OTHERS THEN 'OK'` é teatro — capture a SQLSTATE esperada e re-lance o resto; e prove o teste por falsificação (sabotar a migration e exigir vermelho).** Codex 4× esgotado (janela rolante Plus) → Caminho B (passe adversarial próprio achou o P1 do vazamento na re-triagem; 2-stage review por task; review final de integração achou 2 Important corrigidos). ✅ **Adversarial retroativo do Codex RODADO (gpt-5.5, 2026-06-11, [PR #753](https://github.com/LucasSardenbergL/afiacao/pull/753)): carteiras CONFIRMADAS sem P1 de vazamento** (cada cliente filtrado por `carteira_visivel_para`; re-triagem master sem tools). 2 P1 de ameaça INTERNA fechados: **(P1-A) prompt injection "lavada" pela IA** — o escape de fence só cobria a thread crua, mas `avaliacao_founder`/`titulo`/`rota_origem` (SAÍDA da IA influenciada pelo relato) entravam no prompt copiável FORA do bloco; a IA podia "lavar" instruções do funcionário pro `avaliacao_founder`, que vira "avaliação técnica confiável" no Claude Code do founder → fix `neutralizar()` escapa toda crase em TODOS os campos derivados do funcionário + `avaliacao_founder` em bloco delimitado + system prompt da edge instrui a tratar relato como dado; **(P1-B) re-triagem sem limite** (DoS de custo) — o cap conta mensagens do funcionário mas a edge re-triava o MESMO item infinito por invoke direto (LLM+RPCs) → fix idempotência: última msg já é da IA + `triagem_status='ok'` → no-op barato (fecha tb invokes concorrentes/duplo-clique). **LIÇÃO: escape de fence/delimitação tem que cobrir TODO campo derivado do usuário — inclusive a SAÍDA do LLM, que "lava" a entrada não-confiável pra dentro de campos que parecem confiáveis.** ⚠️ #753 requer **re-deploy da edge** + Publish. Specs/planos `docs/superpowers/{specs,plans}/2026-06-10-melhorias-canal-feedback*`.
- **Programa UX da Farmer — home por persona + Web Push de verdade (2026-06-10/11, PRs [#734](https://github.com/LucasSardenbergL/afiacao/pull/734) + [#736](https://github.com/LucasSardenbergL/afiacao/pull/736) + [#746](https://github.com/LucasSardenbergL/afiacao/pull/746), em prod e publicado)**: origem = founder na lente "Ver como tatyanamartins2002" estranhou a home (cockpit de 6 módulos) e o menu da farmer. **Diagnóstico:** `Index.tsx` decidia só por `isStaff` binário (a persona `vendedor` do `PERSONA_CONFIG` apenas REORDENA as 6 zonas, não esconde) e a regra sales-only do AppShell escondia toda seção `!= 'Vendas'` → o **Meu Dia (`/meu-dia`) e Clientes eram INALCANÇÁVEIS pelo menu da vendedora**. **(1) Home por persona (#734):** helper TDD **`src/lib/nav/home-por-persona.ts`** (`resolverHomeStaff`: farmer/hunter/closer/operacional OU sales-only → `Navigate /meu-dia`; gestão/master/staff genérico → cockpit; gate `displayIsStaff` anti-loop na lente + skeleton até o cargo resolver anti-flash) + **menu sales-only por allowlist de ITEM** (`itemVisivelParaSalesOnly` = seção `SECAO_VENDAS` + `/meu-dia` + `/admin/customers`, aplicado nos 2 navs) + seção Vendas reordenada pelo fluxo do dia + badge SLA WhatsApp religado pra sales-only (era excluída do PRÓPRIO SLA pelo `enableStaffPolls`) + **rename `managerOnly`→`staffOnly`** (a flag sempre significou "qualquer staff"). **(2) Web Push (#736 + card master #746):** tabela `push_subscriptions` (RLS own-only) + **RPCs device-aware** `upsert_push_subscription`/`delete_push_subscription` (SECURITY DEFINER: o endpoint pertence a QUEM ESTÁ LOGADO AGORA — P1 da adversarial: device compartilhado vazava push da vendedora anterior; + `limparPushDoDevice()` no `signOut`, vive em `src/lib/push/device.ts` anti-ciclo de import) + **3 produtores SQL→pg_net→edge best-effort blindados** (whatsapp inbound: dona via `wa_owner_efetivo`, throttle 10min/conversa, stop-keyword fora, corpo SEM texto da msg [LGPD], só `sender_user_id IS NULL` [anti-amplificação]; tarefa nova: `assigned_to`, throttle 2min, pula auto-atribuída; `push_sla_tick` cron `*/15` com **gate de expediente** [anti re-push overnight: minutos úteis CONGELAM fora do expediente] + exception POR dona) + edge **`enviar-push`** (`npm:web-push@3.6.7` FUNCIONA no Deno do Supabase, gate `authorizeCron`, TTL 1h, 404/410→delete, espelho verbatim de `src/lib/push/payload.ts`) + SW handlers `public/push-sw.js` via **`workbox.importScripts`** (generateSW preservado; ⚠️ **editar o push-sw.js = RENOMEAR pra push-sw-v2.js** — importScripts passa pelo HTTP cache) + card `AtivarNotificacoesCard` no FarmerDashboardV2 E MasterDashboard (#746, founder testa no próprio device). VAPID public **hardcoded** em `src/lib/push/vapid.ts` (é pública); private = secrets `VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`. **PG17 17 asserts** (`db/test-push-vendedora.sh`: throttles, cobertura de férias, reatribuição de endpoint, best-effort sem secret). Migration `20260610200000_push_vendedora.sql` aplicada+validada; edge deployada (Lovable commitou na main byte-idêntica = deploy verbatim provado); smoke do runtime OK (200 `sem subscriptions`). ⚠️ **Smoke no DEVICE pendente** — e **o card de push NUNCA aparece no preview do Lovable** (VitePWA desligado no preview → `serviceWorker.ready` pendura → status `carregando` eterno): smoke SÓ no domínio publicado `steu.lovable.app`. **(3) Cold-start dos dados da farmer (diagnóstico 2026-06-11 — ✅ backfill FEITO, ver "Pendências" no fim):** o Meu Dia da Tatyana nasce zerado porque `sales_orders` tem pedido de só **10/571** clientes da carteira (lifetime!) enquanto o score (via `order_items` do analytics) sabe de **102 compradores <90d** — o `sync_pedidos` roda janela rolante 5d e NUNCA houve backfill histórico; vínculo `omie_clientes` = 571/571 PERFEITO (não é gap de cadastro). **Plano provado:** `sync_pedidos` aceita `date_from`/`date_to` (DD/MM/YYYY → `filtrar_por_data_de`) + `start_page`/`max_pages`; retorno `{totalSynced,totalPaginas,nextPage,complete,skippedNoClient,skippedExisting}`; idempotente por hash → backfill 12m da Oben em levas de 8-15 págs via `net.http_post` (timeout 145000) + depois recompute scores. **TRAVADO no incidente de plataforma de 11/06** (abaixo); retomar com sonda `max_pages=1`. ⚠️ **LIÇÕES:** (a) **503 `LOAD_FUNCTION_ERROR` intermitente no boot de edge = incidente de PLATAFORMA Supabase** (2026-06-11 atingiu `omie-vendas-sync` E `omie-financeiro`; sintomas no `net._http_response`: timeouts em cascata, até DNS de 60s; o redeploy verbatim cura o boot mas REGRIDE se a plataforma segue doente → parar de martelar e esperar estabilizar); (b) **diagnosticar pg_net SEMPRE com `error_msg`/`timed_out`** (`status_code NULL` é mudo no leitor simples; o timeout é de quem ESPERA — a edge pode nem ter bootado); (c) o retorno da `omie-vendas-sync` NÃO carrega `action` no JSON (filtrar `net._http_response` por campo único do payload, ex. `skippedNoClient`). **Pendências (revisão 2026-06-29):** resta SÓ o **smoke do push no device** (testar em `steu.lovable.app`). ✅ **RESOLVIDAS:** backfill — `sales_orders` saltou de 10→**626 clientes Oben** com histórico desde 2023 (cold-start do Meu Dia acabou); erro "uma das fontes falhou" — **#771** nomeia a fonte que falha (`useFilaAcoes`); timeouts crons `*/15` — estabilizou pós-incidente de plataforma (`net._http_response` 48h em 2026-06-29: **0 timeouts**). ✅ **Frente 3 — ficha de 30s pré-contato (na main, ⚠️ Publish do frontend pendente):** drawer ao tocar no cliente da `RotaListaLigacao` = munição (reusada) + histórico de compras com top-5 produtos/preço praticado (`derivarHistorico` puro TDD → `useHistoricoCompras` read-only/lazy/3-queries-sem-N+1 → `FichaPreContato` Sheet + `MunicaoResumo` extraído do HUD). **Cores CORTADO** — tintométrico (`tint_vendas`) não tem cliente e o staging de cores está vazio (não dá sem fabricar). Specs/planos `docs/superpowers/{specs/2026-06-29-ficha-pre-contato-design,plans/2026-06-29-ficha-pre-contato}.md`. Spec do programa: `docs/superpowers/specs/2026-06-10-push-vendedora-design.md`.

- **Radar de Clientes — prospecção de empresas (estilo "Radar do Omie") — Fatia 1: fundação de dados (2026-06-10/12, [PR #766](https://github.com/LucasSardenbergL/afiacao/pull/766), EM PRODUÇÃO com carga real validada)**: pedido do founder — funcionalidade de PROSPECÇÃO (hunter) tipo o Radar do Omie: localizar empresas que ainda NÃO são clientes, por segmento/região, com dados de contato, pra abordar. Decisão (brainstorming 1-a-1): **Brasil + CNAEs curados** (não API paga), **dump mensal GRATUITO de dados abertos do CNPJ da Receita Federal**, founder-only (tria e distribui via Tarefas), lista + mapa na v1, ações = registrar contato + cadastrar no Omie + atribuir Tarefa. Lag mensal aceito (é o mesmo do Radar do Omie). **Arquitetura**: 4 tabelas (`radar_empresas` PK cnpj + CHECKs cnpj/cnae/lote + 6 campos operacionais de prospecção; `radar_contatos` append-only; `radar_municipios` p/ mapa; `radar_ingest_state`) + RLS gestor/master (`pode_ver_carteira_completa`, escrita só service_role) + função `radar_recruzar_ja_cliente()` SECURITY DEFINER (cruza CNPJ contra `profiles.document` ∪ `omie_clientes_nao_vinculados.cnpj_cpf` — ⚠️ `omie_clientes` NÃO tem documento, só códigos) · helpers TDD puros `src/lib/radar/normalizar.ts` (19 testes, normalização do dump RFB) · edge **`radar-ingest`** (begin_lote/chunk/chunk_municipios/finalize/status, gate x-cron-secret OU JWT master, **verify_jwt=false** no config.toml, upsert NUNCA toca `prospeccao_status`/`primeira_vista_em`/`ja_cliente` [contrato fixado pelo assert A3 do PG17], re-run preserva `iniciado_em`, dedup intra-chunk anti-21000) · script de carga local `scripts/radar/carga.ts` (dump RFB → DuckDB filtra por CNAE principal → POST em chunks de 1000) + curadoria versionada `scripts/radar/cnaes-alvo.txt` (21 CNAEs moveleiro/metal/madeira/vidro) + `scripts/radar/municipios-ibge.csv` (lat/lng). **Subagent-driven, two-stage review por task** (spec-review + quality-review + fixes); validação **PG17 local** `db/test-radar-fundacao.sh` (A1-A7: recruza pelos 2 conjuntos, upsert preserva estado humano, RLS nega não-gestor/escrita-direta, EXECUTE revogado). ✅ **CARGA REAL DE PRODUÇÃO (2026-06-12): 526.176 empresas, 2.993 já-clientes (cruzamento ✅), 64.251 em MG, 5.440 municípios com lat/lng, 233 MB, lote 2026-05 = complete.** ⚠️ **LIÇÕES DA 1ª CARGA (o motivo de a carga inicial manual existir — todas commitadas em comentários datados no `carga.ts`):** (a) **a RFB mudou o layout em jan/2026** — o dump VIVE num **share Nextcloud público via WebDAV** (`arquivos.receitafederal.gov.br/public.php/webdav/<YYYY-MM>/`, token público `YggdBLfdninEJX9` = identifica o share, NÃO é credencial), não nas URLs antigas (`dados_abertos_cnpj/` dá 404); descobre-se via `PROPFIND`; (b) **DuckDB via `execFileSync` (args em array), nunca shell** — o SQL tem aspas duplas+simples e o quoting do `/bin/sh` quebra; (c) **`names=[...]` explícitos no `read_csv`** — o auto-naming zero-padda com ≥10 colunas (`column05` ≠ `column5`); (d) **`parallel=false`** — o Parallel CSV Reader do DuckDB 1.5.3 não aguenta o CSV da RFB; (e) **encoding: o dump é Windows-1252 com bytes C1 indefinidos** → `iconv -c -f WINDOWS-1252 -t UTF-8` ANTES (o decoder ICU do DuckDB crasha; `latin-1` estrito rejeita; o alias `ibm-1252_P100-2000` decodifica pra FULLWIDTH errado); (f) **a RFB NÃO escapa aspas internas** → linha com nº ÍMPAR de aspas é estruturalmente quebrada (~2-9 em 7,4M) e faz o parser do DuckDB **crashar com assert interno** — e ⚠️ **as opções "tolerantes" (`strict_mode=false`/`ignore_errors`/`null_padding`) ATIVAM o assert, não evitam** → saneamento `awk` de paridade de aspas (descarta e CONTA as linhas podres) + parser em modo ESTRITO. ⚠️ **O `finalize` da edge estoura o timeout de 8s do PostgREST no recompute de 526k linhas** — a 1ª carga foi fechada por **SQL manual** (recruza + UPDATE do state + validação) no SQL Editor; é o **modo de operação esperado da carga INICIAL** (cargas incrementais mensais não têm esse volume). ⚠️ **Colisão de timestamp de migration**: a `20260610200000_radar_fundacao.sql` tem o MESMO timestamp da `20260610200000_push_vendedora.sql` (sessões paralelas) — INÓCUO porque migrations são aplicadas MANUALMENTE no SQL Editor (não por ordem de timestamp), ambas já em prod. ⚠️ **Rede do ISP com rota quebrada pra `api.github.com`** (DNS devolve `4.228.31.149` Azure, i/o timeout) — contornado com `curl --resolve api.github.com:443:140.82.112.6` (IP legado) pro REST de criar/mergear PR; o `gh` não tem `--resolve` (mesma lição da auditoria de velocidade). **FOLLOW-UP não-bloqueante**: re-deploy da edge `radar-ingest` via Lovable pro fix `def3c839` (erro PostgREST serializado com `JSON.stringify` em vez de `String(e)`→`[object Object]` que mascarou o diagnóstico). **Próximas fatias (planos próprios)**: 2 = tela `/radar` ✅ (abaixo); 3 = ações (cadastrar no Omie `IncluirCliente` + atribuir Tarefa + mapa Leaflet agregado + toggle "só cidades da rota"); 4 = GitHub Action mensal + check `radar_ingest` no Sentinela. **Decisão do founder: o filtro v1 usa SÓ o CNAE principal** (secundários inflavam 526k→1,17M com falso-positivo tipo hospital-com-serralheria-interna; `match_via` fica pra v2). Spec `docs/superpowers/specs/2026-06-10-radar-clientes-design.md`, plano `docs/superpowers/plans/2026-06-10-radar-clientes-fatia1-fundacao.md`.
- **Radar de Clientes — Fatia 2: tela `/radar` (2026-06-12, [PR #785](https://github.com/LucasSardenbergL/afiacao/pull/785) mergeado + migration aplicada; ⚠️ requer Publish do frontend)**: a tela utilizável sobre a fundação da Fatia 1 (526k empresas em prod). Founder/gestor abre `/radar` (sidebar → Gestão, gate `gestorComercialOuMaster`), vê KPIs do lote, filtra a fila **server-side** (universo de centenas de milhares — `useInfiniteQuery` + `.range`, NUNCA carregar tudo), abre o detalhe de uma empresa (Sheet) e **registra o resultado do contato com undo**. **Arquitetura**: migration `20260612130000_radar_rpcs_contato.sql` (manual, **aplicada** `coluna_1=1 funcoes_3=3`) = `ALTER radar_contatos ADD status_anterior` + 3 RPCs `SECURITY DEFINER` gestor/master (gate fail-closed `COALESCE(pode_ver_carteira_completa,false)`, `REVOKE FROM anon`): **`registrar_contato_radar(cnpj,acao,nota)`** (muda `prospeccao_status` desnormalizado + loga em `radar_contatos` append-only; `FOR UPDATE` na empresa + advisory-lock + dedupe 2min; o `descarte_motivo` LIMPA ao sair de 'descartado' — não fica stale), **`desfazer_contato_radar(id)`** (undo own+<5min com **guard anti-regressão**: só reverte o status se o status atual ainda for o que ESTE contato setou — não pisa um contato mais novo; grava `status_anterior` na linha do log pra saber pra onde voltar), **`radar_kpis()`** (1 round-trip; "novos" lê `radar_ingest_state`, não conta a tabela). Helpers puros TDD `src/lib/radar/ui-helpers.ts` (vocabulário `ACOES_CONTATO` [4 operáveis: em_conversa/contatado_sem_resposta/virou_cliente/descartado], `presetParaParams` [novas=data_abertura desc; estabelecidas=capital_social desc + corte 5 anos via string-math], `idadeEmAnos`/`rotuloPorte`/`formatarCapital`/`formatarCnpj`). Hooks `src/queries/useRadar{Kpis,Lista}.ts`+`useRegistrarContatoRadar.ts` (lista com busca `ilikeOr` sanitizada [anti-injeção `.or()`], fila default esconde descartados+já-clientes, tie-break `.order('cnpj')`; ⚠️ as 3 RPCs novas usam cast `as never` nos hooks até o Lovable regenerar `types.ts` pós-migration — NÃO adicionar à mão, lição §10). 5 componentes `src/components/radar/` (KPIs, filtros+presets, linha densa, detalhe Sheet com link Maps `encodeURIComponent`+`tel:`/`mailto:`, `RadarOutcomeMenu` espelhando o `OutcomeMenu` da rota [toast+undo, AlertDialog de descarte com motivo, **desabilitado na lente "Ver como"** — o write-guard já bloqueia na fonte, é só UX honesta]). Página + rota lazy + item sidebar. ⚠️ **`<Select>` do Radix NÃO aceita `SelectItem value=""`** (quebra runtime) → sentinel `'__todas_ufs__'`/`'__fila__'` convertido pra `''` na fronteira, sem vazar pro estado/query. **Subagent-driven, two-stage review por task + review final do conjunto** (READY TO MERGE: contratos coerentes ponta-a-ponta, RPCs fail-closed, migration aditiva, zero regressão nos arquivos quentes App.tsx/AppShell). PG17 `db/test-radar-rpcs.sh` (A1-A8: registrar/dedupe/undo/anti-regressão/KPIs/gate/REVOKE/motivo-não-stale). CI: typecheck strict · 3226 testes · lint 0 errors. **Decisão registrada (review):** os 4 KPIs têm escopos diferentes — "A contatar" filtra lote vigente; "Em conversa"/"Viraram cliente (mês)" são globais (pipeline ativo independe do lote); founder pode pedir unificação. **Adiado p/ Fatia 3 (consciente):** toggle "só cidades da rota" (depende de cruzar `route_schedule` — mesma infra do mapa). Plano `docs/superpowers/plans/2026-06-12-radar-clientes-fatia2-tela.md`.
- **Radar de Clientes — Fatia 3: totalizador/ranking de cidades + ações (Omie/Oben + Tarefa) + mapa (2026-06-13, [PR #798](https://github.com/LucasSardenbergL/afiacao/pull/798), subagent-driven 7 tasks; ⚠️ requer migration manual + Publish + smoke no device)**: fecha a v1 do Radar. **(1) totalizador/ranking por cidade** = RPC **`radar_contagem_por_municipio`** (`SECURITY DEFINER`, agrega por `municipio_codigo`+UF — nome repete entre UFs; serve ranking "Onde caçar" + totalizador + bolhas do mapa, UM backend 3 usos; aplica os filtros ESTRUTURAIS da lista, NÃO a busca textual livre [evita ILIKE '%%' full-scan]; front debounça + cacheia). **(2) contatabilidade** = filtro "Só com telefone" (`.or('telefone1.not.is.null,telefone2.not.is.null')` positivo, sem footgun NULL-blind #702) + botão WhatsApp p/ **celular** (helper puro TDD `isCellphone` em `src/lib/phone.ts`, reusa `normalizeBrPhone`). **(3) cadastrar no Omie SÓ OBEN** (founder cortou as 3 contas — "senão me engano e cadastro na empresa errada"; Codex alertou duplicidade) — ⭐ **MELHORIA-CHAVE: reusa a action existente `omie-vendas-sync/criar_cliente` account=oben (idempotente `ListarClientes`-first + `throwOnTransient` #758) → SEM edge nova, SEM deploy de edge** (≠ `omie-sync` que é hardcoded Colacor SC); o front invoca a edge + RPC **`radar_registrar_cadastro_omie`** persiste (`omie_codigo_cliente`/`omie_cadastrado_em`, marca `virou_cliente`+`ja_cliente`, loga em `radar_contatos`). **(4) atribuir Tarefa** = RPC **`radar_atribuir_tarefa`** (sempre pro caller, `customer_user_id NULL` [nullable desde a fase 2 de tarefas], `empresa='oben'`, `categoria='ligar'`, **`modo='data'`+`due_date=hoje+N`+`interacao_tipo NULL`** p/ satisfazer `tarefas_modo_coerencia_chk`, `auto_satisfy_mode='off'` honesto, dedupe advisory-lock+2min) + "retomar em N dias". **(5) mapa Leaflet** (lazy code-split, espelha `AdminRoutePlanner`, bolhas por cidade, toggle Lista|Mapa, clique filtra). **CORTADO: preset "rota de amanhã"** (founder: "Anderson entrega, eu visito" — entrega ≠ visita, a premissa do Codex caiu → follow-up reposicionado "prospects da cidade da rota → Tarefa de ligação pra vendedora"). **3 RPCs gateiam `pode_ver_carteira_completa((SELECT auth.uid()))` 1× no topo** (lição #792 — SECDEF bypassa RLS, então o gate único é a fronteira; avaliado 1×, não por-linha) + REVOKE anon + `p_cnae_*` validado por regex (anti-injeção, sem SQL dinâmico). Ações **bloqueadas na lente "Ver como"** (UI `isImpersonating` + write-guard na fonte). Migration `20260613190000_radar_fatia3.sql`. **Subagent-driven (implementer sonnet por task + review do controller opus + revisor final adversário opus: APROVADO sem P1; 1 P2 de UX corrigido — `onPickRef` interno no mapa, senão refetch da lista a cada 30s dava "snap" ao fitBounds no meio do pan).** ⭐ **PG17** `db/test-radar-fatia3.sh` (A1-A9: agrega por código+UF, respeita filtros, gate nega não-gestor, REVOKE anon, dedupe da tarefa, idempotência do cadastro). Telemetria `radar.{cidade_selecionada,mapa_aberto,lead_cadastrado_omie,tarefa_criada}`. **Decisão consciente (anti-spoof):** o front passa os dados do lead (NÃO lemos server-side na edge — exigiria mexer no `omie-vendas-sync` money-path; risco baixo: é staff, é cadastro, qualquer staff já cria cliente hoje). **Mantém `select('*')` na lista** (paginada 50/vez, sem gargalo — a otimização "campos resumidos" do Codex era p/ "baixar tudo", não se aplica). **Follow-ups (Fatia 3.5, priorizados pelo uso):** opt-out estruturado, abordagem por CNAE (founder cura), preset rota reposicionado, matriz/filiais por raiz; v2 distante: CNAE secundários, materializar `radar_municipio_resumo` se o agregado ao vivo doer. **LIÇÕES:** (a) `omie-vendas-sync/criar_cliente` é account-aware (`getCredentials`) e idempotente — reusar > criar edge nova; (b) `omie-sync.callOmieApi` é hardcoded Colacor SC (não serve p/ Oben); (c) onPick inline + `useEffect([data, onPick])` no mapa = re-fit no pan → ref interno tira da dep. Specs/planos `docs/superpowers/{specs,plans}/2026-06-13-radar-clientes-fatia3-acoes-mapa*`.
- **Roteirizador — "Visitas em campo" (hunter) separado de "Planejamento da equipe" (2026-06-14, [PR #822](https://github.com/LucasSardenbergL/afiacao/pull/822), inline; ⚠️ requer só Publish):** founder achou a tela do Roteirizador confusa (5 modos numa tela só, feita pra equipe inteira). Ele é o hunter (master): visita carteira + prospects de cidade em cidade. Brainstorming (founder + Codex gpt-5.5) → spec aprovada. **100% frontend** — reusa as RPCs do Radar já em prod (`radar_contagem_por_municipio`, `radar_prospects_para_rota`, `radar_salvar_geocode`, `registrar_contato_radar`); **sem migration/PG17/edge**, só Publish. **NÃO toca `useFarmerScoring`** (money-path compartilhado). **Eixo de CONTEXTO** (`PlanningContext` 'campo'|'equipe', em `routePlanner/types.ts`) **acima** do eixo de modo: o "campo" reusa internamente `planningMode='prospeccao'`. Helper puro TDD `src/lib/route/field-targets.ts` (`defaultContextForRole`/`nextModeForContext`/`dedupeStopsById`/`particionarAlvos`/`filtrarAlvos`/`toggleTarget`, 14 testes). **(Fase 1) Navegação:** `RoutePlannerContextTabs` (2 abas, só p/ `temAcessoCampo = isMaster||isGestorComercial`); master abre no campo, gestor na equipe, **vendedor/separador NÃO veem o switcher** (`temAcessoCampo` false não monta as abas → tela atual idêntica); "Prospecção" saiu da faixa de modos (`PlanningModeSelector` agora só logística/comercial/híbrido/manual) → virou o contexto Campo; substitui o default-prospeccao-master do #816. **(Fase 2) Multi-cidade:** `CityMultiSelector` (chips removíveis; `CitySelector` single DELETADO); `selectedCity` → `selectedCities[]`; `loadProspectStops`/`loadCarteiraDaCidade` recebem N cidades → `Promise.all` de N chamadas à RPC single (top-50/cidade) + N `ilike('city')` → juntam + `dedupeStopsById`. **(Fase 3) Curadoria:** universo de alvos (`fieldTargets` = geocodedAllStops no campo) no mapa+lista (`FieldTargetsSummary` resumo "N alvos: X clientes · Y prospects" + filtro Todos/Clientes/Prospects; `FieldTargetCard` "Adicionar/Na rota"); o hunter MARCA quem visitar (`selectedTargetIds`) → `stopsParaRota` (campo = só os marcados; equipe = `filteredStops` intacto) → `optimizedRoute` roda sobre `stopsParaRota`; mapa mostra o universo filtrado, marcados em **azul numerados** (`#2563eb`, só no campo), não-marcados na cor do tipo; **fitBounds estável** (efeito separado, guard por chave de ids — não pula ao marcar). ⚠️ **Auto-revisão adversária (subagentes thrasham neste repo — lição #819 → Caminho B inline) pegou 1 regressão REAL que eu introduzi:** o efeito de markers pintava **todos** os pinos do EQUIPE de azul (`numero != null` p/ todo `optimizedRoute`) → quebrava "equipe intacta" → fix `cor = (noModoCampo && numero != null) ? azul : cor-do-tipo` (+ revertido pino 28→26px). **Limitações v1 (registradas):** `useFarmerScoring` roda em background no campo (não-mexível; não aparece/trava — `isLoading=authLoading` desde #814; extrair p/ só-equipe é follow-up); geocoding Nominatim capa 15/vez (mapa parcial, lista completa, aviso de "alvos demais"); contagem de **clientes** no resumo pós-carga (não no seletor — cruzar `addresses.city` texto-livre × 500 cidades seria caro/impreciso; seletor mostra só prospects/cidade, exato); marcar alvo pela **lista** (toggle-no-pino do mapa = v2); filtrar o universo com alvos já marcados de outro tipo deixa a linha da rota sem pino até voltar pra "Todos". CI: TC strict 0 · LINT 0 · TEST 53 · BUILD 0. **Execução inline** (não subagent-driven — #819). Spec/plano `docs/superpowers/{specs,plans}/2026-06-14-roteirizador-visitas-campo*`.
- **Roteirizador do hunter — carteira + prospects do Radar no MESMO mapa de visitas (2026-06-13, [PR #812](https://github.com/LucasSardenbergL/afiacao/pull/812), subagent-driven + Caminho B; ⚠️ requer migration manual + Publish + smoke no device):** estende o Roteirizador (`/admin/route-planner`, `useRoutePlanner`/`AdminRoutePlanner`) com o modo **"Prospecção"** (gestor/master via `showProspeccao = isMaster || isGestorComercial`): escolhida uma cidade, o mapa Leaflet mostra os **prospects do Radar (pin amarelo `prospect_visit`)** + os **clientes da cidade (pin laranja `sales_visit`)** juntos, com rota otimizada (nearest-neighbor existente) e geocodificação Nominatim **cacheada no banco**. **(A) Fundação SQL** (migration `20260613230000_roteirizador_prospects.sql`): colunas `lat/lng/geocoded_em/geocode_status` em `radar_empresas` + CHECK + RPCs **`radar_salvar_geocode`** (persiste o geocode de 1 prospect) e **`radar_prospects_para_rota`** (prospects da cidade por `municipio_codigo`, exclui já-cliente/descartado/virou, a_contatar primeiro, top-N), ambas gate `pode_ver_carteira_completa((SELECT auth.uid()))` + REVOKE anon. Helper puro TDD `src/lib/route/prospect-stop.ts` (`prospectRowToStopDraft`+`buildGeocodeQuery`). **(B) Client:** `CitySelector` (combobox via `radar_contagem_por_municipio`, label mostra `total`), `loadProspectStops` (RPC + pré-cache do geo no ref `geocodedCoords`), **`loadCarteiraDaCidade`** (clientes da cidade via `addresses.ilike('city', nome)` + `profiles`, `.order('user_id').limit(100)` determinístico), `allStops` case `prospeccao` = `[...prospectStops, ...carteiraCidadeStops]`, geocoding persiste via `radar_salvar_geocode` (filtro `s.lat===undefined && !s.geocodeFailed && !ref.has(id)` pula cacheado/falhado **sem regredir** a carteira que nunca traz `lat`). **(C) Ação ao visitar o prospect — ⚠️ MUDANÇA DE DESENHO vs spec:** a spec previa `route_visits` nullable + `radar_cnpj`; ao construir, optei por **reusar o `RadarOutcomeMenu` (Fatia 3)** no `RouteStopCard` → o founder registra o resultado direto no Radar (`registrar_contato_radar`: em conversa/não atendeu/virou cliente/descartado-com-motivo, toast+undo+lente-aware). **Zero migration, zero risco, fecha o loop no Radar** (o prospect "vive" em `radar_contatos`, não em `route_visits`). O prospect (`customerUserId=''`) **NÃO** tem check-in/checkout/CTA (esconde no card — evita INSERT em `route_visits` com uuid vazio). **Caminho B (Codex fora da cota — usage limit): Explore dos consumidores de `route_visits` confirmou que tornar `customer_user_id` nullable SERIA seguro** (trigger `enqueue_visit_score_recalc_from_visit` já guarda `IF NEW.customer_user_id IS NOT NULL`; RLS via `visited_by`/`carteira_visivel_para(NULL)→FALSE`; edges `visit-score-recalc-*` filtram `.not('customer_user_id','is',null)`; `positivacao_kpis` filtra `IS NOT NULL`; **sem FK** em `customer_user_id`) → **follow-up** se quiser o prospect também no "visitas de hoje" com geo/timestamp. **Review adversarial por subagente opus (substituiu o Codex): sem P1**; 1 P2 (truncamento silencioso da carteira `.limit(50)` sem order → `.order('user_id').limit(100)`) + 3 P3 (`getStopIcon` faltava `prospect_visit`, guarda do geocode assimétrica, `isCheckedIn` do prospect, label honesto "Cliente em X") — todos corrigidos. **PG17** `db/test-roteirizador-prospects.sh` (A1-A10: gate nega não-gestor, isolamento por cidade, geocode ok/falhou, range lat/lng, cnpj, REVOKE anon, CHECK do status). **LIÇÕES:** (a) **Nominatim** é grátis mas proíbe uso em massa → só sob-demanda por cidade (slice-15 + delay 1100ms), cacheado no banco (`radar_salvar_geocode`); nunca geocodificar os 526k; (b) `addresses.city` é texto-livre (diverge do `municipio_nome` RFB em acento/caixa) → `ilike`, imprecisão aceita na v1 (prospects vêm por `municipio_codigo` exato); (c) **o prospect "vive" no Radar** — registrar resultado direto em `radar_contatos` (reusar `RadarOutcomeMenu`) é mais simples e correto que duplicar a visita em `route_visits`; (d) o filtro `toGeocode` deve checar `s.lat===undefined` (não só o ref) p/ pular prospect cacheado **sem** regredir a carteira (que nasce sem `lat`); (e) ⚠️ o implementer (sonnet) pulou a `loadCarteiraDaCidade` (o "juntar", pedido CENTRAL) — o review do controller pegou; **sempre conferir o diff do subagente contra o contrato, não confiar no "feito"**. **Follow-ups:** recarregar/remover o stop do prospect ao registrar resultado (hoje permanece c/ toast; re-selecionar a cidade atualiza); priorizar a carteira-da-cidade por `customer_visit_scores` (hoje prioridade default); StatsStrip sem case `prospeccao`; check-in de prospect em `route_visits` (validado seguro). Spec `docs/superpowers/specs/2026-06-13-roteirizador-prospects-radar-design.md`.

**RECOVERY no Supabase do Lovable (concluída 2026-05-19)**: como Fundação + A1 tinham sido aplicados por engano no Supabase standalone, foi feita re-aplicação completa no Supabase do Lovable via SQL Editor (5 blocos A→B→C→D→E idempotentes) + re-deploy das 5 edge functions via chat do Lovable. Estado final validado: **9/9 tabelas, 6/6 funções, 18/18 triggers anexados, ambas colunas (regime + snapshot_dre_caixa_id), unique constraint com regime, seed config 3 empresas, 3 CNPJs**. Edge functions Active no Lovable: `omie-financeiro` (EDIT, 1493 linhas, lida do repo), `fin-period-override`, `fin-suggest-mapping`, `fin-ic-reconcile`, `fin-cashflow-engine` (4 novas). **2 crons agendados e ativos** (BLOCO E): `fin-cashflow-snapshot-diario` (`0 10 * * *`, snapshot 13s por empresa×cenário) e `fin-ic-reconcile-daily` (`0 9 * * *`). **Módulo financeiro 100% completo e automatizado no Supabase de produção.**

**Programa "Estado da Arte do Financeiro" — status (atualizado 2026-05-25)**: sobre a Fundação/A1, executado em ondas (cada uma validada por consult Codex, TDD em helper puro espelhado no engine Deno, mergeada e em produção no Lovable). Specs/planos em `docs/superpowers/{specs,plans}/`; helpers em `src/lib/financeiro/`.
- ✅ **Onda 1 — NCG** (PCO sem double-count de tributo, estoque real via `fin_estoque_valor`, CCC com PME, rename "capital giro próprio"→"liquidez operacional líquida"). Helper `ncg-helpers.ts`. Mergeada no PR #138.
  - ✅ **Feed de estoque do NCG religado ao Omie (2026-05-28, PR #467, migration aplicada em produção)**. A `fin_estoque_valor` (input MANUAL do balancete, gate deliberado da Onda 1) estava **vazia nas 3 empresas** → `fin-cashflow-engine` caía em `estoque=0` (`index.ts:306`) → NCG subestimava o ACO (~R$2,2M de estoque ignorado) e PME/CCC degradavam (o NCG batia EXATO `cr_aberto − cp_aberto`, sintoma do estoque=0). **Não era sync quebrado** — é tabela de input manual nunca preenchida. Empilhados, 2 bugs no caminho da estimativa: (a) a RPC assistente `fin_estimar_estoque_omie` lia `sku_estoque_atual` com **case-mismatch** (filtra `empresa='oben'`, mas o dado é gravado `'OBEN'` MAIÚSCULO → 0 linhas) × `product_costs.cost_price` (proxy ~2×, `DEFAULT_PROXY`); (b) **só o cron de inventário de `vendas`/oben existia** → `inventory_position` tinha **0 linhas de colacor** (a INDÚSTRIA, ~R$1,9M de estoque) e de colacor_sc. **Fix (opção B, eu+codex 2 consults — mantém o gate humano, sem feed automático silencioso):** RPC reescrita p/ derivar de `inventory_position(saldo×cmc)` (cmc = Custo Médio Contábil do Omie), mapeando empresa→**account canônico** (`oben→vendas`, `colacor→colacor_vendas`, `colacor_sc→servicos`) e **EXCLUINDO os labels crus `oben`/`colacor`** do `sync-reprocess` (mesmo SKU em 2 convenções = double-count); `plpgsql` + `lower(trim)` + `RAISE` em empresa inválida (sem 0 silencioso em money-path); só `saldo>0` no valor (exclui oversold/saldo negativo — guard do codex), `cmc=0` fora do valor mas dentro da cobertura; **assinatura preservada** (frontend `useEstoqueValor`/`ConfigCashflowDialog` não muda). + 2 crons `sync_inventory` p/ `colacor_vendas` + `servicos` (`getCredentials` já cobre os 3 accounts; a action `sync_inventory` já suportava — era **só cron**). **SEM deploy de edge.** Migration `20260528150000_fin_estoque_omie_feed.sql` aplicada via SQL Editor (validado em prod: `fin_estimar_estoque_omie` → oben R$521.499 / colacor **R$1.917.838** / colacor_sc R$18.113; `crons_inventario`=3). ⚠️ **Falta o passo do gate (operacional, só master, não-bloqueante):** o NCG lê `fin_estoque_valor` (manual), que segue vazia até `/financeiro/cashflow` → Configuração → **Estimar do Omie → Salvar** por empresa (×3); até lá a engine fica em R$0 honestamente (banner em `NcgDecomposicao.tsx`). **Lições:** (a) `inventory_position.account` tem 2 convenções (analytics-sync canônico `vendas/colacor_vendas/servicos` × sync-reprocess cru `oben/colacor`, unique por `(omie_codigo_produto,account)`) → mesmo SKU nas duas = double-count; somar por empresa exige eleger SÓ a canônica e excluir a crua; (b) `sku_estoque_atual.empresa` é gravado **MAIÚSCULO** (`'OBEN'`) — armadilha de case; (c) `codex exec` **pendura** esperando stdin (`Reading additional input from stdin...`) se rodado sem `< /dev/null` (travou 40min — sempre redirecionar). **v2 registrado:** opção A (cron auto-gravando `fonte='omie_estimado'` após validar contra 1 balancete real) e PME por **estoque médio** (série de snapshots) em vez de pontual.
- ✅ **Onda 2 — Timing da projeção 13s** (curvas de aging calibradas por exposição, vencidos reagendados, ponte "após horizonte/AR impaired", PMR/PMP + inadimplência ponderados por R$, guard de folha por janela). Helper `aging-helpers.ts` (espelho em `fin-cashflow-engine`). PR #138.
- ✅ **Onda 3 — DRE v2 regime-aware** (em `omie-financeiro/calcularDRE`, helpers `dre-helpers.ts` + `dre-tabelas-tributarias.ts`): **3a** estrutural — deduções (ICMS/ISS/PIS/COFINS/IPI) vs IRPJ/CSLL; **DAS único no Simples** (nunca quebrado, LC 123); caixa por data real + fallback "caixa estimado"; mapping explícito no lugar do prefixo `'3.99'`; gate de confiança (PR #184). **3b** imposto teórico — Simples (RBT12 + anexo + fator-r) e presumido (trimestral + adicional 10% + PIS/COFINS) ao lado do realizado, degradação honesta p/ `null` (PR #188). Coluna opcional `fin_config_cashflow.dre_tributario` setada nas 3 empresas (Colacor SC = `{regime:simples, anexo:III}`). ⚠️ Trigger `trg_audit` foi **removido de `fin_config_cashflow`** (a função `fin_audit_trigger()` deriva período por `data_emissao`, coluna inexistente em config → quebrava UPDATE; migration `20260523210000_*`, PR #192).
- ✅ **A2 — Retorno & Valor** (ROIC / WACC hurdle-rate / EVA / spread + **ROIC incremental** + **normalização de comingling** reportado×normalizado). NOPAT = `EBIT − imposto absoluto do regime` (abaixo da linha, nunca `×(1−t)`); capital investido = giro (NCG) + ativo fixo manual − ajustes; Kd pré-imposto. Helper `valor-helpers.ts`, engine `fin-valor-engine` (**master-only**), tabela master-only `fin_valor_inputs`, rota `/financeiro/valor`. PR #223.
- ✅ **A3 — Cockpit de Valor** (lucro econômico = margem de contribuição − encargo de capital de giro, **por cliente e por SKU**; recomendação de preço/prazo; identidade contábil Σcliente.evp = Σsku.evp = empresa.evp). Helper `valor-cockpit-helpers.ts`, engine `fin-valor-cockpit` (**gestor comercial + master**, escopo Oben via `omie_products.account`, paginação fetchAll anti-truncamento PostgREST), coluna `fin_config_cashflow.cockpit_config`, rota `/financeiro/valor-cockpit`. PR #255.
- ✅ **A4 — Próxima Melhor Ação** (fila priorizada de alocação de capital compondo A1–A3: consertar valor → liberar caixa → crescer com spread>0 → benchmark do hurdle; caixa disponível por empresa **não cruza CNPJ**; crescer sem custo estimado → `falta_dado`, nunca assume custo 0). Helper `next-best-action-helpers.ts`, engine `fin-next-best-action` (**gestor+master**, compõe A1/A2/A3 via service_role em paralelo + timeout). Rota `/financeiro/proxima-acao`. PR #266.
- ✅ **Otimizador Tributário — Comparador de Regime** (Simples × Presumido × Real por CNPJ; aponta o regime ótimo + economia anual). Base de comparação **federal+CPP via decomposição da partilha do DAS** (não compara DAS cheio vs federais parciais — inverteria a recomendação); ICMS/ISS/IPI no eixo indireto sinalizado à parte; elegibilidade por **RBA** (não RBT12); Presumido anualizado c/ **adicional de IRPJ por trimestre** + receitas financeiras integrais na base IRPJ/CSLL; Real como **triagem de baixa confiança** (lucro ≈ resultado contábil sem LALUR; PIS/COFINS não-cumulativo 9,25% − crédito + 4,65% s/ receitas financeiras); **degradação honesta** = status `incompleto`/confiança baixa quando falta folha ou <12m de DRE (nunca fabrica recomendação). **3 passes Codex** (2 metodologia no spec + 1 adversária no código). Helper `regime-tributario-helpers.ts` (28 testes; tabela `PARTILHA_SIMPLES` LC 123 com teto de ISS 5%), engine `fin-regime-tributario` (**master-only**, espelho verbatim), tabela master-only `fin_regime_inputs`, rota `/financeiro/regime-tributario`. PR #291 (admin-merge — CI nunca rodou limpo por causa da rotatividade da main do Lovable; 4 checks bloqueantes validados localmente). ✅ **Em produção (2026-05-25)**: migration `20260524120000_fin_regime_inputs.sql` aplicada via SQL Editor (3 linhas seed, 2 policies master-only) + `fin-regime-tributario` deployada **verbatim da main** (Active no Lovable; sobrescreveu a versão que o Lovable improvisou antes do merge). **Resta só** preencher os inputs da contabilidade (folha, presunções, créditos) no dialog de cada empresa em `/financeiro/regime-tributario` → destrava a recomendação confiante (até lá mostra "estimativa incompleta" de propósito, por design da degradação honesta).
- ✅ **Otimizador de Compras — "comprar mais?" (net-R$ marginal)** (2026-05-25, PR #325). **Integrado na Reposição** (`/admin/reposicao/oportunidades`), NÃO é página nova de financeiro — pega as ideias do programa de valor e formula a área de oportunidades já existente (decisão do Lucas). Responde "vale comprar acima do ponto de reposição?" pelo **net-R$ marginal vs baseline** = `desconto + aumento_evitado + ruptura_evitada − capital_extra − impacto_prazo − frete_incremental`. Regras-chave (todas calibradas em 2 passes Codex + 1 adversária): **capital_extra é carregado desde o dia 0** (`valor_extra × cm_anual × ((q_base/d)+0.5×(q_extra/d))/365`); **aumento_evitado com janela temporal** (`max(0, q_cand − max(q_base, demanda×dias_ate_aumento))` — só conta o que adianta a compra ANTES do aumento anunciado); **ruptura_evitada = 0 na fase 1** (conservador, só sinaliza com flag — não fabrica ganho de disponibilidade); **campos atômicos anti-double-count**; **candidatos entre os thresholds da curva** + o `qtde_oportunidade` como candidato explícito (senão o cenário aumento-puro nunca era testado). **Degradação honesta**: sem demanda → `falta_dado`; grupo (não-SKU) → `simulacao_parcial`; recomenda `comprar_mais` só com escopo='sku' & net>0. **`qtdBase = max(qtde_base, max(lote_minimo, minimo_forcado))`** — o `minimo_forcado` já está no helper p/ atender o requisito futuro do Lucas (alguns itens da sugestão de pedido **obrigam quantidade mínima** — a "R" que força mínimo); **falta decidir ONDE persistir esse mínimo por SKU** (hoje o helper aceita o param, mas não há fonte/coluna/UI alimentando — trabalho de produto futuro → ✅ **RESOLVIDO em #608**: coluna `sku_parametros.minimo_forcado_manual`, ver bullet logo abaixo). Helper **`src/lib/reposicao/compras-otimizador-helpers.ts`** (20 testes; `gerarCandidatos`/`avaliarComprarMais`). **SEM edge function** — roda client-side sobre a view (dado operacional, staff-readable, sem regra financeira sensível). View **`v_otimizador_compras_insumos`** (`security_invoker=on`, idempotente, **só junta fatos**: `v_oportunidade_economica_hoje` + `sku_parametros` (lote_minimo) + CTE `prazo` dedup'd de `fornecedor_prazo_pagamento_config` (padrao+ativo, 1 linha/fornecedor anti-duplicação de SKU) + CTE `frete` pivotado de EAV `fornecedor_custo_adicional_config` (tipo IN frete_perc_valor/frete_fixo/taxa_pedido); join por `empresa`+`fornecedor_nome`). ⚠️ **`custo_capital_efetivo_perc` é %/ANO** (cm_anual×100) — o front divide por 100 (`cm_anual = custo_capital_efetivo_perc/100`). Migration `20260525140000_v_otimizador_compras_insumos.sql` aplicada via SQL Editor. ✅ **Em produção** (CI da main verde pós-merge): view criada, **0 linhas hoje = estado vazio VÁLIDO** (sem promo/aumento ativo — base view == otimizador view == 0 prova que os LEFT JOINs não quebraram). Pra ver o net-R$ em ação: cadastrar promoção teste em `/admin/reposicao/promocoes` (volume_minimo + desconto, vigente hoje, num SKU com `sku_parametros`).
- ✅ **Lote mínimo de compra por SKU — a "R" (#608 + #626, 2026-06-05, em prod)**. Fecha o "falta decidir ONDE persistir esse mínimo" do bullet acima. **Coluna `sku_parametros.minimo_forcado_manual`** (numeric NULL, CHECK `>0`); a RPC `gerar_pedidos_sugeridos_ciclo` faz **`qtde_final = GREATEST(natural, minimo)`** = **PISO, NÃO GATILHO** (só eleva item que JÁ precisa repor — `estoque_efetivo<=ponto_pedido` E `qtde_natural>0`; nunca força comprar sobre-estocado; NULL = idêntico ao anterior → aplicar foi zero-risco). `qtde_sugerida`=natural (audit) × `qtde_final`=forçada (dispara ao Omie). Migration `20260604190000` (corpo da RPC = **superset estrito** da `20260604170000`, confirmado por safety pass adversário contra a def VIVA de prod — zero guarda removida). **Onde setar:** Reposição → **Revisão** (`/admin/reposicao/revisao`) → filtro **Pendente OU Aprovado** (só esses são editáveis; "Primeira compra"/"Aguardando fornecedor" são read-only, sem botão de editar) → clica na **linha** → painel `SkuDetailSheet` → "Editar valores manualmente" → campo **"Mínimo de compra forçado"** → Salvar (NÃO desfaz aprovação — a `updateMutation` só grava os values, não toca `aprovado_em`); OU `UPDATE sku_parametros SET minimo_forcado_manual=N WHERE empresa='OBEN' AND sku_codigo_omie=...` em massa (CHECK barra ≤0). A view `v_otimizador_compras_insumos` (PARTE C da migration) expõe a coluna. ⚠️ **LIÇÕES:** (a) **`CREATE OR REPLACE VIEW` só ACRESCENTA coluna no fim** — a #608 inseria `minimo_forcado_manual` no MEIO de `v_otimizador_compras_insumos` (entre `lote_minimo_fornecedor` e `fornecedor_codigo_omie`) → falharia em prod (`cannot change name of view column`); #626 moveu pro fim (**3ª ocorrência** do view-reorder — sempre `pg_get_viewdef` da prod antes de REPLACE de view; coluna lida por nome no front → posição é indiferente); (b) **tipo:** `sku_parametros.sku_codigo_omie` é **bigint**, `pedido_compra_item.sku_codigo_omie` é **text** → join entre os dois precisa `::text` (senão `42883 operator does not exist: bigint = text`); (c) **identidade Sayerlack:** o "código" que o founder usa (ex.: `FI.6197QT`) está na **DESCRIÇÃO** (`ISOLANTE PU FI.6197QT`), **NÃO** em `omie_products.codigo` (= `PRD01722`); a busca da Revisão casa por **número** (regex só-dígitos → `sku_codigo_omie`) OU **descrição** (ILIKE) — código-com-letras nunca casa como número, e busca por código inexistente devolve tabela vazia ("não tem onde editar"); (d) **item sem linha em `sku_parametros`, OU `v_sku_parametros_sugeridos.status_sugestao='AGUARDANDO_SEGUNDA_ORDEM'`/`ponto_pedido_sugerido` NULL = baixo giro** → o motor não sugere e não há onde gravar o mínimo → **compra MANUAL** (NÃO fabricar params na mão = estocar item que não gira). **Deploy do front é verificável pelos bytes do bundle** (técnica do §"Deploy do FRONTEND", usada com sucesso aqui: enumerar os chunks pela UNIÃO (fechamento transitivo do grafo Vite ∪ precache do Workbox; ~274, não 233) e `grep` a string-alvo em todos — hoje empacotado na skill `lovable-deploy-verify` (`scripts/verify-frontend.sh`) → confirmou o campo do #608 já publicado em prod).
- ✅ **Hardening de segurança do `omie-financeiro` (2026-05-25, validado em revisão adversária com o Codex)** — 3 PRs money-path, TDD em helper puro espelhado verbatim no Deno, `deno check` sempre com erro-set inalterado:
  - **#322** — **injeção PostgREST no DRE**: `ano`/`mes`/`meses` vinham do body sem coerção runtime e entravam crus no `.or()` do `calcularDRE` (`{"mes":"01),or(id.gte.0"}` quebrava o `and()`). Helper `dre-period.ts` (valida inteiro no range; ausente→default, presente-inválido→`DrePeriodError`→HTTP 400) no boundary **e** re-assertado no `calcularDRE`. Fecha também o bug `ano+1` de dezembro.
  - **#324** — **allow-list de empresa** (`resolveCompanies` em `omie-request.ts` → 400 fora de `[oben,colacor,colacor_sc]`) + **`if (error) throw`** nas queries do DRE (`buscarCR`/`CP`/mappings/hist): falha de DB deixou de virar DRE zerada/mis-categorizada **persistida** no upsert.
  - **#327** — **gate master+gestor (BFLA)**: `validateCaller` aceitava QUALQUER `employee`; agora exige `master` (user_roles) OU gestor comercial (`commercial_roles` ∈ `{gerencial,estrategico,super_admin}`) — espelha o gate do `fin-valor-cockpit`. Helper `hasFinanceiroAccess`. Crons/service_role inalterados.
  - **Sweep das ~48 edge functions**: resto **sólido** (sem nova injeção `.or()`; `verify_jwt=false` todas gated por cron-secret/webhook-secret/JWT/WebAuthn). Único achado extra = nit de timing-compare baixíssimo no `omie-nfe-webhook` (`!==` vs o `timingSafeEq` do irmão `omie-webhook`) — follow-up opcional. Fora de escopo (consciente): tenant-scoping por empresa (org única, by-design) e validação de `maxPages`/`filtro_data` (baixo valor; `requestedRegime` já seguro). Specs em `docs/superpowers/specs/2026-05-25-{dre-period,omie-financeiro}-*`.
  - ✅ **Redeploy do `omie-financeiro` FEITO (2026-05-27)** via chat do Lovable: lido do repo (2065 linhas, marcadores #322/#324/#327 confirmados) e deployado **verbatim**; **Active**, invocação de teste retornou 400 com a lista de ações válidas (prova que o `validateCaller` responde no runtime). Os 3 PRs de hardening money-path (#322 anti-injeção PostgREST no DRE, #324 allow-list de empresa + `if (error) throw`, #327 gate master/gestor BFLA) agora estão **em produção**.
- ✅ **Custo Marginal de Funding — "de onde vem o caixa, e a que custo" (2026-05-27, PRs #345 + #364)**. Fecha o lado **fonte** do caixa (complemento do A4, que decide o **uso**). Master-only (tesouraria/CFO), padrão A2/regime. **Princípio (revisado por Codex):** tudo em **R$ no horizonte relevante**, taxa anualizada só pra exibir — comparar % a.a. de instrumentos com prazos diferentes pra cobrir um gap **pontual** escolhe errado. Pré-imposto (sem tax-shield, coerente Simples/Presumido). **Duas entregas:** (A — #345) **decisão de antecipação por título** de `fin_contas_receber`: custo da antecipação (`V − v_liq`; deságio comercial por fora + IOF de crédito PJ `0,38%+0,0082%/dia` só p/ desconto, factoring=0 + tarifa) vs. benchmark contextual — `gap` (há déficit até o vencimento) → fonte alternativa mais barata (capital de giro/cheque); `sobra` → retorno do melhor uso do caixa (A4) ou `cm_anual`. (B — #364) **planejador de cobertura de gap**: `identificarGap` (vale mais profundo da projeção 13s; horizonte **até a recuperação**, não a semana do vale) + `montarPlanoCobertura` (merit-order **em R$**, não % a.a.) empilhando **só fontes EXTERNAS** (capital de giro, cheque; cheque pode vencer gap curto → flag emergência) + **custo da inércia em R$**. **Decisões-chave de metodologia (2 rodadas Codex adversarial — 2 P1 no A, 3 P1+2 P2 no B):** ⚠️ **caixa próprio NÃO é fonte do planejador** (o gap vem da projeção que JÁ inclui o saldo atual → seria double-count; o gap é o que falta DEPOIS do caixa próprio); **gap×sobra pelo menor `saldo_final` projetado até o vencimento** (inclui a semana do vencimento); **simulação de 2 cenários** (`checaValeEmT`) sinaliza se antecipar cria vale futuro; **estrutural × calendário** (gap em ≥6/13 semanas → flag + "antecipar é rolagem, renegocie prazo/preço/estoque"); **por-CNPJ** (não cruza caixa entre empresas); **coobrigação obrigatória**; **CET (não taxa nominal) pra dívida**; **composição A4** consome só `caixa_livre` + `retorno_marginal` (dimensionado), não re-decide uso. **Degradação honesta em tudo** (sem taxa→`falta_dado`; sem cm_anual/A4 em sobra→`falta_dado`; sem projeção→contexto `indefinido`/cm_anual; custo da inércia=`null` sem cheque; taxa negativa→tratada como não-configurada — NUNCA fabrica recomendação). Helper **`src/lib/financeiro/funding-helpers.ts`** (35 testes, vitest) **espelhado verbatim** na edge **`fin-funding`** (master-only, compõe `fin-cashflow-engine` + `fin-next-best-action` + `empresa_configuracao_custos` (cm_anual) + `fin_contas_receber`). Tabela master-only **`fin_funding_inputs`** (migration `20260526100000`, RLS `role='master'`, 3 empresas seed) — taxas default por fonte + override. Hook `useFunding` + `FundingInputsDialog` + rota **`/financeiro/funding`** (decisão por título + planejador). ✅ **Em produção (2026-05-27)**: migration aplicada via SQL Editor (`linhas=3, policies=2`) + `fin-funding` deployada verbatim da main (706 linhas, Active; 401 no curl = gate master-only OK). **Resta só** preencher as taxas no dialog (botão "Editar taxas") — até lá a tela mostra "falta dado" de propósito. **Limitações v1 (ver `docs/FINANCEIRO_CONFIABILIDADE.md`):** granularidade da projeção é semanal; `cria_vale_em_T` só sinaliza (re-custo=v2); concentração por sacado é aviso; antecipação NÃO entra como fonte do planejador (v2); capacidade de dívida/cheque ilimitada (limite=v2); `caixa_livre`=`caixa_disponivel` do A4 (não desconta ação aprovada); risco de crédito/sem-coobrigação não valorado (v2).
- ✅ **Orçamento Rolling — Forecast de Aterrissagem (2026-05-27, PR #376, sub-PR A)**. Constrói SOBRE o "Orçado × Realizado" que **já existia** (`/financeiro/orcamento` + tabela `fin_orcamento(company,ano,mes,dre_linha,valor_orcado)` + `getOrcamento`/`upsertOrcamento`) — NÃO reimplementa. Projeta **onde o ano FECHA** = `landing = Σ realizado dos meses FECHADOS + Σ forecast dos restantes`, por linha de DRE, e a **variância projetada** vs orçado anual. **Princípio (Codex): método POR LINHA, não média global** — pipeline ORDENADO `projetarDRE` (cada driver usa a base FORECASTED da etapa anterior): `receita_bruta → deducoes → receita_liquida → cmv → demais → impostos → derivadas`. Métodos: receita = **sazonal ajustado por tendência YTD** (`receita_ano-1[mes]×fator`, cap [0,5;2,0]; ano-1 esparso no mês → cai p/ run-rate, não zera) → run-rate → orçado; deduções/cmv = **driver razão-YTD** (% da receita_bruta/receita_líquida forecasted); despesas fixas = run-rate; financeiras = média 3m; **impostos = razão-YTD** (Σimp/Σreceita fechados × receita FC — NUNCA média mensal cega; flag <3 fechados/<1 trimestre Presumido); **derivadas (receita_liquida/lucro_bruto/resultado_operacional/resultado_antes_impostos/resultado_liquido) CALCULADAS pela fórmula IDÊNTICA ao DRE v2** (`dre-helpers.ts` — financeiras entram no resultado_operacional) p/ bater com o realizado; orçado das derivadas **calculado das 11 linhas orçadas** (não null/zero). **Variância sign-aware** por conjunto LITERAL (receita/derivadas-de-resultado acima=favorável; deduções/cmv/despesas/impostos acima=desfavorável) + `fura_meta`=`|var|>max(10%×orçado, piso)` (orçado≤0→só piso). **Mês corrente PARCIAL fora da base e do realizado** (só meses fechados). **Degradação honesta**: 0 fechados→sem forecast; <3→confiança baixa; denominador de driver ≤0→run-rate+flag (sem NaN); orçado AUSENTE (≠zero, via operador `in` no draft)→variância não computável. **Por empresa.** Metodologia revisada por **Codex em 4 etapas** (metodologia + spec [10 findings] + plano [8 P1 de testes fracos→assertivos] + código adversarial [3 P1: resultado_operacional=DRE oficial, orçado-derivado não some variância, sazonal esparso→run-rate]). Helper **`src/lib/financeiro/orcamento-forecast-helpers.ts`** (20 testes vitest; `projetarDRE`+primitivas). **CLIENT-SIDE — sem migration, sem edge function, sem deploy** (roda no front sobre `getDRE`+`getOrcamento`, igual à tela existente). Seção "Forecast de aterrissagem" em `FinanceiroOrcamento.tsx` + item **na sidebar** (a rota era órfã, só por URL). ✅ **Em produção** (CI verde, nada a fazer do lado do founder). ✅ **sub-PR B — Seed de baixa fricção (2026-05-27, PR #386, client-side, em produção)**: botão **"Sugerir de {ano-1}"** + input de crescimento % (default 10) no modo de edição → `seedOrcamento({dreBase, crescimentoPerc, fatorOutlier=3})` pré-preenche cada (linha, mês) com `realizado_ano-1 × (1+g)`. **Winsorize por múltiplo da MEDIANA** (`capSup=med×fator`, `capInf=med/fator` — robusto; std-com-outlier infla o próprio limite); **amostra curta** (`<3` meses com valor MATERIAL; zero = sem-movimento NÃO conta; `|v|<1%` do pico = ruído, achado adversarial do Codex) → `null` + flag `amostra_curta_sem_sugestao` (NÃO fabrica); **mês ausente** → `mediaCap×(1+g)` + flag `mes_ausente_media` (honesto, founder revê p/ sazonalidade); mês capado → flag `winsorizado`. Guards: crescimento NaN→0/clamp `≥-100`, fatorOutlier finito `≥1` senão 3, round2. **Botão NÃO-destrutivo** (achado adversarial): preenche SÓ células vazias do draft (conta preenchidas/mantidas), linhas `null` ficam em branco, NÃO chama `upsertOrcamento` (founder revisa e salva pelo fluxo existente). **Codex no plano** (winsorize por mediana, assert exato mediana×fator) **+ adversarial no código** (1 P1 materialidade + 2 P2 + 1 P3). Helper em `orcamento-forecast-helpers.ts` (28 testes vitest). **CLIENT-SIDE — sem migration/edge/deploy.** ✅ **Drill de variância por categoria (2026-05-27, PR #404, client-side, em produção)**: clicar numa linha do Forecast que **fura a meta** expande inline e decompõe o **realizado YTD** dela pelas categorias do Omie (atribuição por **CÓDIGO** via `fin_categoria_dre_mapping`, company sobrescreve `_default` determinístico), com **delta YoY nos mesmos meses fechados**. **Honesto por design (Codex):** explica o **realizado YTD, NÃO a variância anual** (categoria não decompõe o forecast restante → mostrado à parte; categorias não somam no landing); **reconciliação SEMPRE visível** (realizado snapshot vs Σ categorias vs resíduo + % → `ok` ≤5% **E** ≤R$10k / `parcial` / `diagnostico` >20%); linguagem "principais componentes", nunca "explicam o furo". **Aliases fiscais regime-aware (Codex no spec, verificado no `montarDRE`):** `deducoes` snapshot = `deducoes+ded_*+das+impostos legado` (DAS cai aqui no Simples), `impostos` = `0` (simples)/`irpj+csll` (presumido) → `aliasesDaLinha` evita que linha fiscal vire resíduo gigante. **Fonte de reconciliação = `fin_dre_competencia_base` (Codex ADVERSARIAL no código, P1):** a matview dimensional comum é por `data_vencimento`, mas o snapshot é **competência (`data_emissao`)** → drillar contra ela acusaria resíduo FALSO; a `fin_dre_competencia_base` (CR+CP por emissão, `status≠CANCELADO`, `valor_documento`) é a MESMA base do `calcularDRE` competência → reconcilia de verdade. Soma por código sobre CR+CP (calcularDRE classifica por código, não por razão); **paginação** (`.range` em loop — a view passa de 1000 linhas) + filtro de meses fechados server-side. Helper **`src/lib/financeiro/orcamento-drill-helpers.ts`** (15 testes; `drillLinha`/`aliasesDaLinha`/`fontesDaLinha`) + `getCategoriasCompetenciaRaw` + `DrillVarianciaPanel` + expansão inline na `FinanceiroOrcamento.tsx` (query por company/ano + `drillLinha` via `useMemo`). **Codex em TODAS as etapas** (metodologia + spec + plano + adversarial no código — 2 P1: base temporal + paginação). **CLIENT-SIDE — sem migration/edge/deploy.** **Limitações v1:** mapping não-versionado (reclassifica o passado com a regra atual → v2); resíduo reflete categorias não mapeadas/fallback heurístico do `calcularDRE` (exposto, não escondido). ✅ **Drill v2 — concentração por fornecedor/cliente (2026-05-27, PR #415, client-side, em produção)**: **toggle** no painel ("Por categoria | Por fornecedor·cliente") re-agrega a linha por **entidade**, só em **linhas puras** (despesas→fornecedor/CP, receitas→cliente/CR; deduções/impostos/derivadas SEM lente). **2 Paretos:** NÍVEL (top-N por `abs(realizado)`/Σabs) + AUMENTO (`aumento_bruto=Σmax(delta,0)`; top-N/aumento_bruto; `null` se não cresceu). **Identidade:** `cnpj_cpf` válido (rejeita sentinela todos-iguais)→nome normalizado→`sem_identificacao`; badges Novo/Sumiu/Sem ID. **Honesto (Codex P2):** "concentração do realizado YTD e variação YoY", NUNCA "causou o estouro da meta". **Fonte = `fin_contas_pagar/receber` diretas** (mesmos filtros da `fin_dre_competencia_base`) → reconcilia contra o **total-por-categoria do v1** (não o snapshot); **a diferença é classificada** (badge ok/parcial/diagnóstico, mesmos limiares do v1) com aviso de "lançamentos no razão oposto" — porque o v1 soma CR+CP e o v2 filtra uma razão (Codex adversarial P1). Limita server-side ao **horizonte fechado** (senão ano-1 trunca/perde YTD); **chunked `.in()`**, **`.order(id)`**, **teto 20k→diagnóstico**. Helper **`orcamento-entidade-helpers.ts`** (16 testes; `concentrarPorEntidade`/`coletarTitulosEntidade`/`classificarReconciliacaoEntidade`) + **`codigosDaLinha`** extraído do v1 (reuso fecha a reconciliação) + `getTitulosEntidadeRaw` + toggle no `DrillVarianciaPanel`. **Codex nas 4 etapas** (metodologia + spec [3 P1] + plano [7 P1: fixtures CNPJ, horizonte fechado, orquestrador testável] + adversarial [1 P1 cross-razão + 2 P2]). **Limitações:** nome normalizado não resolve filiais/grupos; decomposição volume/preço/título = v2 futuro. **v2 do Orçamento:** versionamento/snapshot do forecast, impostos regime-aware (compõe `fin-regime-tributario`), decomposição volume/preço/mix, consolidado cross-CNPJ. ⚠️ ✅ **Follow-up de regime RESOLVIDO (2026-05-27, PR #421, hardening P0):** a badge dizia "Regime de Caixa" mas a página SEMPRE calculou competência (`getDRE(..., 'competencia')` no realizado YTD e no forecast; competência é o correto p/ orçado×realizado e o forecast depende dela p/ reconciliar). Era só **rótulo errado** (não cálculo) → badge corrigida p/ "Regime de Competência" (torna o label verdadeiro). As telas com regime variável (Cockpit/hook principal) têm toggle; a Tributário usa competência (default) — sem inconsistência cross-tela.
- ✅ **Consolidação do Cockpit — religar à engine A1 + rótulos honestos (2026-05-28, PR #443, client-side, em produção)**. O Cockpit (`/financeiro/cockpit`) foi construído **antes** das engines A1 e nunca foi religado: calculava a projeção 13s por uma **RPC legada** (`fin_projecao_13_semanas`) e exibia **NCG = CR−CP** (proxy grosseiro) → divergia das telas que já usam a engine (NCG/Cashflow). Esta consolidação **religa o Cockpit à fonte real** = **snapshot diário da engine** (`fin_projecao_snapshots`, gravado pelo cron `fin-cashflow-snapshot-diario` via `fin-cashflow-engine` com `save_snapshot`), e **NCG = ACO−PCO da engine** (não mais CR−CP). **Decisão (Codex): snapshot-first** — tela executiva semanal, 3 invocações ao vivo seria caro; o snapshot já tem `dados`=semanas[] + `ncg`. Helper **`src/lib/financeiro/cockpit-consolida-helpers.ts`** (`consolidarCockpit`, 13 testes TDD): consolida as 3 empresas por **coorte de data de referência** (`dataRef = max(snapshot.inicio.slice(0,10))` — **slice de string, NÃO `new Date`**, p/ não escorregar fuso), **dedupe latest-wins** por `Date.parse(snapshot_at)` com desempate determinístico `.order('id',desc)`, soma **total + por-empresa** (caixa **NÃO é fungível** entre os 3 CNPJ → exibe total **e** quebra), marca semana `completa` (todas as empresas têm) vs parcial. **Degradação honesta:** `getProjecaoSnapshotsCockpit` filtra semana com `Number.isFinite` nos 3 campos numéricos (**ausente≠zero** — descarta malformado em vez de virar R$0); `COCKPIT_VAZIO` marca `parcial:true`/`ncg_parcial:true` (falha de carga **não** vira falso-verde de R$0); guard de corrida `loadIdRef` no toggle de regime (resposta velha não sobrescreve a nova). **Rótulos honestos:** `Projecao13Card` ganha badge "Consolidado 3 CNPJ · dados de DD/MM/YYYY" + defasagem "Nd atrás" + aviso intercompany + banner parcial **pela coorte real** (`empresasPresentes.length`, não a 1ª semana); `FinanceiroCockpit` troca "Caixa Projetado 30d" por **"Posição líquida (abertos)"** e o card NCG fica `positive` quando **≤0 = folga** (badge "ACO − PCO (engine A1)"); `DataBasisFooter` regime-aware. **Bug gêmeo alinhado:** `NcgDecomposicao.tsx` pintava a cor do NCG **invertida** (NCG<0 = erro) → corrigido p/ **NCG≤0 = folga** (sucesso), coerente com a semântica ACO−PCO e com o Cockpit. **Codex em todas as etapas** (metodologia + spec [3 P1 + P2] + plano [testes anti-impl-preguiçosa] + adversarial no código [2 P1: `Number.isFinite` no lugar de `Number(x)||0`; `COCKPIT_VAZIO` parcial; + P2: banner por coorte, `.order(id desc)`, guard de corrida, alinhamento do `NcgDecomposicao`]). **CLIENT-SIDE — sem migration, sem edge function, sem deploy** (o snapshot já é gravado pelo cron existente). **Não-objetivos:** invocar engines ao vivo no Cockpit (snapshot-first); consolidar caixa fungível cross-CNPJ (não é — total + quebra por empresa). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-27-cockpit-consolidacao*`.
- ✅ **Valor A2 — Guard de NCG indisponível (ausente ≠ R$0) (2026-05-28→29, PR #469, em produção)**. Continuação da frente de consolidação (alvo escolhido por consult Codex após o Cockpit). O engine de **Valor A2** (`/financeiro/valor`, ROIC/EVA) lê o **NCG da engine A1** (`fin_projecao_snapshots.ncg`) como capital de giro — **mesma fonte do Cockpit** — mas quando **não havia snapshot de NCG** assumia `capital_giro = 0` silencioso (`fin-valor-engine` L250 `latestNcg ? ... : 0`). Com ativo fixo informado, `capital_investido = ativo_fixo` (subestimado) → `roic()` **não** retornava null (capital>0) → **ROIC superestimado**, e `capital_parcial` só olhava ativo fixo → confiança **não** rebaixada; só um motivo enterrado. Violava "ausente≠zero" e divergia do Cockpit (que já trata NCG ausente como parcial). **Fix:** NCG ausente → `capital_giro`/`capital_investido` = **`null`**, `roic`/`spread`/`eva` = `null` (não fabricados), confiança = **baixa**, UI diz "Sem snapshot de NCG — capital de giro indisponível" (não mais "* capital parcial (sem ativo fixo)"). **0 e negativo (folga) são NCG REAIS** (`ncgFinito`, sem truthiness); capital ≤0 por folga → `roic=null` **por capital conhecido** = confiança **media** (distinto de ausente=baixa). **Frescor (Codex P1.3):** NCG 45+ dias (cron é diário) → **media** + "NCG de DD/MM/YYYY (Nd atrás)" visível; stale ≠ indisponível (NCG é real; pipeline morto já é vigiado pelo Sentinela). **Guard anti-coerção (Codex P1.1):** `capital_normalizado` null não vira `null+ajuste=número`; EBIT/NOPAT normalizado seguem calculados. **Função pura composta `resolverCapitalParaValor`** (resolver+frescor+−12m+capitalInvestido) testada ponta-a-ponta e espelhada **verbatim** no edge — **defeita o "`capital_giro = ncg ? ncg : 0`" inline** (Codex plano P1: senão consertar só o helper e deixar o `:0` no edge passaria no teste). **Reconciliação (critério de pronto):** com snapshot válido, `A2.reportado.capital_giro` == último `fin_projecao_snapshots.ncg` da empresa = o mesmo NCG que o Cockpit usa. **`ncgFinito` (Codex adversarial P2):** `ncg` como `''`/whitespace (cast runtime do PostgREST) não fabrica giro=0 — tratado como ausência; string numérica (`numeric` do Postgres) segue valor real. Helper `valor-helpers.ts` (+`resolverCapitalGiro`/`frescorGiro`/`acharCapitalGiroAnterior`/`resolverCapitalParaValor`; **58 testes**) espelhado no `fin-valor-engine`; contrato `ValorEmpresaResult` nullable (+`giro_indisponivel`/`giro_snapshot_at`/`giro_dias`); UI `FinanceiroValor.tsx`. **Codex em todas as etapas** (spec [3 P1] + plano [3 P1: composta anti-inline, asserts exatos, distinção NCG-ausente×capital≤0] + **adversarial: sem P1**; P2 `ncgFinito` endurecido; P3 cosmético `Math.round` deixado). **A4 (`fin-next-best-action`) lê só `wacc`/`spread` (já nullable) → não quebra.** ⚠️ **Sem migration; REQUER deploy do `fin-valor-engine` via chat do Lovable** (ROIC/EVA calculados no edge — client-side só avisaria depois do número nascer errado). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-28-valor-ncg-guard*`.
- ✅ **A3 Cockpit de Valor — Guard de hurdle indisponível (não fabricar 20%) (2026-05-30→31, PR #522, em produção)**. Mesma frente do A2, alvo por consult Codex. O A3 (`/financeiro/valor-cockpit`, lucro econômico EVP por cliente/SKU) cobrava o capital de giro a um hurdle `k` que, **sem o Ke configurado** (`fin_valor_inputs.ke.base` ausente), virava **20% fabricado** (`fin-valor-cockpit` L204 `keBase ? ... : 0.20`) → `encargo = k × (AR+estoque)` → `evp = cm − encargo` → **EVP fabricado** por cliente/SKU + UI "@ 20.0%". Mesmo "ausente vira número" do A2. **Fix:** Ke ausente/inválido → **`k = null`** (`resolverHurdleCockpit`; âncora obrigatória; soma ≤0 = "capital grátis" → null; `''`/whitespace não fabrica) → `encargo`/`evp` = **`null`** em célula/rollup/empresa; **margem (cm) segue**; confiança **baixa**; UI troca "@ 20.0%" por banner "EVP indisponível — configure o Ke" + EVP null neutro (não verde). **Acumuladores null-aware (Codex):** `encargoTotal`/`encEmp` guardados (`if (cel.encargo != null)`) + flags `encargoTotalNull`/`encNull` → `null + x` nunca vira número. **Recomendações gated (Codex):** com hurdle ausente, `evp=null` = "não calculável" (≠ "destrói valor") → suprime prazo/despriorizar/crescer; **"Subir preço"** (margem-pura) segue; "Cortar desconto" motivo hurdle-aware. **Distingue `evp=null` por CUSTO ausente** (conservador atual preservado — teste de regressão) **de por HURDLE ausente** (flag `hurdle_indisponivel`, global). ⚠️ **Aviso de hurdle vive na confiança + banner da UI, NÃO nas recomendações por-cliente** — senão vazaria pro **A4** (`fin-next-best-action` mapeia `recomendacoesCliente` → candidatos) como N itens "Configurar hurdle" na fila do CFO (achado na execução, não no design). **Escopo (Codex): manter Ke; WACC é follow-up** (`A3.k=Ke` × `A2.wacc` divergem; trocar p/ WACC misturaria bugfix com mudança de metodologia + ampliaria a indisponibilidade quando falta Kd/dívida/PL — fica p/ quando o founder preencher os inputs). Helper `valor-cockpit-helpers.ts` (+`resolverHurdleCockpit`; **43 testes**) espelhado no `fin-valor-cockpit`; contrato `ValorCockpitResult` nullable (`k`/`encargo`/`encargo_total` + `hurdle_indisponivel`). **Codex em todas as etapas** (spec [3 P1 + escopo Ke×WACC] + plano [2 P1 regressão/misto + P2.3 asserts exatos + P3.6 UI neutra] + **adversarial: sem P1 financeiro**; único P1 = 4 erros de tipo nos testes antigos de identidade contábil pós-nullable, consertados com `!`). ⚠️ **Lição: a main consolidou o typecheck** — `typecheck:strict` foi removido (migração strict convergiu); o gate único agora é `bun run typecheck` (= `tsc -p tsconfig.app.json`). ⚠️ **Sem migration; REQUER deploy do `fin-valor-cockpit` via chat do Lovable** (EVP calculado no edge). Gate gestor comercial + master. Specs/planos em `docs/superpowers/{specs,plans}/2026-05-30-valor-cockpit-hurdle-guard*`.
- ✅ **Cockpit — caixa inicial da projeção exposto (transparência) + FRENTE DE CONSOLIDAÇÃO ENCERRADA (2026-05-31, PR #532, client-side, em produção)**. Alvo final escolhido por consult Codex (rodada 3, que avaliou a frente como madura). A engine A1 grava `saldo_inicial` em cada semana do snapshot (= `fin_contas_correntes.saldo_atual` ativo=true no momento da geração), mas o client **descartava** o campo → o Cockpit mostrava o saldo bancário "agora" (`totalCC`) **ao lado** da projeção 13s, sem dizer de qual caixa inicial ela partiu (divergência silenciosa quando o snapshot é de dias atrás). **Fix:** helper puro `parseSnapshotSemanas` lê `saldo_inicial` (jsonb→número; **null/ausente/não-número → null, NUNCA 0** — `Number(null)===0` seria fabricação, Codex adversarial P1) **fora do filtro rígido** (campo só-display não dropa a semana); `consolidarCockpit` expõe `caixa_inicial_projecao` (Σ do `saldo_inicial` da semana de **menor `inicio`** das empresas presentes — não `semanas[0]` literal, robusto se a semana 0 foi filtrada; **coorte vazia → null, não 0**, Codex P2) + `caixa_inicial_por_empresa`/`caixa_inicial_parcial`; helper puro `compararCaixaInicial` mostra o delta vs saldo atual **SÓ com coorte completa** (`caixa_inicial` da coorte parcial × `totalCC` das 3 = maçã×laranja). UI `Projecao13Card`: "Caixa inicial da projeção: Y · saldo atual Z · Δ W (a diferença pode refletir movimentações após o snapshot)" — **sem alerta/threshold** (não é diagnóstico; Codex). **Mesma fonte** engine×hook (`fin_contas_correntes.saldo_atual` ativo=true) → snapshot de hoje tende a Δ~0; defasado mostra quanto o caixa mudou. **Não muda nenhum número** (só expõe base+delta). **CLIENT-SIDE — sem migration/edge/deploy** (snapshot já grava o campo). Helper `cockpit-consolida-helpers.ts` (+`parseSnapshotSemanas`/`compararCaixaInicial`; **24 testes**). **Codex em todas as etapas** (spec [sem P1] + plano [sem P1; campo obrigatório>opcional] + **adversarial [P1 `Number(null)`→0, P2 coorte vazia→0, P3 teste do parser — todos consertados via helper puro]**). ⭐ **FRENTE DE CONSOLIDAÇÃO CROSS-TELA ENCERRADA:** os 3 furos "ausente vira número" (NCG no A2, hurdle no A3, caixa do snapshot) + a reconciliação de caixa estão fechados; o Cockpit foi religado às engines. **Veredito do Codex: a frente está madura** — o maior valor seguinte NÃO é mais reconciliação, e sim o **founder preencher os inputs de contabilidade** (folha/Ke/dívida/taxas em `/financeiro/valor`, `/financeiro/regime-tributario`, `/financeiro/funding`) que destravam regime/valor/funding/A3-hurdle confiantes (hoje degradam honestamente por falta deles). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-31-cockpit-caixa-inicial*`.
- ⚠️ **Deploy é manual via chat do Lovable após cada merge** — `omie-financeiro`, `fin-cashflow-engine` e as engines de valor/ação/regime/funding (`fin-valor-engine`, `fin-valor-cockpit`, `fin-next-best-action`, `fin-regime-tributario`, `fin-funding`) leem o arquivo do repo (branch main). Sem isso, a main e a produção divergem.

**Padrão de cron descoberto no Supabase do Lovable (2026-05-19, referência pra futuros crons)**:
- `pg_cron` + `pg_net` já habilitados.
- **Project URL (Supabase do Lovable):** `https://fzvklzpomgnyikkfkzai.supabase.co` — confirma a identidade do Supabase correto (NÃO confundir com o standalone `lkotrsfdvnwxqyevhffh`).
- **Auth canônico de cron** (usado por ~25 crons que já rodam): header `'x-cron-secret', (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name='CRON_SECRET' LIMIT 1)`. O secret `CRON_SECRET` já está no Cofre (Vault) — não precisa criar nem rotacionar. Coluna é `decrypted_secret` (não `value`); nome do secret é `CRON_SECRET` (maiúsculo).
- `cron.schedule(nome, schedule, comando)` faz **upsert por nome** → idempotente, pode rerodar.
- ✅ **Crons legados corrigidos (2026-05-19, BLOCO F+G)**: 3 crons da Reposição estavam com `x-cron-secret` placeholder (`<<COLE_O_CRON_SECRET_AQUI>>` / `<<48>>`) → falhavam 401. `gerar-pedidos-diario-oben` (9h15) e `disparar-pedidos-aprovados-oben` (13h) foram corrigidos pro padrão vault. `omie-cron-diario-oben` (diário 7h) era redundante com `afiacao_omie_oben_sync_incremental_2h` (mesma função/body, roda a cada 2h) → **deletado** via `cron.unschedule`. Lição: ao herdar crons de outro contexto, conferir se o `x-cron-secret` é placeholder antes de assumir que rodam.

- ✅ **Auditoria + recuperação de crons (2026-05-24)**: usuário reportou "dados desatualizados". Diagnóstico achou **401 sistêmico** — o `CRON_SECRET` no **Vault** divergiu da env var `CRON_SECRET` das **edge functions** (provável rotação/redeploy), então ~20 crons que mandam `x-cron-secret` do Vault levavam 401. **Lição-chave de diagnóstico:** `cron.job_run_details` mostra `succeeded` mesmo quando a edge function responde 401 — ele só registra que o `net.http_post` foi **enfileirado**. A verdade do HTTP está em **`net._http_response`** (status_code/content). Sempre cruzar os dois + frescor real das tabelas. **Fix:** rotacionei o `CRON_SECRET` (novo valor forte) no Vault **e** nas edge functions (chat do Lovable) — alinhou os dois lados, ~20 crons voltaram. Migrations: `20260524095612` (tira secret hardcoded de scoring/visit-recalc → lê do Vault), `20260524100500` (cria `fin-omie-sync-2x-diario` `0 8,14 * * *` + conserta `sayerlack-portal-watchdog` que lia secret de um GUC `app.settings.cron_secret` nunca setado).
- ✅ **Financeiro destravado (2026-05-24)**: contas a pagar/receber estavam **56 dias parados** por 3 motivos empilhados, todos escondidos atrás do 401: (1) **não existia cron** chamando `omie-financeiro` (só os `fin-*` que calculam EM CIMA dos dados) → criado o `fin-omie-sync-2x-diario`; (2) `omie-financeiro.validateCaller` **não aceitava `x-cron-secret`** (só `Bearer service_role`/JWT staff) → editado pra aceitar (preferido a pôr SERVICE_ROLE_KEY no Vault, que é lido por quem tem SQL Editor e bypassa toda RLS); (3) **bug nos triggers genéricos** `fin_audit_trigger` e `fin_period_lock_trigger` (migration `20260524102500`): o `CASE` de data fazia acesso **direto** `(NEW).data_movimento` — coluna que só existe em `fin_movimentacoes` — quebrando TODA gravação em `fin_contas_pagar/receber` com `column "data_movimento" not found`. **Padrão correto pra trigger multi-tabela:** ler campo via `(to_jsonb(rec)->>'col')::tipo` (tolera coluna ausente = NULL) em vez de `(NEW).col`. Também: `fin_period_lock_trigger` agora bypassa quando `auth.role()='service_role'` (sync do ERP espelha o Omie; a trava é pra edição humana via app — o snapshot do DRE preserva os números do fechamento). **Cobertura de paginação CP/CR (fix #1, 2026-05-24):** CP/CR/mov ficavam parciais porque o `sync_all` (uma invocação por empresa) tem **time-budget de 130s compartilhado**, e o **CP da colacor (116 págs) consumia tudo antes de CR/mov rodarem** (CR colacor parava em ~6 págs; mov em 0). ⚠️ **O `registros_por_pagina: 500` que tentei NÃO funciona — o Omie capa em 100/página e ignora valores maiores** (confirmado: deployado com 500, comportamento seguiu 100/pág; doc Omie diz limite 100). O 500 ficou no código mas é **no-op**. **Fix real = budget dedicado por entidade**, só via cron (sem editar a função): em vez de 1 cron `sync_all`/empresa, crons separados por entidade (`sync_contas_pagar`/`sync_contas_receber`/`sync_movimentacoes`) **escalonados no tempo**, cada invocação com os 130s inteiros → colacor CR (292 págs × ~0,4s ≈ 117s) cabe. Concorrência OK: as 3 empresas são **contas Omie distintas** (rate-limit por conta), então disparar as 3 juntas é seguro; escalono só entre entidades pra uma mesma conta não fazer CP+CR+mov ao mesmo tempo. Migration `20260525000000_*` cria os crons por-entidade e remove o `fin-omie-sync-2x-diario`. **Cursor de continuação (implementado, 2026-05-25):** mesmo dedicado, a colacor CR (~292 págs × ~1,1s ≈ 320s — gargalo é a latência do Omie, NÃO o banco; audit-skip do `service_role` em `20260525010000` deu só ganho marginal 115→126 págs) não cabe em 130s. Solução: tabela `fin_sync_cursor(company, resource, next_page)`; as actions `sync_contas_pagar/receber/movimentacoes` lêem `next_page` (param `startPage`), retomam de lá e gravam o progresso (NULL quando completam); cron `fin-sync-continuacao-10min` (`*/10`) avança só cursores pendentes (`next_page IS NOT NULL`) → colacor CR fecha em ~3 ciclos (~30 min); os crons por-entidade do #267 são o kickoff de cada passada nova. Migrations `20260525010000` (audit-skip) + `20260525020000` (cursor + cron). ⚠️ Requer redeploy do `omie-financeiro`.
- ✅ **Watchdog de integridade do sync + hardening do `omie-financeiro` + observabilidade do vendas (2026-05-26/27, PRs #321/#330/#338/#347, todos em prod)**. Dor original (este §5): o `CRON_SECRET` divergiu → ~20 crons 401 **silencioso**; `job_run_details` dizia "succeeded"; founder descobriu por reclamação dias depois. Agora há um vigia.
  - **Watchdog (SQL puro, sem edge function nova)**: funções `fin_sync_watchdog_check()` (cron `fin-sync-watchdog` `*/30`) + `fin_sync_heartbeat()` (cron `fin-sync-heartbeat` `0 11 * * 1-5`, dead-man-switch). Cruza 3 sinais por empresa×recurso (frescor `fin_sync_log.complete` >18h só p/ par ATIVO em 7d; erro recente; cursor `fin_sync_cursor.next_page` travado >2h) → grava em **`fin_alertas`** (UNIQUE parcial `(company,tipo) WHERE dismissed_at IS NULL` = anti-spam) e enfileira email em **`fornecedor_alerta`** (`tipo='outro'`, drenado por `dispatch-notifications`) só na **transição** ok→problema; dismiss ao resolver. Migration `20260525200000` (#321).
  - **Iteração 2 — varredura de órfãs (#330, migration `20260526030000_..sweep_orphans`)**: diagnóstico achou **25 linhas `fin_sync_log` travadas em `status='running'`** (até 57 dias) com **0 `error`** — porque o **`catch` do handler do `omie-financeiro` NÃO chamava `completeSync`** (+ kills `WORKER_RESOURCE_LIMIT`/timeout nem passam pelo catch) → a falha ficava órfã em `running`, nunca virava `error`, e o sinal `sync_error` NUNCA disparava (o bug **derrotava o próprio watchdog**). Fix duplo: (a) **código** — o catch agora finaliza o log como `error` (guarda `syncFinalized` p/ não sobrescrever sucesso; try/catch interno p/ não mascarar o 500); (b) **SQL** — o watchdog varre `running AND action LIKE 'sync_%' AND started_at < now()-30min` → `error` (`completed_at=now()` só p/ órfã <6h = alerta fresco, senão preserva `started_at` = limpa silencioso). **Premissa validada com dado: teto de duração legítima = 133s → `running` >30min é inequivocamente morto** (≠ staleness, não-confiável aqui pq o sync loga em rajada).
  - **Causa-raiz das órfãs de `sync_movimentacoes` (#338)**: mov completava em **p95 129s/max 131s** vs `TIME_BUDGET_MS=130s` e kill duro de 150s → raspava o teto e às vezes passava dos 150s antes de gravar cursor. Fix: **`TIME_BUDGET_MS` 130→100s** (folga; global, mas CP/CR/mov têm cursor+`*/10`, só faz mais ciclos) + `sync_movimentacoes` parou de gravar `metadata: {detalhes,resumo}` (payload Omie **bruto, 85% da linha ~1KB, que NINGUÉM lê** — `FinanceiroConciliacao`/`financeiroService` só usam campos já normalizados) → `metadata: null`. ⚠️ **Lição: o `metadata` NÃO era bomba de memória (linhas ~1,2KB); o driver das órfãs era o time-budget raspando, não memória.**
  - **Observabilidade do `omie-vendas-sync` (#347)**: o vendas NÃO logava em `fin_sync_log` → o watchdog não via falhas de vendas (silenciosas). Agora as 3 actions de SYNC (`sync_products`/`sync_estoque`/`sync_pedidos`) logam (running→complete/error, **best-effort**: falha de log nunca derruba o sync) com `action LIKE 'sync_%'` + `companies=[account]` → a varredura de órfãs + o sinal `sync_error` **cobrem vendas sem mudar o watchdog**. ⚠️ **O `WORKER_RESOURCE_LIMIT` do vendas NÃO era memória dos loads upfront** (diagnóstico: `sales_orders` ~1000, `omie_clientes` 6909 ≈ 2MB total) — NÃO otimizei memória (seria especulativo no money-path); só tornei observável. Causa real do resource-limit segue diagnosticável só pelos logs da edge no Lovable, se recorrer.
  - **Decisão de cadência (lição transversal)**: tentei na iter 1.5 uma staleness **adaptativa por par** (`2*avg_gap`) e deu **falso-positivo** — o `fin_sync_log` loga `complete` em RAJADA (a cada ~10min enquanto o cursor drena, depois silêncio quando idle), então "idle saudável" parece "quebrado". **Staleness por tempo-desde-complete é intrinsecamente não-confiável aqui; o sinal de `running` travado é o confiável.** Revertido.
  - **`farmer_copilot_sessions` SELECT own-only (#359, migration `20260527010000`)**: follow-up do #340 (que endureceu as 5 tabelas de relacionamento — ver §10). Das 2 tabelas sensíveis, decisão codex: **`farmer_calls` MANTÉM cobertura** (o Customer 360 lê por `customer_user_id` → o dono precisa do histórico COMPLETO de ligações do cliente; apertar quebraria a continuidade do 360), mas **`farmer_copilot_sessions` apertada pra own-only** (`pode_ver OR farmer_id=uid`) — é sessão de trabalho do vendedor com a IA, não histórico do cliente; nenhum consumidor lê por `customer_user_id`. Aplicada+validada em prod (`✅`).
  - **Incidente "vendas morto 8 dias" + 4 bugs sistêmicos de sync (2026-05-27, PR #378, em prod)**. O **Sentinela de Saúde de Dados** flagou `carteira_scores` stale; puxar o fio revelou **4 falhas silenciosas** (o `job_run_details` mascarava todas como "succeeded" — só registra o enqueue do `net.http_post`): **(1) Vendas congelado 8 dias** (`sales_orders` parou em 19/05; colacor em 17/04) porque **NENHUM cron chamava `omie-vendas-sync sync_pedidos`** (o criador inbound de pedidos; os crons de vendas ativos chamam `omie-analytics-sync`, que só ENRIQUECE pedido existente). O cron tinha sido perdido por **nunca ter sido versionado em migration**. Recriado oben+colacor (`vendas-sync-pedidos-{oben,colacor}-2h`, `5/20 */2`, janela rolante 5d, max_pages 10, idempotente por `hash_payload`, migration `20260527160000`) + backfill manual (oben→1246, colacor→1023). **(2) `calculate-scores` retornava 200 "6908 clients" mas NÃO gravava**: o update-path fazia `upsert(batch, {onConflict:'id'})` com batch parcial **sem `customer_user_id`/`farmer_id`** (NOT NULL sem default); `INSERT...ON CONFLICT` valida NOT NULL **antes** de detectar o conflito → batch inteiro estourava, erro só logado (`if (uErr) console.error` — não lançado) → `calculated_at` congelado em 25/05 (última vez que o SEED path, com registros completos, rodou). Fix: +`customer_user_id`/`farmer_id` no `ScoreUpdate` e no `updates.push` (redeploy via Lovable; log de prod mostrou o erro exato `null value in column "customer_user_id" ... violates not-null`). **(3) `daily-calculate-scores` sem `timeout_milliseconds`** → `pg_net` usa **default de 5s** → o recompute (~30s) era abortado antes de gravar. **(4) Mais 12 crons na mesma armadilha de 5s** (compute_costs, compute_association_rules, carteira-rebuild, sync-reprocess×2, process-recurring-orders, omie-sync-metadados/status-produtos, sync-colacor-vendas-products, monthly-report, weekly-audit, positivacao-snapshot) — re-agendados com timeout 150000 (migration `20260527170000`). **LIÇÕES (não repetir):** (a) **todo cron `net.http_post` PRECISA de `timeout_milliseconds` explícito** — o default de 5s mata SILENCIOSAMENTE qualquer função >5s, e o `job_run_details` mente "succeeded"; (b) **crons devem ser versionados em migration** (o de vendas viveu só no banco e sumiu sem rastro); (c) `sales_orders.created_at` é a **DATA DO PEDIDO no Omie** (vimos pedido futuro 18/06), NÃO a hora de inserção → péssimo sinal de frescor — usar `fin_sync_log` (action `sync_pedidos`). **Ponto cego do Sentinela exposto:** não havia check de VENDAS (por isso o maior bug passou 8 dias invisível — foi o check de carteira, downstream, que denunciou por tabela) → check de vendas via `fin_sync_log` adicionado na sequência. Desenho do cron revisado com codex (contas separadas/escalonadas, max_pages bounded, sem ressuscitar `sync_all`).
  - **Sentinela ATIVO (push) — fecha o ponto cego de vendas + o modo-de-falha raiz do incidente (2026-05-27, PRs #385/#391/#394/#401/#405/#409, em prod)**. O Sentinela era **pull-only** (badge/página/banner) → por isso o incidente passou 8 dias mudo (ninguém abriu). Agora é **ativo**: **(a) fonte única de verdade** `_data_health_compute()` (SECURITY DEFINER, sem gate de auth, REVOKE de anon/authenticated/PUBLIC) com **9 checks** — `get_data_health()` virou wrapper fino (auth + redação por papel via `pode_ver_carteira_completa`), contrato do frontend **idêntico** (#401). **(b)** `data_health_watchdog()` (cron **`data-health-watchdog` `*/30`**, SQL local — sem `net.http_post`, sem armadilha do timeout 5s) alerta na **transição ok→degradado** pros 4 domínios não-financeiros (`vendas_pedidos`/`estoque_inventario`/`reposicao_sugestoes`/`carteira_scores`) reusando o padrão provado do `fin_sync_watchdog`: INSERT em `fin_alertas` (`tipo='data_health_'||source`, UNIQUE parcial anti-spam) + IF FOUND enfileira `fornecedor_alerta`; **dismiss no ok**. Financeiro fica com o `fin_sync_watchdog` (donos disjuntos; `data_health_*` × `sync_*` não colidem). Severidade: critical→critico/urgente, warning→aviso/atencao. `company='oben'` é carrier (CHECK não aceita 'global'). **(c)** checks novos: `vendas_pedidos` (#385, via `fin_sync_log.sync_pedidos`, worst-of oben/colacor, stale>6h — fechou o ponto cego), `estoque_inventario`+`reposicao_sugestoes` (#394). Cron redundante `sync-orders-vendas-2h` (546-ava sempre, `omie-vendas-sync` já cobre order_items+sales_price_history) **removido** (#391). **(d)** heartbeat `fin_sync_heartbeat` estendido com seção de saúde de dados (dead-man-switch: ausência = vigia morto). **(e) baseline versionado dos 45 crons** (#405, `20260527230000`) = safety net anti-cron-perdido (lição do incidente). **(f) "quem vigia o vigia" (#409, eu+codex 2 consults):** 9º check **`alert_channel`** (domínio `alertas`) — `broken` se `fornecedor_alerta` preso em `pendente_notificacao` >2h **ou** ≥5 `falha_notificacao`/24h; **FORA do push do watchdog** de propósito (não se avisa a quebra do canal PELO canal quebrado — superfícies: dashboard pull + heartbeat + **ausência** do heartbeat). E **`dispatch-notifications` reagendado de DIÁRIO (`10 11`) pra `*/30`** (latência de alerta ~24h→~60min; idle barato = early-return em 0 pendentes antes do OAuth/Gmail; volume bounded pelo anti-spam) — job renomeado `afiacao_dispatch_notificacoes_30min`. **Validado em prod end-to-end**: `broken` pegou o backlog real (3 emails da cadência antiga) → invoquei dispatch (3/3 enviados, 0 falhas) → `ok`. ⚠️ **Fato operacional pra debug futuro:** dispatch agora é **`*/30`, não diário**; o baseline #405 ficou stale nesse 1 cron (tem o `_diario` antigo) → corrige na próxima regen. **(g) +2 checks HIGH (#419/#424, eu+codex):** triagem dos 45 crons → só 4 não-cobertos valem check (**não cobrir por simetria** — o resto vira ruído). Os 2 de FRESCOR DE DADO viraram check (agora **11 checks**): `custos_produtos` (`product_costs.updated_at`, daily→stale>30h) e `vendas_cadastros` (`max(updated_at)` de `omie_clientes`/`omie_products`). **LIÇÃO (anti-falso-alarme):** a 1ª versão do `vendas_cadastros` mediu o `sync_state` (job log) e ia alarmar — o diagnóstico em prod mostrou `sync_state.customers` preso em `running` há ~2 meses (o N+1 do `syncCustomers` estoura o budget e nunca chama `completeSync`, igual às órfãs do financeiro) MAS o DADO está fresco (o loop de upsert mantém vivo + outros crons) → refinei pra medir o DADO, não o job (reforça a filosofia: vigiar EFEITO NO DADO, não status técnico). **DEFERIDO restante:** só o `process-recurring-orders` — e é **no-op hoje** (foto de prod 2026-05-30: **0 recorrências cadastradas** em `recurring_schedules`; a feature de afiação recorrente está dormente). Check adiado por design (§5 "não cobrir por simetria" — card permanentemente-verde seria ruído); vira add de ~10 min (domínio novo `servico` + 1 UNION no `_data_health_compute` + IN do watchdog) **quando houver schedule ativo**. ✅ **Os 2 HIGH de AÇÃO promovidos ao push (2026-05-30, PR #490, migration `20260530190000`, eu+codex, em prod).** `disparar-pedidos-aprovados` (check `reposicao_disparo`, status='aprovado_aguardando_disparo', >48h/>168h, inalterado) e o portal Sayerlack entram no IN do `data_health_watchdog` (6→9). O check de portal foi **DIVIDIDO em dois** por semântica/SLA (consult adversarial codex): **`reposicao_portal_pipeline`** (automático DEVERIA drenar — `pendente_envio_portal`/`erro_retentavel` fresco/`enviando_portal`; >1h stale/>6h broken) e **`reposicao_portal_humano`** (NÃO drena sozinho — `indeterminado_requer_conciliacao` = risco de PO duplicado que o motor `sayerlack-retry-orfaos */15` deliberadamente não toca, `erro_nao_retentavel` = SKU sem mapeamento, `aceito_portal_sem_protocolo`, `falha_envio_portal`, `erro_retentavel` esgotado tentativas≥3/>3d; >2h stale/>24h broken). O check antigo via só `pendente_envio_portal` → era cego nos estados perigosos do incidente de R$82k. Severidade `warning` (push emaila igual; a mensagem carrega a urgência); frontend não muda (get_data_health repassa tudo; SaudeDados agrupa por domain). **Validado em cluster PG17 descartável E em prod** (14 checks, os 3 de ação `ok` com backlog limpo → push só na transição). ⚠️ **Drift descoberto no pré-check do apply:** as migrations `20260528020000`+`20260528194751` (que tinham criado `reposicao_disparo`/`reposicao_portal`) **NUNCA foram aplicadas** (armadilha §5) → prod rodava **11 checks** (a função viva era a do #488/`20260530160000`, base 11-check do #447 — sem os checks de ação); a `20260530190000` é `CREATE OR REPLACE`, **supersede as 2 órfãs** e leva prod a **14 checks**. (AT TIME ZONE + fix P1 já estavam vivos via #488; o net-new foi os checks de reposição + a promoção ao push + heartbeat rico.) ✅ **`syncCustomers` preso em `running` — RESOLVIDO (2026-05-28, PRs #435/#438/#439, em prod).** Estava travado há ~2 MESES (dado fresco via caminhos redundantes, mas o job nunca completava). Conserto em **3 camadas, cada uma revelada ao validar a anterior** (debugging em cascata): **(1) #435** N+1 removido (`syncCustomers` fazia ~2-3 queries/cliente × ~10k → 2 bulk reads `Map código→user_id`/`doc→user_id` + dedup por user_id + bulk upsert, padrão do `syncNaoVinculados`); **(2) #438** ainda dava `WORKER_RESOURCE_LIMIT` (enumeração de ~158 págs do Omie não cabe no budget SÍNCRONO) → a action `sync_customers` passou a rodar em **BACKGROUND via `EdgeRuntime.waitUntil`** (responde 202; `sync_state` running→complete é a verdade), customers REMOVIDO do `sync_all` (re-prendia o estado), + cron dedicado `sync-customers-vendas-daily` (`0 5`); **(3) #439** ainda morria com `Omie SOAP-ERROR: Broken response` (flakiness intermitente do Omie) → **retry/backoff em `callOmie`** (transitório: broken response/soap-error/5xx/timeout/rede; 4× com 0.8/1.6/3.2s; leitura idempotente). **Validado em prod: `sync_state.customers`=complete, total_synced=5238**, 158 págs nos logs. **LIÇÃO:** pra enumeração PESADA do Omie (~10k+), **remover o N+1 não basta — precisa rodar em background (`waitUntil`)** (escapa do budget do request) **+ retry** (a API do Omie flaka). O `vendas_cadastros` do Sentinela vigia a regressão (frescor do dado). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-27-sentinela-ativo-*`. ✅ **Falso-alarme do `last_error` em check ok — RESOLVIDO (2026-05-30, PRs #464 frontend + #488 SQL, em prod).** Card verde (ex.: `vendas_pedidos` com sync fresco) exibia `orphaned_running_timeout` em vermelho: o `last_err` do check pegava o último erro de TODO o histórico (desacoplado do status), carregando indefinidamente a flag que o `fin-sync-watchdog` grava ao varrer uma órfã de sync **JÁ recuperada** — o watchdog funcionando, a UI ressuscitando erro morto. Fix em **2 camadas**: **frontend** (`shouldShowDiagnostics(check)=status!=='ok'` em `health-helpers.ts`, esconde `last_error`/`how_to_fix`/`probable_cause` em check ok; cobre TODOS os checks) + **fonte** (`_data_health_compute` anula os 3 campos de diagnóstico no SELECT final quando `status='ok'` — gate genérico na projeção, CTE **verbatim** da 20260528140000, migration `20260530160000`). Aplicada+validada em prod (`11 ok / 0 com diagnóstico`). Mesma filosofia tail-failing; `data_health_watchdog`/`fin_sync_heartbeat` intactos (só leem message/status, só agem com status<>'ok'). ⚠️ Lição operacional: `codex exec` headless em background **trava** neste ambiente (47min sem output / stdin pendurado) → sempre envolver com `timeout` rígido e ter decisão solo de fallback. ⚠️ **Regressão por cascata multi-sessão + restore (2026-05-30, PR #493→#498, em prod).** O #493 ("filtros acionáveis" nos checks de ação) foi construído do corpo do #460 (SEM o split do #490) e, por timestamp maior + `CREATE OR REPLACE`, **reverteu o `_data_health_compute` pro `reposicao_portal` ÚNICO** sem tocar `data_health_watchdog`/`fin_sync_heartbeat` (que o #490 fez referenciar `reposicao_portal_pipeline`/`_humano`) → **push de portal ÓRFÃO** (o watchdog procurava `source` names que o compute não produzia mais; o frontend perdeu o split). Fix-forward **#498** (`20260530210000`) restaura o **#490 VERBATIM** (SQL idêntico, confirmado por `diff`; supersede o #493 por timestamp + CREATE OR REPLACE). **Diagnóstico em prod confirmou a regressão** (o compute devolvia `reposicao_portal` único) → pós-restore re-validado em prod: **14 checks**, `reposicao_portal_pipeline`+`reposicao_portal_humano` ambos `ok`, watchdog/heartbeat re-sincronizados. **Higiene junto (#498, `20260530210001`):** `cancelar_pedido_sugerido` agora zera `status_envio_portal='nao_aplicavel'` + `portal_proximo_retry_em=NULL` (cancelado parava de ficar `pendente_envio_portal` poluindo Pendentes/KPIs/check `reposicao_portal_pipeline`; espelho no `useDetalhesModal` pro cancelamento via UI). 🔴 **LIÇÃO (3ª vez que a cascata morde ESTE arquivo — #460/#490/#493):** `_data_health_compute` é arquivo QUENTE multi-sessão; ao mexer, **parta SEMPRE da migration de MAIOR timestamp** (nunca de um corpo antigo de `CREATE OR REPLACE`) e, se tocar o compute, **recrie `data_health_watchdog`+`fin_sync_heartbeat` JUNTO** (os IN-lists referenciam os `source` names — mudar o conjunto de checks sem atualizar os dois desincroniza o push). O cabeçalho da `20260530210000` deixa esse aviso pra próxima sessão.
  - **Falso-susto "[Sync parado]" do CR/CP — investigado e DELIBERADAMENTE não-mexido (2026-05-30, sem PR).** O founder recebeu 3 emails `[Sync parado]`/`sync_stale` urgentes: `contas_receber` colacor+oben (alerta 29/05 00:00) e `contas_pagar` colacor_sc (30/05 02:30). **Diagnóstico (read-only no SQL Editor): eram eventos PASSADOS já auto-curados.** Prova: TODOS os `fin_alertas` correlatos já tinham `dismissed_at` preenchido (o watchdog dismissa sozinho quando o sync volta — ex.: CR colacor+oben criado 29/05 03:00 → dismissado 29/05 08:30); o DADO estava fresco (`max(updated_at)` de `fin_contas_receber` 30/05 ~14:2x-14:41; colacor 29.224 títulos = backfill #426 fechado; oben 12.203; sc 961); crons todos `active:true`; CR/CP rodando `succeeded`; cursores de CR/CP = `null` (passada completa). **Causa-raiz: outage TRANSITÓRIO do Omie (`SOAP broken response`) em 28/05, ANTES do retry do #420 chegar em prod** → toda passada de CR abortava → >18h sem nenhum `complete` → a **seção 1 (`sync_stale`, >18h)** do `fin_sync_watchdog_check` disparou (corretamente). Com o retry #420 ativo, estabilizou e o watchdog se auto-curou. **Email não some da caixa sozinho; o alerta no banco sim** — por isso o susto parecia atual. **Decisão founder+análise: NÃO endurecer o watchdog.** A seção 1 é o backstop de "realmente parado" e foi exatamente ela que PEGOU este incidente; afrouxá-la pra "ignorar quando o cursor avança" arriscaria cegar um outage real lento (o anti-padrão do próprio §5: vigiar EFEITO NO DADO, não status técnico). O `#420` (retry/backoff em `callOmie` p/ fault transitório) já ataca a **recorrência**. O `sync_error` já foi de-noised na iteração 3 (tail-failing, #423). ⚠️ **Detalhe de código confirmado na investigação** (relevante se um dia for mexer): passada de backfill que estoura o `TIME_BUDGET_MS` (100s) **retorna normal** (`{complete:false, nextPage, timedOut:true}`, NÃO lança) → o handler chama `completeSync(..., undefined)` → vira **`status='complete'` parcial**. Logo "stale" durante backfill saudável só acontece se o Omie derrubar TODAS as tentativas por >18h (= outage real, que é o que queremos alertar). **Armadilha de diagnóstico (não repetir):** contar "abertos" por `status_titulo IN ('ABERTO','VENCIDO')` dá **sempre 0** — o banco usa vocabulário NATIVO do Omie (`'A VENCER'`/`'ATRASADO'`/`'VENCE HOJE'`); usar `OPEN_TITLE_STATUSES` de `src/lib/financeiro/titulo-status.ts`.
  - **`[Sync parado]` REAL — edge `omie-financeiro` parou de CARREGAR (`503 LOAD_FUNCTION_ERROR`); redeploy resolve (2026-06-11).** Mesmo sintoma do falso-susto de 30/05 (3 e-mails `[Sync parado]` urgentes, CP/CR/mov das 3 empresas, >18h) mas **causa oposta e REAL**: os `fin_alertas` estavam **ATIVO** (não dismissados) e **zero `complete` no dia**. **Diagnóstico read-only (4 sinais que desambiguam em minutos):** (a) `fin_sync_log` sem NENHUMA passada de CP/CR/mov iniciada hoje — **nem `running`** → a edge nem começava; (b) syncs de **vendas** (edges diferentes) = `200` → não é global/Omie; (c) **zero `401`** → não é `CRON_SECRET` (descarta o incidente de 24/05); (d) nenhum cursor travado. **Causa-raiz no `content` do `net._http_response`** (o `cron.job_run_details` mente "succeeded"): `{"code":"LOAD_FUNCTION_ERROR","message":"Failed to load edge function"}` (503) + `WORKER_RESOURCE_LIMIT` (546) + timeouts 60s → o **gateway Supabase não conseguia BOOTAR a edge** (por isso nunca logava `running` — o handler insere `running` só DEPOIS do boot, no `try`). Código intacto desde #477 → não é código; provável colateral de um publish de edges no Lovable na véspera (`enviar-push`/`tint-sync-agent`/`sayersync`). **Conserto (sem código):** (1) redeploy **verbatim da main** via chat do Lovable → `Active`; (2) forçar passada via `net.http_post` no SQL Editor (`…/functions/v1/omie-financeiro`, header `x-cron-secret` do Vault, body `{action:'sync_contas_pagar'|'sync_contas_receber'|'sync_movimentacoes', company}` por empresa, `timeout_milliseconds:=150000`) — senão o próximo sync só seria no cron 8h/14h e a continuação `*/10` não roda sem cursor pendente; (3) `SELECT public.fin_sync_watchdog_check()` → dismissa os `sync_stale` quando os 3 recursos de cada empresa têm `complete` <18h. **LIÇÕES:** (a) `503 LOAD_FUNCTION_ERROR`/`546`/timeout no `net._http_response` **+ zero `running` no `fin_sync_log`** = edge **não carrega (boot)**, NÃO Omie/auth/cursor → o fix é **redeploy**, não código (o watchdog vê o EFEITO, é cego pra causa de boot); (b) **sempre ler `content`/`error_msg` do `net._http_response`** (`WHERE status_code>=500 OR status_code IS NULL`) — o sinal que o `job_run_details` esconde; a tripla "vendas OK / zero 401 / zero `running`" mata as hipóteses global/secret/Omie em segundos; (c) o watchdog FUNCIONOU (pegou em 18h) — o gap foi só o tempo até VER o e-mail.

- **Auditoria "Max" do financeiro → root-cause da data de baixa ausente (2026-05-27, PR #396 em prod; resto especado/planejado).** Varredura achou que **`fin_contas_receber.data_recebimento` e `fin_contas_pagar.data_pagamento` (a data de baixa real) estão SEMPRE NULL** (39k+ liquidados) — **provado com payload cru**: o endpoint LIST do Omie (`ListarContasReceber`) NÃO retorna a baixa. Cascata em 5 engines: **(1) PMR/PMP/Ciclo** (já corrigido, degradação honesta #381 — mostra "—"); **(2) `getFluxoCaixa` realizado** (`entradas/saidas_realizadas` só somavam com baixa → **sempre 0**); **(3) aging→projeção 13s** (`liquidado=!!data_recebimento` sempre false → `taxa_recebimento=0` confiantemente errada); **(4) valor-cockpit** (todo título conta como aberto → AR médio inflado); **(5) DRE caixa** (sempre fallback de vencimento "estimado" — já honesto). **A baixa real EXISTE** em `financas/mf/ListarMovimentos` (`dDtPagamento`, gravado em `fin_movimentacoes.data_movimento`, joinável por `omie_codigo_lancamento`=`nCodTitulo`) MAS o sync chama **sem filtro de data** (default 3 meses no código) → só ~5 meses (movimentos começam **2025-12-29**; cursor completo) → cobertura de baixa derivável **17% CR / 23% CP** na oben (CR 97% são, PMR ~32d; CP 38% anômalo). **Confirmado em código real:** o `mf` aceita `dDtPagtoDe`/`dDtPagtoAte` (DD/MM/YYYY) → passar a janela destrava o histórico em massa, **sem N+1**. **JÁ FEITO (#396):** Fase 1 — `getFluxoCaixa` realizado agora soma `fin_movimentacoes` por dia (E/S, exclui transferência sem título, paginação anti-truncamento); helper `src/lib/financeiro/fluxo-realizado-helpers.ts`. **[⚠️ ESTE PARÁGRAFO "PLANEJADO/Dependência aberta" É O DIAGNÓSTICO ORIGINAL DE 27/05 E ESTÁ SUPERSEDED — o programa inteiro foi CONCLUÍDO; ver "PROGRAMA DA DATA DE BAIXA 100% FECHADO" abaixo. Mantido por histórico.]** ~~PLANEJADO (spec+plano em `docs/superpowers/{specs,plans}/2026-05-27-omie-baixa-date-*`):~~ Fase 0 = backfill histórico de movimentos com filtro de data — **desenho A do codex: persistir janela no `fin_sync_cursor`** (`filtro_data_de/ate`+`mode`; a continuação `*/10` passa só `{action,company}` → a função lê a janela do cursor; **congelar `filtro_data_ate` no kickoff**; **guard de concorrência** contra race de cursor; colunas neutras pra CR/CP; medir 1 empresa/ano antes). Codex **rejeitou** o "deploy amplo→reverter" (frágil em money-path). Fase 3 = derivação em tabela lateral `fin_titulo_baixas` (provenance/confiança, ponderado por valor, só `dDtPagamento`) + ligar aging/valor/DRE/PMR. **Decisão de sequência (eu+codex): derivação na janela recente PRIMEIRO** (cobertura alta lá; é o que importa pro CFO; `fin_titulo_baixas` recomputável → forward-compatible com backfill depois), **backfill adiado** (money-path sensível, não rushar). **Dependência aberta:** rodar `{action:'debug_raw',entidade:'movimentacoes',company:'oben'}` no chat do Lovable (travou 403 — precisa preview logado) pra desenhar o filtro limpo do CP.
  - ✅ **ACHADO DE MAIOR IMPACTO — contas a receber do colacor truncado em 2022 (PR #426, em prod, confirmado).** Puxando o fio da baixa (a cobertura derivável do colacor era só ~9% mesmo pós-backfill de movimentos), o diagnóstico revelou que **o `fin_contas_receber` do colacor estava CONGELADO em set/2022** — `max(data_emissao)=2022-09-18`, **ZERO títulos emitidos em 2023+**, enquanto o CP estava atual (até 2028). `debug_raw` provou: o Omie tem **~29.212** recebíveis do colacor (até hoje) mas só **~12.800** (os mais ANTIGOS) eram sincronizados → **faltavam ~16.400 recebíveis recentes (2023-2026)**: receita/aging/projeção/caixa do colacor 3,5 anos defasados. **Causa-raiz:** `syncContasReceber`/`syncContasPagar` confiavam no `total_de_paginas` do Omie (**sub-reportado p/ listas grandes**) + `registros_por_pagina: 500` (o Omie **ignora**, devolve 100/pág — o 500 fazia o total_de_paginas mentir) + early-exit por `validRows`-vazio (página de código-nulo contava como fim). CP só não estourava por ser lista menor (mesmo código, idêntico). **Fix (#426, codex challenge 2 rodadas):** paginação **data-driven** em CR **e** CP — `PAGE_SIZE=100` (limite real), paginar até a **página VAZIA** (fim real; página parcial no meio NÃO trunca — codex #3), **guard anti-loop** por fingerprint (1º código+count) se o Omie repetir página em vez de devolver vazia (codex #1), removido o early-exit por `validRows`; `total_de_paginas` só p/ log; cursor preserva resume por time-budget (`writeCursor` grava `next_page` sempre que `!complete`, não só em timeout — codex #2). ✅ **Confirmado em prod:** após redeploy + reset do cursor CR/CP do colacor + re-sync, **colacor CR saltou 12.800 → 29.212** (= total do Omie), `max_emissao=2026-05-27`, +12.599 recebíveis 2023+. ⚠️ **oben/sc** (listas menores, já 100% de cobertura de baixa) provavelmente já completas, mas **auto-curam no próximo kickoff agendado** (cursor NULL → re-sync da página 1 com o código novo). **Implicação pra Fase 3:** com os recebíveis recentes do colacor agora presentes + os movimentos 2023-2026 (backfill #407), a cobertura de baixa derivável do colacor deve subir muito (re-rodar a query de cobertura). **Lição:** nunca confiar no `total_de_paginas` do Omie em listas grandes — paginar até página vazia/parcial. O `registros_por_pagina:500` documentado como "no-op" no sync de movimentos era na verdade **ativamente nocivo** no CR/CP (corrompia o total_de_paginas).
  - ✅ **Fase 3 (início) — PMR/PMP/Ciclo ligados à baixa derivada (PRs #434 + #442, em prod, validado).** Duas views `security_invoker` (apply manual via SQL Editor, confirmado): **`v_titulo_baixas`** (#434) = baixa por título derivada de `fin_movimentacoes` (NUNCA grava na coluna base — o sync re-zeraria; **só títulos liquidados** RECEBIDO/LIQUIDADO p/ CR, PAGO/LIQUIDADO p/ CP; type-match CR↔'E'/CP↔'S'; `valor>0`; `prazo_ponderado_dias` = média emissão→baixa ponderada por valor; 2 P1 do codex no desenho) → **`v_capital_giro_prazos`** (#442) = agrega em PMR/PMP ponderado por valor + **cobertura** (fração dos liquidados com baixa derivável) por empresa. O front (`getCapitalDeGiro`) lê **1 linha/empresa** (em vez de baixar ~14k títulos) com **gate de confiança no consumidor** (`COBERTURA_MIN=0.4`): cobertura <40% → PMR/PMP/ciclo = `null` ("—", degradação honesta). **Confirmado em prod:** oben PMR 30 / PMP 10 / ciclo +20d (100% cobertura), colacor_sc 4/5/−1d (100%) → **números reais**; **colacor cobertura ~10%** (PMR 20, PMP 77 sobre amostra de 10% — exatamente o número enganoso que o gate esconde) → **"—"**. ⚠️ **A cobertura ~10% do colacor é teto real, não bug:** o colacor liquida ~80-90% dos títulos **sem movimento `mf` individual** joinável (5.112 títulos com baixa de ~40k); ≠ oben/sc que casam ~100%. Substituiu as 2 queries que liam `data_recebimento`/`data_pagamento` (sempre NULL) + os loops de PMR/PMP. ~~**Restante da Fase 3 (ADIADO p/ sessão dedicada, money-path):** ligar **aging→projeção 13s** (`fin-cashflow-engine` + helper `aging-helpers`), **valor-cockpit** (`fin-valor-cockpit`), **DRE caixa** (`omie-financeiro`) à `v_titulo_baixas`~~ → **✅ SUPERSEDED (2026-05-31): TUDO CONCLUÍDO E DEPLOYADO** — aging→projeção (#458), valor-cockpit AR (#474), DRE-caixa (#477) já leem a baixa derivada; os 3 engines redeployados; cascata 5/5 fechada + backfill concluído. Ver os bullets "DRE-caixa via baixa derivada (#477) — FECHA A CASCATA (5/5)" e "PROGRAMA DA DATA DE BAIXA 100% FECHADO" abaixo (= **NADA aberto**). **`getFluxoCaixa` realizado já usa movimentos diretos** (#396, não depende dessas views).
  - ✅ **Fase 3 — aging (projeção 13s) + indicadores do engine ligados à baixa derivada (PRs #458 + #463, mergeados; ⚠️ redeploy do `fin-cashflow-engine` pendente).** **#458 (aging):** o `calibrarCurvas` (helper `aging-helpers.ts` + espelho verbatim no engine) tratava TODO título como aberto (data_recebimento sempre NULL) → `taxa_recebimento=0` com `confianca='alta'` ("ninguém recebe") nas faixas com volume = **confiantemente errado** na projeção de caixa. Fix: `liquidado` por **STATUS** (RECEBIDO/LIQUIDADO/PAGO); `valorPagoEfetivo` robusto (valor_recebido → doc−saldo → face, cobre `valor_recebido=0`+saldo cheio do #396); timing (lag/faixa) via `v_titulo_baixas.data_baixa_final` (query paginada via `fetchAllRows` — oben CR ~11k > cap 1000); **gate cobertura-empresa ≥40%** (colacor ~10% = subconjunto enviesado → default+`baixa`) + **gate por-faixa** (`pago>0` & `countLiqData≥5`); liquidado sem baixa derivada → excluído (sabe QUE pagou, não QUANDO). **#463 (B1 — indicadores do engine):** o `calcularIndicadores` do MESMO engine ainda mostrava **PMR=0d/PMP=0d** em `NcgDecomposicao` (divergindo do card client-side). Agora lê a **`v_capital_giro_prazos`** (mesma fonte+gate do `getCapitalDeGiro` → consistência) via helper `prazoComGate`; CCC=null se PMR ou PMP null; contrato `Indicadores.{pmr,pmp,ccc}` → `number|null` (front `useCashflowProjection`+`NcgDecomposicao` render "—"). PMR/PMP/CCC **não** são persistidos no snapshot (só retorno ao vivo) → sem risco de constraint. **Escopo isolado (codex):** a baixa derivada entra SÓ na calibração+indicadores; **`dias_cobertura` é o B2 pendente** (precisa de caixa real das movimentações OU saída projetada — decisão de fonte + janela efetiva + exclusão de transferência, validado c/ codex). Ainda da Fase 3: **valor-cockpit** (`fin-valor-cockpit`) e **DRE caixa** (`omie-financeiro`). ⚠️ **Cada PR que toca o `fin-cashflow-engine` exige redeploy manual via chat do Lovable** (ler de `supabase/functions/fin-cashflow-engine/index.ts` na main, deploy verbatim) — senão main e prod divergem.
  - ✅ **Fase 3 — dias_cobertura (B2, #471) + valor-cockpit AR (#474), mergeados; ⚠️ redeploy de `fin-cashflow-engine` E `fin-valor-cockpit` pendente.** **#471 (dias_cobertura):** lia `cps.data_pagamento` (NULL) → `dias_cobertura=999` ("cobertura infinita") → o alerta "Caixa cobre só X dias" nunca disparava. Fix (codex: **forward projetado**, evita classificar transferência): `dias_cobertura = saldo_cc / (Σ total_saidas do horizonte / horizon*7)` = CP por vencimento + folha/impostos (eventos), operacional por construção. `saldo≤0`→0 crítico; sem base→null (alerta pula); contrato `number|null` (coluna do snapshot já nullable; só alerta+snapshot, não renderizado). Helper `diasCoberturaProjetado`. **#474 (valor-cockpit AR):** `arMedioTTM` lia `data_recebimento` (NULL) → todo título "aberto" com **saldo cheio** (#396) → AR médio inflado → encargo inflado → **EVP por cliente/SKU subestimado**. Fix (codex): liquidado por STATUS; fechamento via baixa derivada → **VENCIMENTO como proxy MARCADO** (não excluir = otimista); liquidado contribui `valor_documento` (não saldo cheio); aberto `saldo` (fallback doc−recebido); **gate global** cobertura<80%→ar_indisponivel (oben ~100%; protege escopo futuro); `cobertura_baixa_ar` na resposta; guard ISO no `.or()`. `arMedioTTM` retorna `{ar_medio,v_real,v_proxy,v_sem_fecho}`; +11 testes. Escopo **oben** (`omie_products.account`). **Estado da cascata da data de baixa:** PMR/PMP/ciclo (cards+engine), getFluxoCaixa realizado, aging→projeção 13s, dias_cobertura e valor-cockpit = ✅; **resta só o DRE-caixa** (`omie-financeiro/calcularDRE`, que já degrada honesto "estimado" — menor impacto). ⚠️ **Redeploy pendente dos DOIS engines** (`fin-cashflow-engine` p/ #458/#463/#471 + `fin-valor-cockpit` p/ #474) via chat do Lovable, verbatim da main.
  - ✅ **Fase 3 — DRE-caixa via baixa derivada (#477, mergeado; ⚠️ redeploy de `omie-financeiro` pendente) — FECHA A CASCATA (5/5).** `calcularDRE(regime='caixa')` alocava tudo pelo VENCIMENTO ("estimado", `fallback_pct`≈100%, data_recebimento/pagamento sempre NULL). Agora aloca pela **baixa derivada** (`v_titulo_baixas`); vencimento só fallback. **3 passes codex** (risco = double-count no DRE): load por **(venc-no-mês) ∪ (baixa-no-mês)** + **dedupe por `omie_codigo_lancamento`** (sem o ramo baixa-no-mês, título pago atrasado sumiria; dedupe evita double-count venc∩baixa); **baixaMap paginado + THROW em erro** (parcial reintroduz double-count → nunca degrada silencioso); `.in()` chunked; **`valorCaixaEfetivo`** (liquidado tem valor_recebido=0 #396 → `0??doc` mantinha 0 → alocaria ZERO; usa face). **Competência INTACTO**; `resolverDataCaixa` inalterado; `fallback_pct` cai (real) → confiança sobe; colacor ~10% segue honesto. Helpers puros `valorCaixaEfetivo`+`dedupePorCodigo` (+6 testes); espelho verbatim. **Limitação v1:** parcial em meses diferentes → `data_baixa_final=MAX` no mês da última parcela (raro; v2=por movimento). **CASCATA DA DATA DE BAIXA FECHADA (5/5):** PMR/PMP/ciclo · getFluxoCaixa · aging/projeção 13s · dias_cobertura · valor-cockpit · DRE-caixa = ✅. ✅ **Os TRÊS engines redeployados** (verbatim da main, confirmado Active no Lovable): `fin-cashflow-engine` (#458/#463/#471), `fin-valor-cockpit` (#474), `omie-financeiro` (#477). **Backfill (Fase 0) CONCLUÍDO — ver bullet abaixo** (specs em `docs/superpowers/{specs,plans}/2026-05-27-omie-baixa-date-*`).
  - ✅ **Backfill de movimentos CONCLUÍDO + teto de cobertura do colacor confirmado ESTRUTURAL (2026-05-30) — PROGRAMA DA DATA DE BAIXA 100% FECHADO.** O backfill (`sync_movimentacoes` com `filtro_data_de` amplo + drain da continuação `*/10`) já rodou: hoje os movimentos cobrem o histórico inteiro — **colacor 2020-04-17→2026-05-30 (8.845)**, oben 2023-09-13 (33.712), sc 2024-07-10 (3.879). ⚠️ **A nota "movimentos começam 2025-12-29" (bullet do root-fix acima) é do diagnóstico de 27/05 e está STALE.** Cobertura de baixa derivável medida em prod via `v_capital_giro_prazos` + contagem crua: **oben 100% CR/CP** (11.402/11.402 · 2.581/2.581 → PMR 30 / PMP 10 reais), **colacor_sc 100%** (876/876 · 719/719 → PMR 4 / PMP 5), **colacor 9,5% CR (2.693/28.381) · 10,2% CP (1.155/11.299)** → abaixo do gate 40% → **"—"**. **TETO ESTRUTURAL CONFIRMADO e TIME-INVARIANT:** o backfill NÃO moveu o colacor — a janela de 3 meses dava ~10%, o histórico cheio (2020+) dá os MESMOS 9,5%/10,2% (PMR 20 / PMP 77). O colacor liquida **~90% dos títulos SEM movimento `mf` individual joinável** por `nCodTitulo` (baixa agregada por lote/conta-corrente). **A expectativa do #478 ("backfill eleva a cobertura do colacor") estava ERRADA** — não é gap de histórico, é o **padrão de baixa** do colacor; puxar mais movimento não ajuda. **colacor fica em "—" por design** (degradação honesta — PMR/PMP de um subconjunto enviesado de 10% seria pior que nada; oben/sc baixam 1:1 → 100% reais). **Estado final do programa:** cascata 5/5 (PMR/PMP/ciclo · getFluxoCaixa · aging/projeção 13s · dias_cobertura · valor-cockpit · DRE-caixa) + 3 engines redeployados + backfill concluído + teto do colacor provado estrutural = **NADA aberto**. **v2 de alocação por título DESCARTADO (eu + codex, 2026-05-30).** Diagnóstico do gap em prod: **0% dos títulos liquidados do colacor têm `omie_codigo_lancamento` NULL** (CR 0/28.381, CP 0/11.299 → o link EXISTE) MAS **~90% não têm movimento `mf` individual casando** (CR 25.688/28.381 = 90,5% · CP 10.144/11.299 = 89,8%). O colacor baixa em **LOTE/conta-corrente** (8.845 movimentos pra ~40k títulos liquidados — 1 movimento quita N títulos, ou a baixa nem gera `mf` por título). Codex (adversarial): alocar valor de baixa em lote across títulos é **modelo inventado** (FIFO/rateio) — não prova qual título foi quitado nem em que proporção, mistura tarifa/transferência/juros/desconto → desloca PMR/PMP dezenas de dias = número de CFO falso no money-path. **Critério de morte batido** (link determinístico inexistente p/ 90% + cobertura <40% → só heurística). **colacor fica em "—" (correto).** **Única alternativa honesta** (futuro opcional, se precisar de capital de giro do colacor): **DSO/DPO contábil agregado** (AR médio ÷ receita diária; AP médio ÷ compras/CMV diário) — rotulado como tal, auditável, NÃO derivação de baixa por título; o subconjunto 10% rastreável só como diagnóstico ("PMR rastreável 20d, cobertura 9,5%"), nunca KPI.
  - ✅ **DSO contábil agregado pro colacor — a "alternativa honesta" acima, ENTREGUE (2026-05-30, PR #500, eu + codex 2 consults).** Card "Lente contábil agregada (DSO)" em `/financeiro` → Posição Agora (**colacor-only**, seção separada do Ciclo que mostra "—"). **DSO = AR aberto hoje ÷ receita_bruta diária TTM** (point-in-time, 12m fechados competência); NÃO usa data de baixa → funciona onde o PMR title-based é "—". **DPO DROPADO na v1** (codex): AP do colacor (792k) >> CMV (199k) → DPO-sobre-CMV ~1057d incoerente (AP inclui matéria-prima/capex/tributo ≠ CMV; sem denominador de compras confiável). **Guard de plausibilidade** (DSO/DPO >730d → null) nunca exibe falsa precisão. Degradação honesta (receita≤0 ou <12m → null; AR=0 c/ receita>0 → 0). Helper puro `src/lib/financeiro/dso-dpo-helpers.ts` (19 testes vitest, incl. janela TTM bissexto) + soma de AR/AP **paginada** (robusto vs cap 1000). **CLIENT-SIDE — sem migration/edge/deploy** (lê `getDRE` + soma de saldo aberto). ⚠️ **ACHADO (data-quality): o DRE competência do colacor estava com receita ZERADA** — artefato pré-#426 (o CR ficou congelado em 2022 até o #426; os snapshots de `fin_dre_snapshots` não tinham sido regenerados pós-fix) → o DSO nascia "—". **Data-fix:** invocar `calcular_dre` regime competência (2025+2026 p/ o TTM; 2023-2024 p/ o histórico) — lê o CR agora-completo + faz **upsert em `fin_dre_snapshots`**. ✅ **Validado em prod (2026-05-30):** receita_bruta_ttm 0 → **R$1,16M**, meses 11→**12** → **DSO colacor ≈ 45 dias** (real, sano); o guard nula o DPO (~1057d > 730). **Contexto do founder:** a receita do colacor cai ano-a-ano (2023 R$4,05M → 2024 R$2,9M → TTM R$1,16M) porque **parte do faturamento do colacor MIGROU pra oben** — é shift de negócio real, NÃO buraco de sync (o DRE reflete o CR completo pós-#426). **LIÇÃO: após um fix de FONTE (#426 destravou o CR), os snapshots de DRE NÃO se regeneram sozinhos — `calcular_dre` precisa ser re-invocado** (ele faz upsert no snapshot que o `getDRE` lê). Specs/consults em `.context/dso-dpo-*`.

**Sempre que adicionar migration nova**: avisar no PR description "**ATENÇÃO: migration manual necessária**" e colar o SQL no body do PR + entregar inline na conversa.

### Deploy do FRONTEND (app) — Publish MANUAL no Lovable (lição 2026-05-31)

> 🔴 **O Lovable NÃO auto-deploya o frontend a partir de push no GitHub.** Mergear PR na `main` deixa o código na main, mas o app em `steu.lovable.app` **continua servindo o build anterior** até alguém clicar **Publish** no editor do Lovable (e o editor pode precisar **sincronizar com o GitHub** antes — senão publica um estado velho dele). Isso pegou de surpresa: após mergear 6 PRs eu disse "está pronto pra QA" — mas o ar ainda era um build velho (provado nos bytes). **Sempre lembrar o founder de dar Publish** quando quiser uma feature live. As TRÊS coisas são deploy manual e independente: **frontend** (Publish no editor), **edge functions** (chat do Lovable — ver abaixo), **migrations** (SQL Editor — ver acima). NENHUMA acontece sozinha no merge.

> 🟢 **Como VERIFICAR um deploy do frontend sem acesso ao Lovable (pelos bytes do bundle):** o `steu.lovable.app/` vem com `cache-control: no-cache` e referencia `/assets/index-<hash>.js`. (1) `curl` o index → o `<hash>` **muda** quando sobe build novo (mesmo hash = não publicou / não propagou). (2) Pra confirmar que um COMMIT específico entrou, escolha uma **string-literal única** dele (ex.: um `.select('razao_social, name, document')` novo, um texto de UI, uma rota nova como `/sales/new`), baixe os chunks `/assets/*.js` referenciados e `grep`. ⚠️ **O Vite agrupa por content-hash e pode jogar um hook/módulo num chunk de nome INESPERADO** (visto: um hook do unified-order caiu no `TintColorSelectDialog-*.js`) → grepar só os chunks de nome "óbvio" dá **falso-negativo**; **varra TODOS os chunks** referenciados pra ter certeza.

### Edge functions — caminho oficial Lovable (confirmado via docs.lovable.dev 2026-05-19)

> 🔴 **REGRA**: Edge functions no Lovable Cloud são **criadas e editadas pelo chat do Lovable**, NÃO pela UI de "Edge functions" do Cloud. A UI Cloud → Edge functions é **só pra visualizar logs, status e invocations**. Não há botão de "Create function" / "New function" ali.

> 🟢 **CAPACIDADE CONFIRMADA (2026-05-19, descoberta durante deploy de omie-financeiro)**: o Lovable AI **CONSEGUE ler arquivos do diretório `supabase/functions/<nome>/index.ts`** no repo do projeto. Inicialmente o founder achou que não — mas o próprio Lovable AI confirmou capacidade quando precisou montar a função após truncamento do histórico de chat. Caminho preferencial para edge function GRANDE (>500 linhas) ou EDIT de função existente: instruir Lovable AI a ler `supabase/functions/<nome>/index.ts` do repo e fazer deploy verbatim, em vez de pastear código no chat (que pode truncar). Para edge function nova pequena (<300 linhas) ou quando o arquivo ainda não existe no repo, continuar pastando código no chat.
>
> Doc oficial: `https://docs.lovable.dev/integrations/cloud` diz: *"Edge Functions: Easy to create — just describe the function you need in Lovable chat."*
>
> **Workflow correto pra entregar edge function nova ou edit**:
> 1. Eu monto um **prompt para o chat do Lovable** com:
>    - Instrução clara (ex: "Create a new Supabase edge function named `fin-period-override`" OU "Edit the existing edge function `omie-financeiro` and replace its code with the following:")
>    - Código completo do `index.ts` em fenced block, self-contained (com helpers inlineados se necessário — UI do Lovable parece não suportar convention `_shared/`)
>    - Lembrete pra NÃO modificar/reinterpretar o código (Lovable AI tende a "melhorar" — não queremos)
> 2. Founder cola o prompt no chat do Lovable
> 3. Lovable AI cria/edita a function
> 4. Verificar no Cloud → Edge functions se aparece "Active"
>
> **Não sugerir mais**: `supabase functions deploy` via CLI (mesmo com CLI já instalada — o founder ainda perderia tempo confirmando project ref e o standalone supabase ainda paira como armadilha). Mesmo que CLI funcione tecnicamente, o caminho oficial Lovable é via chat — e o founder prefere "colar e clicar".

**Auditoria de quais custom migrations estão aplicadas no banco**:

- Inventário completo em [`docs/migrations-audit.md`](docs/migrations-audit.md) (38 custom migrations, 262 objetos esperados — tables, indexes, functions, triggers, cron jobs, enum values, RLS policies)
- Script SQL pronto pra colar no Supabase SQL Editor em [`scripts/audit-custom-migrations.sql`](scripts/audit-custom-migrations.sql) — read-only, retorna duas tabelas: (a) `supabase_migrations.schema_migrations` cross-reference, (b) existência objeto-a-objeto via `pg_catalog`/`information_schema`. Linha com `❌` = precisa apply manual
- Regenerar quando adicionar migration nova: `bun run audit:migrations` (parser regex em `scripts/audit-custom-migrations.ts`, idempotente)
- **Audit de 2026-05-19**: 262 objetos checados, 2 gaps (`standard_processes` nunca aplicada + `idx_customer_contacts_birthday` partial). Fechados via `scripts/apply-missing-migrations-2026-05-19.sql` (verificado ok=true).
- **Audit de 2026-05-20**: 40 custom migrations / 274 objetos (re-gerado após PRs paralelos). Nova migration `20260520010000_scoring_visit_p1_fixes.sql` — **rodar `scripts/audit-custom-migrations.sql` no Studio pra confirmar apply**.
- ⚠️ O histórico de auditorias vive aqui (não no `docs/migrations-audit.md`, que é auto-gerado e sobrescrito a cada `bun run audit:migrations`).

### Snapshot de schema (`supabase/schema-snapshot.sql`) — ⚠️ o repo NÃO é rebuildável via migrations

> **As migrations em `supabase/migrations/` NÃO são uma cadeia restaurável.** Diagnóstico de 2026-05-24 (set-difference catálogo de produção × `CREATE`s das migrations): **~210 objetos existem em produção sem `CREATE` commitado** — 60 tabelas, 41 funções, 25 views, ~22 triggers, 5 enums, ~56 policies. Módulos inteiros (Reposição/DES/Picking) foram criados direto em produção pelo Lovable e nunca ganharam migration de criação; as migrations seguintes só `ALTER`am tabelas-fantasma (ex: `sku_parametros` é ALTERada em 20 migrations mas nunca criada). Um `supabase db reset` a partir do repo **quebra**.

Solução adotada (híbrido em fases, PR #244):
- **`supabase/schema-snapshot.sql`** — `pg_dump --schema-only --schema=public --no-owner --no-privileges` de produção (gerado pelo chat do Lovable; 23.745 linhas; contagens batem 1:1 com prod). É a **fonte de DR/auditoria/base de staging do schema**, NÃO um sistema de migrations.
- **`supabase/schema-extensions-prelude.sql`** + **`README-schema.md`** + **`schema-snapshot.manifest.md`** — pré-requisitos de extensions, runbook de restore e contagens pra revisar drift por diff. **Ler o README antes de restaurar** (só em projeto Supabase, por causa de `auth.*`; armadilhas `CREATE SCHEMA public`/`\restrict`/`pg_cron`).
- **Re-gerar:** prompt pro chat do Lovable (ver `README-schema.md`) → commitar o `.sql` → atualizar o manifest. Cadência: após módulo/tabela nova do Lovable, antes de staging, ou mensal.
- ✅ **Restore validado por replay local (2026-05-24)** via `db/verify-snapshot-replay.sh` (Postgres 17 + `db/stubs-supabase.sql`): prelude + stubs + snapshot em transação única (`ON_ERROR_STOP`) rodaram limpo e as contagens batem 1:1 com prod (212/37/4/86/76/14/474). Prova ordem/dependência/sintaxe do `public` + criação das 474 policies. **Silver+ (2026-05-30): enforcement RLS AMOSTRADO** (own-scope / staff-gate / anon-deny, via override de `auth.uid()` por GUC de sessão + seed em `recurring_schedules`) — prova que as policies FILTRAM em runtime, não só criam. O **Gold completo** (TODAS as 474 + auth/storage runtime) ainda pede Supabase cloud vazio/docker (`supabase start` exige docker, ausente nesta máquina).
- **Complemento funcional ENTREGUE** (PR #246): `supabase/schema-infra-outside-public.sql` (buckets + realtime publication, idempotente), `schema-rebuild-runbook.md` (ordem de rebuild + recriação dos 33 crons + verificação) e `schema-security-report.md`. ⚠️ **NUNCA mexer em `supabase/migrations/`** (não mover snapshot pra lá, não arquivar migrations de lá) enquanto o Lovable é dono operacional do backend — decisão pós-codex: a pasta é reconhecida pelo ecossistema Lovable/Supabase → mexer arrisca confundir builder + tracking `schema_migrations` (por isso o archive das 222 foi DESCARTADO). **Pendente real = só o "Gold" completo** (runtime Supabase real, TODAS as policies + auth/storage); o replay sintático/ordem/dependência **e o Silver+ de enforcement RLS amostrado** já passam (ver bullet acima + `db/verify-snapshot-replay.sh`).

