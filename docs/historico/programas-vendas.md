# Programas de Vendas/Comercial (WhatsApp+Rota, Buddy) — registro

> Movido do CLAUDE.md (§5) em 2026-06-14. Resumo dos programas; o detalhe (specs/planos) está em `docs/superpowers/`, e o histórico de cada PR em [bugs-resolvidos.md](bugs-resolvidos.md).

### WhatsApp + Motor de Rota (programa, 2026-05-28→31)

Objetivo do founder: a IA lê o WhatsApp da empresa e, na **véspera de cada rota de entrega**, contata proativamente os clientes das cidades de amanhã (WhatsApp accept-a-proposal + lista de ligação) pra puxar recompra/lucro. Arquitetura: **Cloud API via BSP 360dialog** → webhook → edge function → LLM (single-provider Anthropic na v1). A IA **conversa e monta a cesta, mas o preço firme passa por gate humano**; **nunca inventa SKU/preço** (pricing determinístico do Omie). Specs/planos em `docs/superpowers/{specs,plans}/2026-05-28-whatsapp-*`.

- ✅ **PR1 — Fundação inbox (#479, em prod):** inbox do número central no OS. Edge `whatsapp-inbound` (webhook: verifica segredo via header `x-whatsapp-secret` OU `?token=`, dedup por `wa_message_id`, find-or-create conversa SEM resetar estado em webhook dup, match cliente por telefone, roteia por `carteira_assignments`) + `whatsapp-send` (staff reply, gate `authorizeCronOrStaff`, janela 24h, 360dialog `${D360_BASE}/messages`). Tabelas `whatsapp_webhook_events/conversations/messages` (+RLS staff employee/master, +Realtime). Helpers puros TDD em `src/lib/whatsapp/` (`waPhoneCandidates`/`parseInboundWebhook`/`is24hWindowOpen`, espelhados inline no edge — Deno não importa do `src/`). Front: `useWhatsappInbox` + tela `/whatsapp` (sidebar→Vendas, managerOnly). Migration `20260528140000_whatsapp_fundacao.sql`. ⚠️ **e2e ao vivo pendente** (precisa conta 360dialog + número).
- ✅ **PR2a — Motor de rota + lista de ligação (#505, em prod, phone-free):** substitui o "pra quem ligar" route-blind (só churn) por fila priorizada pelas **cidades da rota de amanhã (D-1)** × **valor econômico**. Helpers TDD: `normalizeCityKey`/`cityKeyEquals` (normaliza city+UF; **exclui DIVINÓPOLIS/TO** por construção — validado 24/24 cidades em prod, formato `'CIDADE (MG)'`), `resolvePrepForWorkday` (rota de amanhã + diárias Div/Carmo do Cajuru, sexta-só-diárias, feriado via `route_calendar_override`), `valorDaLigacao`+`buildContactList` (gates "nem se paga"/cadência/jit + reservas win-back/cold-start). Hook `useRouteContactList` (cidades D-1 × `customer_visit_scores` [city-aware, RLS por carteira] × `customer_metrics_mv` × `profiles`) + tela `/rota/ligacoes`. Migration `20260528160000_route_fundacao.sql` (`route_schedule`/`route_calendar_override`/`route_disparo_config`/`route_contact_log` +RLS +seed 24 cidades).
- ✅ **PR2b-foundation — opt-in + pacing (#513, em prod):** `nextOptInStatus` (PARAR/SAIR/CANCELAR→`opt_out` sticky/LGPD; 1ª resposta→`opt_in`) fiado no `whatsapp-inbound` (seta `opt_in_status` no inbound novo, sem resetar em dup). `selectDisparoBatch` (pacing: janela **07:30–15:30** + teto do tier Meta descontando o já-enviado + exclui opt_out + ordem por valor) — pronto pro PR2b-send. ⚠️ **REQUER redeploy do `whatsapp-inbound` via chat do Lovable** (verbatim da main) p/ o opt-in valer em prod.
- ✅ **Hardening codex §6.5 (#518, em prod):** passe adversário do codex nos critérios (sem P1; P2 operacionais). Corrigidos: win-back ordenado por VALOR+piso (era bug — ordenava por churn-depth), dedup de canal (`whatsappQueue` exclui `callQueue`), guardrail cold-start `min(piso, ceil(cap×10%))`, nomenclatura "valor esperado" (margem é constante). Adiados pro piloto (precisam de dado): dupla-contagem prontidão×pConverte, capacidade por TEMPO, cadência por janela-de-rota.
- ⏸️ **BLOQUEADO no 360dialog (founder pega o celular depois):** **PR2b-send** (disparo real accept-a-proposal: templates Meta aprovados, escrita em `route_contact_log`, invoca `whatsapp-send`) + **dashboard do piloto** 2-4sem (motor diário Div/Cajuru já no seed → métrica de decisão **automatizar mais vs contratar**). O **montador da cesta accept-a-proposal** é engine novo (agrega `order_items`) — decidido NÃO construir às cegas (sem o celular pra validar + template Meta-gated).
- ✅ **PR2c — Closed-loop da lista de ligação (cadência ao vivo + registro de outcome) (PR #550, em prod):** fecha o ciclo *liguei → resultado → não repetir → fechou?* da `/rota/ligacoes` — a parte de "cadência ao vivo via route_contact_log" que o `useRouteContactList:171` apontava como "PR2c". **Phone-free** (reaproveita a ligação manual/WebRTC; NÃO depende do 360dialog). O motor `buildContactList`/`gate()` **fica INALTERADO** — só passei a alimentar os 3 campos antes hardcoded (`optOut`/`contatadoHaDias`/`fechouHoje`) com sinais reais. **Helper puro TDD** `src/lib/route/route-outcome.ts` (`derivarSinaisContato`, 15 testes): opt_out **sticky sem janela**; `jaConvertidoNaRota` por **`data_rota`** gravada (não `created_at` — fila é D-1); cadência **separa tentativa de bloqueio** — `respondido`/`convertido` bloqueiam normal, `sem_resposta` só bloqueia em **≥3 DIAS distintos** na janela 7d (conta dias, não linhas → não martela; abaixo do limiar é só badge = cadência curta); ignora datas futuras. **Escrita por RPC** `registrar_contato_rota` (SECURITY DEFINER): gate staff **+ visibilidade de carteira** (`pode_ver_carteira_completa` OR `carteira_visivel_para` — staff-only deixaria registrar opt_out/convertido de cliente alheio), `farmer_id` server-side, canal fixo, **advisory lock + dedupe 2min** (duplo-toque do celular); `desfazer_contato_rota` (own + <5min, retorna `{deleted}`) → **undo** curto no toast. Vocabulário canônico (Fechou→`convertido`/Falou→`respondido`/Não atendeu→`sem_resposta`/Não quer→`opt_out`, confirmação no opt_out). Hook lê o log em **2 queries PAGINADAS** (opt_out full-history + cadência 90d, **canal='ligacao'**), deriva por cliente (agrega farmers — sinal é por CLIENTE), **fail-open** (erro de leitura → defaults, NÃO esconde cliente, + aviso). UI: badges "contatado há Xd"/"sem resposta N×", seção "Resolvidos hoje", métrica do dia (ligados/atenderam/fecharam por `created_at` SP). **Fix junto:** `RotaListaLigacao` usava `new Date().toISOString().slice(0,10)` (UTC) → às 22-23h SP virava o dia seguinte → rota errada; trocado por `spBusinessDate` (novo util em `sp-day.ts`). **Codex em todas as etapas** (metodologia + spec [3 furos: sem_resposta×cadência, jaConvertidoNaRota×D-1, opt_out×janela] + plano [P0 carteira na RPC + advisory lock + undo {deleted} + 5 testes anti-impl-preguiçosa] + adversarial no código [2 P1: dataRotaFila fallback, paginação do log; 2 P2: canal, resolvidos por sinal]). Migration `20260531170000_route_contact_log_escrita.sql`. ⚠️ **Migration manual (RPCs) — colar no SQL Editor.** Specs/planos em `docs/superpowers/{specs,plans}/2026-05-31-rota-closed-loop*`. **Não-objetivos v1 (cortados, Codex/YAGNI → v2):** vincular o PV exato (mede conversão pelo status), auto-captura no Dialer desktop (vendedor externo = `tel:` nativo), follow-up agendado, obs livre, reversão de opt-out por UI (é SQL manual do master na v1), opt-out cross-canal com WhatsApp.
- ✅ **SLA de resposta do WhatsApp — "cliente sem resposta" (2026-06-04, [PR #587](https://github.com/LucasSardenbergL/afiacao/pull/587), em prod):** indicador + alerta de quanto tempo um cliente está sem resposta HUMANA no WhatsApp, escopado pela vendedora dona, + digest diário por e-mail pro founder. **SQL é a fonte única:** função `whatsapp_minutos_uteis` (minutos de expediente seg-sex 07:30-17:30 SP, config em `company_config`) + view **`v_whatsapp_sla`** (`security_invoker`): "esperando desde" = **primeira `in` não respondida** (tuple `(anchor,created_at,id)` > última `out` HUMANA), "respondido" = `direction='out' AND sender_user_id IS NOT NULL` (exclui blast de template/IA de graça → **encaixe da IA sem coluna nova**), âncora = `coalesce(wa_timestamp,created_at)` **com guarda de futuro** (clock-skew → cai pro created_at), stop-keyword (`wa_is_stop_keyword`, translate p/ acento = paridade c/ TS) e `status='fechada'` fora, limiares 15/30min. **Dono derivado AO VIVO** (⚠️ o `assigned_operator_id` é **congelado** na criação da conversa, nunca atualiza — codex pegou) via **`wa_owner_efetivo(customer)` SECURITY DEFINER** (carteira + cobertura/férias; definer pq o `security_invoker` resolveria só a carteira visível ao leitor → falso "sem dono" pro gestor não-master; REVOKE de anon/public). Front (Realtime + refetch 30s): selo no inbox + card na Meu Dia da vendedora (toggle Minhas/Todas, saliente FORA do "modo antigo" recolhido do #584) + badge na sidebar + painel `/whatsapp/sla` (gestor/master, por vendedora + balde "sem dono"; **display-only** — RLS do inbox inalterada, é a mesma de hoje que mostra tudo a todo staff). **Digest (F2):** cron SQL local 18h BRT (seg-sex) → `whatsapp_sla_digest_tick()` **idempotente** (`whatsapp_sla_digest_log(data_local PK)` + `on conflict do nothing` + `if not found return`) → `fornecedor_alerta` (tipo novo `whatsapp_sla`, CHECK estendido preservando os 8 de prod) → `dispatch-notifications` **SEM edição** (manda `titulo`+`mensagem` genérico, sem filtro de tipo → o digest pré-renderiza o corpo). **REVOKE no `whatsapp_sla_digest_tick`** (anon/authenticated) — senão qualquer logado marcava o dia no log e SUPRIMIA o e-mail das 18h. **Codex no design (6 P1 folded; correção do `assigned_operator_id` congelado) + adversarial no código (2 P1 + 4 P2 folded).** **SQL validado em PostgreSQL 17 local** — 10 asserts da função (atravessa noite/fim de semana/clamps/mesmo-instante) + cenários da view (esperando/bola/fechada/stop/sem-dono/template-sem-sender + dono ao vivo) + idempotência do digest + guarda-futuro (`db/test-whatsapp-sla.sql`, base `verify-snapshot-replay.sh` + foundation migration p/ as tabelas WhatsApp que o snapshot stale não tem). Migrations `20260604130000_whatsapp_sla.sql` + `20260604140000_whatsapp_sla_digest.sql` (**manuais, aplicadas em prod**). ⚠️ **Publish do frontend pendente.** **Lição (treadmill multi-sessão):** a `main` moveu **3×** durante o merge; o `roadmap-sessao.md` é ímã de conflito recorrente → resolver com `git checkout --theirs` zera o treadmill (meu branch == main → o arquivo deixa de conflitar nos merges seguintes). **4 P2 disclosed (follow-up baixo):** cast de config frágil (input só-master), badge degrada silencioso (graceful), view scan-heavy (ok no volume v1), nome do CHECK (confirmado idêntico em prod). Specs/planos em `docs/superpowers/{specs,plans}/2026-06-04-whatsapp-sla-resposta*`.
- ⚠️ **Pendências do founder no banco/Lovable:** redeploy `whatsapp-inbound` (opt-in); `DELETE FROM whatsapp_conversations WHERE phone_e164='5599999999999';` (dado de teste do smoke do PR1). Secrets a setar no PR2b-send: `D360_API_KEY`/`D360_BASE_URL`.
- **Lições:** (a) lint do CI cobre `supabase/functions/` → lintar edge também (pegou um `@ts-ignore` banido); (b) `deno check` do `whatsapp-inbound` tem 4 erros de typing do `supabase-js` sem generic `Database` — **net-zero vs main**, pré-existente, runtime do Supabase compila fine, CI não roda deno check (não "consertar"); (c) `bun build` (bundler nativo) ≠ `bun run build` (vite) — usar `bun run build`; (d) `customer_metrics_mv` casa por `customer_user_id` (não `user_id`).

### Programa "Buddy" (copiloto interno, inspirado na UPOPS/PMBuddy do Itaú) — 2026-06-04/05

Princípio: **"contradição com evidência"** (a fila/dado diz X, os sinais do cliente dizem Y → aja), **determinístico-PRIMEIRO**; LLM só onde agrega e **nunca** fabrica número no money-path. Reframe da UPOPS: já somos o OS consolidado → o transferível NÃO é chat-sobre-tudo (teatro p/ <20 users), é o **motor de contradição**.

- ✅ **Crítica da Fila v1** (rep-facing, [PR #585](https://github.com/LucasSardenbergL/afiacao/pull/585), em prod) — bloco **"Por que agora"** nos top-5 cards do Meu Dia (`FilaDoDia`): timeline de fricção + **4 badges de contradição determinísticos** (`recorrente_sumiu`/`sem_resposta_repetido`/`tarefa_feita_sem_prova`/`alto_valor_fora_rota`) + feedback PostHog (`fila.critica_*`). Motor puro TDD em **`src/lib/fila/critica/`** (`montarEvidencePack` + `buildCriticaInputs` + hook `useCriticaFila`). **100% frontend** (sem migration/edge/cron); reusa `customer_metrics_mv`/`route_contact_log`/`v_tarefas_estado`; thresholds reusam o `useAiOps`. Piloto de 2 semanas com **critério de morte** embutido. Claim sem evidência é descartado; degradação honesta (`faltaDado`).
- ✅ **GestorBuddy** (founder-facing, [PR #612](https://github.com/LucasSardenbergL/afiacao/pull/612), em prod) — card **"Exceções"** no `MasterDashboard` (**`src/lib/gestor/excecoes/`**, hook `useExcecoesGestor`): 3 fontes agrupadas por **dependência** — Dados quebrados [Sentinela `useDataHealth`] → Clientes em risco [`ai_decisions` **freshness-first**: >48h vira meta-exceção "Análise desatualizada" + botão Atualizar] → Confirmações pendentes [`v_tarefas_estado` team-wide; **proof-gap, copy NÃO-acusatória**, ≥1 dia]. Teto 8-10 (críticos sempre entram), merge visual, **recibos (fonte+frescor por linha)**, telemetria `gestor.excecoes_*`. 100% frontend.
- 📐 **v1.5 — camada de LLM "linha de abordagem"** ([PR #617](https://github.com/LucasSardenbergL/afiacao/pull/617) = spec do shell) — **BUILD PILOT-GATED** (não construir LLM antes do piloto da v1 passar — decisão eu+codex; senão é teatro). Shell robusto: **Anthropic direto + forced-tool + anti-alucinação ENFORÇADA** (IDs estáveis no `SinalVoz` + pós-validação que rejeita número/`R$`/`%`/id inexistente) + **on-demand-on-open** + degradação silenciosa. Regra-mestra: **prosa do LLM é descartável, a evidência é o produto**. Transcrição/voz = v1.6. Specs/planos em `docs/superpowers/{specs,plans}/2026-06-0{4,5}-{critica-da-fila,gestorbuddy-excecoes,critica-narrar-v15}*`.

### Backfill histórico de pedidos da Oben (dependência DURA do OTE) — 2026-06-14

`sales_orders`/`order_items` cobria só **~19%** das vendas reais da Oben (R$404k/6m no banco vs ~R$2,1M reais; 1.937 pedidos lifetime; 4 meses zerados) porque o `sync_pedidos` roda janela rolante de 5d e **nunca houve backfill histórico**. Pré-requisito do OTE (remuneração variável das farmers — spec `2026-06-13-ote-remuneracao-variavel-farmer-design.md`, §2/§13). Sessão só-SQL (via SQL Editor; sem código novo).

- ✅ **Backfill 12 meses CONCLUÍDO (em prod):** **19% → 107% do esperado** — receita 6m **R$404k → R$2,24M** (5,5×), 12m **R$4,69M**, lifetime **1.937 → 5.533**, 12 meses uniformes (~300-485/mês). Método: **motor cron temporário serializado** invocando `sync_pedidos` mês-a-mês (1 a cada 6 min, zero concorrência) + 2ª passada com `start_page` escalonado nos meses grandes. Lições técnicas (concorrência Omie = rate-limit silencioso, edge roda em background, `pg_sleep` causa rollback, filtro por previsão) em `docs/agent/sync.md` → "Backfill via invocação SERIALIZADA".
- ✅ **Recência corrigida:** `order_items.created_at` (DEFAULT now(); o sync não seta) dos 4.408 itens backfillados → `UPDATE = order_date_kpi` (senão o `calculate-scores` veria todo cliente "comprou hoje"). `customer_metrics_mv` refrescada; scores de carteira recomputam pelos crons noturnos (já corrigidos).
- ✅ **Outlier resolvido:** pedido `omie nº 9647` (`omie_pedido_id 12040804760`, fev/2026) = **R$615M** era digitado-errado + cancelado no Omie, veio como `importado` no sync (cancelamento atípico, **NÃO sistêmico** — 274 cancelados capturados normalmente) → corrigido p/ `status='cancelado'`. **Guard de plausibilidade** adicionado ao spec do OTE (§5) p/ erros futuros de digitação.
- ⏳ **Pendência:** conferir a fila/scores das vendedoras após o recompute noturno (crons `0 6`/`0 7`/`30 7`).

### Sub-projeto 1: cursor+lease pro backfill CONFIÁVEL de pedidos Omie + piloto colacor — 2026-06-17

O backfill da Oben (acima) foi via **ritual manual** (cron temp + validação por contagem). O Codex matou esse padrão pro money-path ("`*/6` + contagem é heurística, não serialização — o risco é produzir um backfill que parece completo mas perdeu páginas"). Este sub-projeto 1 constrói a **infraestrutura confiável** que substitui o ritual. Spec `docs/superpowers/specs/2026-06-17-vendas-omie-cursor-lease-design.md`. **Sessão fresca, money-path** (`sales_orders`/`order_items` → positivação/OTE/comissão).

- ✅ **Fundação ([PR #929](https://github.com/LucasSardenbergL/afiacao/pull/929), em prod):** migration `vendas_sync_cursor` (tabela+RLS+**4 RPCs** `lease_acquire`/`heartbeat`/`finish`/`release`, SECURITY DEFINER gated `service_role` + cron `*/6` `DISTINCT ON (account)` = mesma-conta serializada, contas paralelas) + índice **UNIQUE parcial** `sales_orders(account,hash_payload) WHERE hash_payload LIKE 'omie\_%'` (idempotência insert-only NO BANCO, não na memória do edge). Edge `omie-vendas-sync`: `throwOnTransient` (null=fim real, throw=pausa — conserta o #1 null-ambíguo), lease + **heartbeat persiste progresso por página** (crash retoma da página em curso, ≤1 página re-feita idempotente, não o run — fix Codex `cursor-finish-rewind`), `classifyPedidosPage` 3-vias (vazio que contradiz `total_de_paginas` = pausa, NUNCA falsa-completude), cliente-transitório (`ConsultarCliente`) pausa a janela em vez de cachear ausência falsa. Helpers puros `omie-vendas-sync/pagination.ts` + teste Deno. **Provado:** PG17 cursor 32/0 + índice 5/0 (**6 falsificações**) · Deno 10/10 · **3 Codex challenges xhigh + painel tri-modelo** (8 P1 triados; 5 fixados+provados, 3 documentados). Edge deploy **provado por COMPORTAMENTO** (smoke test de janela vazia: HTTP 200 + `use_cursor`/`complete` no `fin_sync_log` — distingue edge novo de velho/503).
- 🔍 **Piloto colacor/março 2026 (em prod):** revelou colacor **~96% sub-sincronizado** (5 de ~132 pedidos no banco) — **mesma situação da Oben**. O backfill encheu **127 pedidos** (R$62k, soma), **0 duplicatas** (índice UNIQUE segurou — 125 hashes distintos), **0 outliers** (>5×p99 = 0; maior R$7,5k). Recência corrigida cirurgicamente (262 itens dos backfillados → `created_at = order_date_kpi`) ANTES do `daily-calculate-scores` 06:00, guard verificado (não tocou os 15 itens reais de hoje). Validou a premissa do projeto AO VIVO (o sync rolante de 5d perdia o histórico).
- 🚧 **Gates p/ sub-projeto 2:** **OBEN backfill BLOQUEADO** — o cron `sync-reprocess` reescreve `hash_payload` `omie_`→estrutural → re-sync duplicaria (o índice parcial não pega hash de-namespaced); oben já tem **~511 dups** por `omie_numero_pedido` HOJE (pré-existente; checar como o read-path de comissão conta dup ANTES de limpar). **Recência mais ampla:** até pedidos do incremental atrasado têm `created_at`≠data do pedido → cobrir TODOS os `omie order_items` é sub-proj 2. **Insert pedido+itens não-atômico** (itens podem faltar sem reparo — skip por hash na retomada). O cursor de março fica como **registro de auditoria** (cron não re-dispara janela completa).
- **Lições:** (a) `.or()` em UPDATE do PostgREST quebra (42703) → lease atômico vira **RPC SQL-pura** (e fica provável no PG17); (b) supabase-js `.rpc()` **não lança** em erro de RPC → checar `{error}`, não `try/catch` (engolia heartbeat negado); (c) o índice UNIQUE parcial é o guard de idempotência **REAL** — a memória do edge não segura concorrência; (d) `total_de_paginas` mente → `null` é o único sinal forte de fim, vazio-contradiz-total = pausa (precisão>recall: janela presa visível > falsa-completude silenciosa).

### Sub-projeto 2 · Fase 1a — recência ampla colacor (`created_at` canônico) — 2026-06-18

A recência de colacor (`order_items.created_at`) estava colada na data de **CARGA**, não do pedido: o sync antigo (insert-only, pré-#930) gravava os FILHOS com `now()` enquanto o PAI (`sales_orders`) tinha a data certa (1177/1248 ok). 2345/2736 itens colacor errados. A RPC `criar_pedidos_com_itens` (#930, filho herda `created_at` do pai) já blinda o FUTURO; esta fase conserta o HISTÓRICO. Decisão de sequência: `/codex` consult (sessão `019ed883`) validou colacor-primeiro/oben-em-paralelo e mostrou que o #B é "parar de reescrever `hash_payload`", não "dedup melhor".

- ✅ **Migration `20260618130000_recencia_colacor_created_at` (aplicada+validada em prod):** 2 UPDATEs idempotentes → `created_at = meio-dia-UTC(order_date_kpi)` p/ omie_ colacor divergente. Escopo **blindado** (oben fora = bloqueado no #B; não-omie e `order_date_kpi` nulo intactos). Provado PG17 `db/test-recencia-colacor-created-at.sh` **13/0** + falsificação **F1** (filtro de conta) + **F3** (timezone meio-dia). Pós-apply: 0 divergente colacor, oben intacto (5448).
- **Achados (`/codex` challenge):** o engine de recência do farmer é o **hook UI `useFarmerScoring`** (lê `sales_orders.created_at`, 94% certo) → recompute **orgânico** quando a UI roda, não via cron. `calculate-scores` nightly só **re-normaliza** scores existentes (o `order_items.limit(10000)` dele só roda no auto-seed). Logo o dano money-path na priorização do farmer é **menor** que o temido (71 SOs), e maior em `fin-valor-cockpit` (TTM, lê `order_items.created_at` direto).
- ⏳ **Aberto (sub-proj 2):** **B1** `calculate-scores` trunca `order_items` em 10k sem `.order()` (14135 linhas — dormente, acorda em reset — `task_0f7904e8`); **B2** 222 clientes cross-account colacor+oben (decisão de produto: carteira por empresa ou por cliente? — difere p/ #B); **Fase 2** backfill colacor via sonda de counts Omie (`registros_por_pagina:1` → semear só mismatches); **#B** `sync-reprocess` parar de reescrever `hash_payload` + canonizar as 513 de-namespaced antes do backfill oben.

### Sub-projeto 2 · Fase 2a — piloto-2 backfill colacor (jan+fev/2026) — 2026-06-18

Escalada do piloto de março via a infra cursor+lease (sub-proj 1). Semeadas 2 janelas mensais sub-sincronizadas no `vendas_sync_cursor` (gate humano §6: founder colou o INSERT); o cron `vendas-sync-continuacao-6min` + lease processou serializado (jan fechou 16:38, fev 16:44).

- ✅ **Backfill (em prod):** jan **2→119**, fev **8→121** = **+230 pedidos, R$131k**. **0 duplicatas** (índice UNIQUE parcial), **0 outlier** (maior R$6,2k vs p99 R$5,1k; regra >5×p99 = zero), **0 sem cliente/item** (atomicidade RPC #930). Cobertura/outlier/integridade via `psql-ro` (read-only, eu).
- ✅ **Fecho de recência:** o edge grava `created_at`=`data_previsao`/`now()` (NÃO `dInc`) → 115 itens + 62 pais nasceram sujos → re-apliquei os 2 UPDATEs da migration `20260618130000` (idempotente, escopo global colacor) + `REFRESH CONCURRENTLY customer_metrics_mv` → **0 divergente** (dupla-checado via `psql-ro`). Scores recomputam orgânico (`useFarmerScoring`).
- 🧭 **Achado p/ Fase 2b (conserto de fonte):** o `created_at` do `omie-vendas-sync` (~L1195) deveria sair de `dInc` (=`order_date_kpi`), não de `data_previsao`/`now()` — re-suja a recência a CADA sync, não só no backfill. Levar ao Codex junto do desenho da **sonda de counts + buraco 2020-2025** (semear ~60 janelas às cegas = vetado).

### Sub-projeto 2 · Cockpit Oben — Bug C (janela) + Bug D (FK ausente) consertados NO CÓDIGO — 2026-06-18

O `fin-valor-cockpit` (EVP da Oben) era o **maior vítima money-path** da recência poluída (lê `order_items.created_at` direto). Como o conserto de FONTE da Oben está **bloqueado no #B** (sync-reprocess re-poluiria — ver migration `20260618130000`), consertei no **código do cockpit** (desbloqueado, robusto mesmo pós-#B pois `order_date_kpi` é canônico). PR draft [#950](https://github.com/LucasSardenbergL/afiacao/pull/950). ⚠️ **Deploy MANUAL da edge** (chat Lovable) após merge. Contexto: A/B/F já no main (#946 + join `sales_orders` — esse último apagou os R$615M de cancelado).

- ✅ **Bug C (janela por `order_date_kpi`):** TTM passou de `order_items.created_at` (data de CARGA) p/ `sales_orders.order_date_kpi` (DATE); `created_at` vira só prefiltro de busca (folga 90d). Tirava **823 linhas / R$357.495,19** de pedidos >1 ano. Provado `psql-ro`: **0 falso-negativo** (`created_at ≥ order_date_kpi` sempre).
- ✅ **Bug D (recupera FK ausente):** 178 linhas app-orders de 2026-03 com `product_id` NULL dropadas; resolvo `SKU→product_id` Oben (único 3660/3660) **com guard de conta** (pedido pai `account='oben'`) → recupera linha + custo. Com C junto: **+120 linhas / R$76.413,15** (as outras 58 eram pedido velho = falso-positivo do próprio C).
- ✅ **Efeito líquido (reconciliado psql-ro):** receita Oben **R$5.059.623,41 → R$4.778.541,37** (−R$281.082,04). ⚠️ **Degrau visível** pro gestor — é correção (janela pela data do pedido), não sumiço.
- ✅ **Painel Codex** (gemini indisponível=cota → degradado): **0 P1**; P2a guard-de-conta **folded**; P2b fronteira UTC/string **refutado** (`order_date_kpi` é DATE, mesmo padrão do `carteira-positivacao-snapshot`); P3 (prefiltro sem sentinela, mapa overwrite) documentados. `deno check`+`typecheck` verdes.
- ✅ **`cost_final` no EVP** (follow-up, PR draft [#953](https://github.com/LucasSardenbergL/afiacao/pull/953)): o cockpit usava `cost_price` (legado) enquanto o motor grava `cost_final` (canônico; o `recommend` já usa). Trocado p/ `cost_final` com fallback `cost_price` (guard `>0`, ausente≠zero). **NÃO era minor** — ponderado por volume, **+R$72.593,39** de cm/EVP (base R$1.637.922,60→R$1.710.515,99): 5 SKUs CMC de alto giro tinham `cost_price` ≈2× o `cost_final` real → o cockpit subestimava a margem Oben. Cobertura preservada (0 perde custo). Codex 0 P1 + 1 P2 (`fallback-alem-de-ausente`: os 14 fallbacks são `cost_final<=0` EXPLÍCITO, não null) → fallback mantido (cost_price real; stakes cm R$5.084,92) e tornado **observável** (`console.warn`).
- ✅ **v2 — confiança por custo de baixa qualidade** (PR draft [#1003](https://github.com/LucasSardenbergL/afiacao/pull/1003)): `scoreConfiancaCockpit` recebe `custo_baixa_confianca_pct` e **degrada a confiança** (T1 — NÃO nullifica o cm; `ausente≠zero`). Limiar `cost_confidence<0,7`=baixa, classificado APÓS resolver o custo usado → **exclusivo** de `custo_ausente`. Severidade (Codex): ≥0,2 rebaixa p/ média, 0,05–0,2 informa, nunca <média sozinho. Prova psql-ro (célula=combo): **25,95%** (919/3541; ok 2313, ausente 309 — soma fecha) → rebaixa alta→média; **sem efeito no número de margem/EVP**. Decisão de design + code-review **conjuntos com Codex** (0 P1; 2 P2 dobrados: flag atômica com overwrite do Map; null-conf=baixa por política). Helper **espelhado** (frontend fonte+teste / edge); vitest 68/68; deno+typecheck verdes. **Deploy edge-only** (helper não roda no frontend → sem Publish). v3 (telemetria): split proxy-vs-legado nos motivos + métrica ponderada por valor.
- 🧭 **Aberto:** quando o **#B** canonizar `created_at`=`order_date_kpi` da Oben, o prefiltro do Bug C vira redundante-mas-correto (sem mexer no cockpit).

### Sub-projeto 2 · Fase 2b — recência da `customer_metrics_mv` por `order_date_kpi` (não `created_at`) — 2026-06-23

Fecha o 🧭 da Fase 2a. A `customer_metrics_mv` (recência/cadência/janelas) lia `sales_orders.created_at` (= previsão/now do edge), não `order_date_kpi` (= dInc). **Reorientação do diagnóstico:** o conserto certo NÃO é a fonte (edge) — a RPC autoritativa `get_customer_sales_summary` (→ `farmer_client_scores`) e o cockpit JÁ usam `COALESCE(order_date_kpi, created_at)`; só a MV e o hook display-only `useFarmerScoring` ficaram para trás. Mexer no edge dependeria do **#B** (sync-reprocess re-poluiria a Oben) e re-sujaria a cada sync. Evidência prod (`psql-ro`): Oben 1786/10169 (17,6%) com `created_at::date ≠ order_date_kpi`, cauda até 736d; colacor já 100% alinhado; `order_date_kpi` só 3 nulos Oben. Spec `docs/superpowers/specs/2026-06-23-recencia-mv-order-date-kpi-design.md`. **Decisão founder:** consumidores (MV+hook), janela 90d junto, escopo só-data.

- ✅ **Migration `20260623120000_recencia_mv_order_date_kpi` (PROVADA, aguarda apply):** DROP+CREATE transacional da MV; 4 CTEs sobre `d = COALESCE(order_date_kpi, (created_at AT TIME ZONE 'America/Sao_Paulo')::date)`; janelas com **teto** (`d <= today_sp`, futuro fora) e **sem-overlap** (`prev < today-90`); `ultima_compra_data = (max(d)::timestamp AT TIME ZONE SP)` (preserva timestamptz); cadência `((max(d)-min(d))::numeric / NULLIF(...))`. Preserva as 13 colunas (contrato de `get_customer_metrics` SELECT *). Edge **intocado** → aposenta o patch manual pós-backfill.
- ✅ **Hook `useFarmerScoring.ts`:** `orderTime = order_date_kpi ?? created_at` (display-only; frontend → **Publish**).
- ✅ **Codex challenge xhigh (7 P1, todos folded):** teto/sem-overlap, AT TIME ZONE explícito (ultima_compra_data + fallback), cast numeric, DROP+CREATE transacional. **Follow-ups separados:** alinhar status/`deleted_at` MV↔RPC; `faturamento` via `order_items` (paridade); camada DRY `valid_sales_orders_kpi`; `get_customer_sales_summary` usa `created_at::date` cru (mesmo bug latente).
- ✅ **Prova PG17 `db/test-recencia-mv-order-date-kpi.sh` (31/0):** 14 positivos + contrato late-bound + GRANT (42501) + anti-TZ (UTC×SP idênticos) + **7 falsificações com dente**. **Achado F7 (lição):** `date`→`timestamptz` **coage SILENCIOSAMENTE** (não quebra `get_customer_metrics`), mas a coerção usa a TZ da sessão → reintroduz não-determinismo silencioso; o `AT TIME ZONE` explícito é o que blinda, não só o tipo.
- ⚠️ **Handoff (apply manual):** (1) colar a TRANSAÇÃO inteira no SQL Editor → Run; (2) `REFRESH MATERIALIZED VIEW CONCURRENTLY public.customer_metrics_mv`; (3) `useFarmerScoring` → **Publish** do frontend. Validar via `psql-ro` (pg_get_viewdef + diff por cliente — `faturamento_90d`/recência da Oben vão **mudar** = correção, não sumiço; degrau visível ao gestor).
