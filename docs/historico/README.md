# docs/historico/ — diário de PR / entregas (movido do CLAUDE.md)

Registro consultável de **PRs/bugs/programas/auditorias já entregues** — a narrativa íntegra (problema → diagnóstico → fix → lição), movida das §5/§9/§10 do CLAUDE.md na frente 1 do refactor (2026-06-14/15; decisão do founder: **mover íntegro, zero descarte**). Consolidou a antiga `docs/registro/`.

**Não é lido a cada sessão** — o CLAUDE.md ficou só com regras vivas. Venha aqui quando precisar do **detalhe/contexto** de uma entrega. Ao concluir uma entrega nova, **registre aqui** (não no CLAUDE.md).

> 🚦 **A tabela abaixo é GATEADA** (`scripts/docs-indice-gate-check.ts`, dentro do `bun run test`): o que vale é a **1ª coluna** — ser citado dentro do resumo de outra entrada **não** conta como estar indexado. Deixam o CI **vermelho**: `.md` sem linha própria · linha apontando para arquivo que não existe · o mesmo doc em duas linhas · `[a.md](b.md)` (texto ≠ destino) · resumo com menos de 30 chars. Não é burocracia: a frase acima já existia e o índice apodreceu três vezes — o #1658 reconciliou **9 arquivos invisíveis**, horas depois o #1659 (uma PR de feature) reintroduziu um, e o #1674 destravou a main quando o #1212 entrou com um doc sem linha. Contramedida textual reincide; gate estrutural para. Vale igual para `docs/runbooks/`. Detalhe em [gate-indice-docs.md](gate-indice-docs.md).

| Arquivo | O que tem |
| --- | --- |
| [bugs-resolvidos.md](bugs-resolvidos.md) | (era §10) cada bug/contradição/débito resolvido, com PR + lição (multi-domínio: KB, Radar, reposição, sync…) |
| [programas-vendas.md](programas-vendas.md) | WhatsApp + Motor de Rota; copiloto "Buddy" |
| [fila-plano-tatico.md](fila-plano-tatico.md) | PTPL fase 1: 533 planos com zero desfecho e 72% inalcançáveis — a fila sem saída (expiração + recorte por risco). Fase 2: os 533 eram **80 clientes** — a trava de idempotência media o DIA, e a fila mede a JANELA |
| [auditoria-ux-redesign.md](auditoria-ux-redesign.md) | (era §9/§9b/§11) auditoria UX 2026-05-13 + redesign visual v3 + premissas/glossário |
| [estoque-picking-recebimento.md](estoque-picking-recebimento.md) | offline-first + closed-loop (Picking Bridge Oben, Recebimento honesto) |
| [tintometrico.md](tintometrico.md) | vínculo `tint_skus`↔Omie (resgate de 62 cores que somem do seletor) + lições de matching de catálogo |
| [geocoding-cep.md](geocoding-cep.md) | geocoding por CEP do Roteirizador-campo (`cep_geo` SoT, gate CEP Aberto 91,5%, edge-proxy `cep-geo-resolver`, import da carteira) |
| [reposicao-caixa-skill.md](reposicao-caixa-skill.md) | skill `reposicao-caixa` (decisão de compra ↔ caixa/NCG da Oben; custo marginal do caixa, gate de caixa como veto, antecipar aumento/promoção); lições do teste real: `custo_capital_efetivo_perc` é % a.a. (erro de 12×) + RPC de caixa gated a staff |
| [posthog-erro-email.md](posthog-erro-email.md) | ponte PostHog Error Tracking → e-mail (edge `posthog-error-webhook` + RPC `enfileirar_erro_app` dedupe/circuit-breaker + `dispatch-notifications`); fix do `listaUrl` do rollup; follow-ups do founder |
| [perf-ondas.md](perf-ondas.md) | auditoria de performance 2026-07-04 (5 PRs: telefonia fora do boot, contexts memoizados, virtualização, projeções/waterfall, optimistic recebimento, PWA prompt); bugs que o Codex pegou (efetivar NF-e sobre unidade não persistida; quebrar offline-first no 1º acesso); lições de método |
| [prime-colacor.md](prime-colacor.md) | programa de assinatura B2B paga (franquia de afiação + extrato honesto): PR-1 fundação de dados (append-only, grandfathering, contrafactual no banco) + PR-2 admin `/admin/prime`; decisões de reativação e catálogo-imutável; pendências do piloto |
| [programa-canal-whatsapp.md](programa-canal-whatsapp.md) | benchmark "WhatsApp da Lu" (Magalu) → programa do canal WhatsApp (núcleo HSM → fila → funil → proposta 1-toque → status transacional → disparo por rota); parecer Codex; ações externas (aprovar templates na Meta) |
| [epico-drop-espelho-omie.md](epico-drop-espelho-omie.md) | aposentadoria da tabela-espelho `omie_clientes` (identidade Omie↔user pela proof): fatias 3-edges (#1364/#1367) + hardening P1 do Codex (#1425, fecha a fábrica de clones — dedup capado em 1.000 de 5.276); **validação em produção** (clones imóveis em 1.633, espelho em 6.909, 392 clientes novos sem duplicata); o que trava o DROP (Fatia 5) e os writers da Fatia 4 |
| [authz-custo-fu4f.md](authz-custo-fu4f.md) | **FU4-F "vendedor não vê custo"** (#1434/#1465/#1473/#1485) — o que fechou (`inventory_position`+view operacional, `cmc_snapshot`, `get_tint_price(s)`, `pedido_compra_item`) + régua decidindo no servidor) e o que **NÃO** fechou (espelho `product_costs`; oráculo do cockpit, aceito). Lições: fechar a fonte não fecha o dado se há CÓPIA; permissivas combinam com OR (fechamento fantasma); assert com `ok` nos dois ramos; `CREATE OR REPLACE VIEW` não remove coluna; validar prod contra um PR **draft** — ou contra a medição do início da própria sessão — produz diagnóstico falso |
| [farmer-aprendizado-conversao.md](farmer-aprendizado-conversao.md) | post-mortem da `farmer_category_conversion` (2026-07-21): o "auto-aprendizado" dos engines de cross-sell/bundle **nunca escreveu uma linha** — writer atrás de `markAsAccepted` que nenhuma UI chamava, 3.659 recomendações 100% `pendente`, `n_tup_ins = 0`, mais um `upsert` sem `onConflict` que falharia em silêncio; por que o RANKING estava correto mesmo assim (constante = fator de escala) e o que exigiria construir o loop de feedback de verdade |
| [farmer-sensor-desfecho.md](farmer-sensor-desfecho.md) | 2026-08-21: instala o **sensor de desfecho** que faltava ao motor Farmer (17.316 recomendações, ZERO desfecho — o passo (1) do post-mortem acima). A UI **não tem o `id`** da linha, então o writer localiza por chave de negócio; o `/codex` derrubou o estado intermediário `'ofertado'` porque ele atribuiria o aceite a um cálculo que a vendedora nunca viu, e chave ambígua passou a **recusar** em vez de escolher. Traz também o `NULL IN (...)` que torna um CHECK **satisfeito** por ser NULL, e a classe **"falsificar pela UI só prova a camada de cima"** — clique em botão `disabled` não chama o handler, então o assert da lente passava com o guard do hook REMOVIDO. |
| [benchmark-concorrentes-marcenaria-2026-07.md](benchmark-concorrentes-marcenaria-2026-07.md) | **Onda 1 em produção (4 PRs: #1323/#1327/#1372/#1389)** — benchmark de 7 distribuidores de marcenaria (Leo Madeiras/GMAD/Rede PRÓ/Sim/W/Madeiranit/Gasômetro); tabela de 15 gaps com evidência `arquivo:linha` + parecer Codex; tese "Central da Ferramenta e Serviços" (reposicionar afiação/ROI, não copiar nesting/3D/BNPL); programa em 4 ondas — quick-wins #8/#13 entregues, Ondas 2-4 pendentes |
| [farmer-apriori-denominador.md](farmer-apriori-denominador.md) | as duas fatias do Apriori do farmer (2026-08-20/21): corrigir a LEITURA (cap de 1.000 → 479 de 30.259 pedidos) multiplicou o denominador por 63 e, como `support` é RAZÃO, o mesmo piso derrubou 24 regras para **2** — a correção é **segmentar por `account`** (14 regras com o MESMO piso), não baixar o piso. Cobertura ANTES×DEPOIS **com denominador** nos 3 consumidores, a armadilha da perda parcial de segmento no lote único, e por que o isolamento entre contas é fato do DADO (0 códigos colidem hoje), não do desenho. |
| [farmer-bundle-linhas-produzidas.md](farmer-bundle-linhas-produzidas.md) | a lacuna de teste DECLARADA no #1800 (`linhasProduzidas` contava CLIENTES, não linhas persistíveis) e a auditoria de **alcançabilidade** que a fechou: cliente que entra em `allCustomerBundles` só com `bestIndividual` não vira linha no `p_linhas`, e contá-lo travava o `registrarVazio()` do `catch` — o head parava de se mover e "nenhum registro novo" voltava a significar duas coisas opostas (o defeito do #1765/#1791 por um terceiro ângulo). Traz a **enumeração trecho a trecho** do que pode lançar entre `linhasProduzidas` e o fim do `try` (veredito: nada, por blindagem deliberada — `avaliarCompletude` é pura, `registrarGeracaoFarmer` tem try/catch na RPC + `mensagemDeErro` que não lança + `captureException` embrulhado por `withPosthog`, e a RPC de substituição só roda com linhas > 0, onde os dois mundos concordam), o teste entregue mesmo assim com o **gatilho declarado sintético** (`bundle-head-registra-vazio-sem-linha`, âncora `FG-VAZIO-SEM-LINHA`, falsificado nos dois locales), as duas armadilhas de vacuidade do cenário (`regras` é insumo obrigatório; `falha_rpc` não trava o slot `jaRegistrou`) e a lição: "sem teste" e "sem caminho" são diagnósticos diferentes |
| [farmer-margem-cobertura-custo.md](farmer-margem-cobertura-custo.md) | análise 2026-07-22 (100% leitura): a margem NULL em 84% de `farmer_client_scores` **não é lacuna de custo** — 97% dos sem-margem simplesmente não têm venda (R$0 de NF-e), custo só destrava 156 clientes (2,8%); padrão sistêmico = produto `ativo=false` (cobertura 98,5% ativos vs 3,2% inativos; **99,59% na receita de 90d**); achado colateral (margem sem janela temporal, cobre 58% da receita; 415 clientes >½ fora) + saída barata (`itens_com_custo`/`itens_sem_custo` já computados e DESCARTADOS no writer `calculate-scores:572/688`); veredito: NÃO fazer mutirão de cadastro |
| [farmer-scoring-paridade-ts-sql.md](farmer-scoring-paridade-ts-sql.md) | o baseline de paridade TS×SQL que a §5.1 do spec pedia e o #1543 não fez (saiu com a divergência DECLARADA em vez de MEDIDA). Medido em prod 2026-08-13 sobre as **3 personas reais** (1 master + 2 farmers): **FAIXA** muda em 58 (6,9%) no master e 497 (59,5%)/405 (48,5%) nos farmers; **HEALTH** Δ médio 1,36/4,08/3,56 e **CLASSE** em 14 (1,7%)/78 (9,3%)/44 (5,3%); **AGENDA: ZERO** entram ou saem — nenhuma dimensão de quota lê `g` (o comentário do #1543 dizia que a ordem "pode mudar junto" e era **falso**), só o rótulo `healthClass` muda em 3/1/2 dos 20 slots. O delta tem **dois eixos de sinais opostos**: UNIVERSO só ADICIONA (28 ganham margem, 0 perdem) e ESCOPO só REMOVE (472/379 perdem, porque 541/440 dos 835 clientes na tela são de OUTRA carteira) — somar os dois daria número sem significado; `eligible` **não** é a causa (os 2.127 não-eligible são aliases fiscais sem pedido), e a perda **não é regressão, é a autorização funcionando**. Cenário C medido (alinhar a allowlist): 835 → 1.195 clientes e **9 dos 20 slots** da agenda trocam, fechando o eixo universo (delta do master → 1 cliente) e não tocando o escopo — decisão de PRODUTO. Cenário D medido (tela do farmer restrita à carteira, o eixo dominante): 835 → 294/395 clientes, faixa despenca a **8,5%/6,6%** (o patamar do master ⇒ sobra só o eixo universo) e "sem margem" cai de 562/464 para **21/24**, ao custo de **11–12 dos 20 slots** — mais que o cenário C, porque mexe na população e logo em `m`/`recover`/`priority`; o **master é o controle e não se move em nada** (835→835, 58→58, 0 slots), o que prova que o filtro atinge só quem não tem `cap_carteira_ler`. Harness `db/test-fu4f-fase3-paridade-ts-sql.sh` com 3 falsificações, sendo a F3 (perturbar a prioridade ⇒ agenda diverge) a que faz o "zero" da agenda valer. ⚠️ reprodutibilidade: `mudou_faixa` é idêntico entre execuções, `mudou_classe` carrega **±2 de ruído** (nasce da recência, que anda com o relógio) — não é regressão |
| [f3-fontes-faturamento-bu.md](f3-fontes-faturamento-bu.md) | **F3 "verdade fiscal das 3 BUs" (#1731)** — a fatia pedia fazer `venda_items_history` (6.009 linhas, `empresa='OBEN'` apenas) cobrir as três empresas; a medição mostrou que **a entrega pedida não era o caminho mais curto para o resultado pedido**. Causa do OBEN-only: a edge `omie-sync-vendas-items` **não tem cron próprio** (roda como 5º step do `omie-cron-diario`, cujo único cron passa `{empresa:'OBEN'}` fixo) — faltava o cron, não a credencial; cadeia provada por **correlação temporal** (insert 22:16:10 ↔ log 22:16:04), com 2 pistas falsas descartadas (cron homônimo de outra edge; webhook morto há 90d). O achado que reescreveu a fatia: o financeiro **já cobre as 3 BUs** via `fin_dre_competencia_base` (colacor R$17,79M · oben R$9,62M · colacor_sc R$751,8k) → para comparar faturamento **não era preciso sync nenhum**. Armadilhas medidas: `fin_contas_receber` **≠ faturamento** (título/parcela; diverge do DRE em R$527k só na colacor — fecha bonito e é o número errado); **3 grafias** para as mesmas contas Omie → filtro errado dá **zero silencioso**; `colacor_sc` sem NF-e de produto é **desenho** (Simples/serviços: 0 produtos, 0 pedidos, mas fatura R$751,8k via NFS-e); devolução CFOP 6202 soma como positivo (R$10.043); 2,13% das linhas são consolidação de SKU repetido (valor/qtd certos, `cfop` da 1ª linha, falta `nCodItem` na chave natural). Codex confirmou o de `contas_receber` e teve o da chave natural **parcialmente refutado** pela medição. Ligar COLACOR ficou **deliberadamente fora**, condicionado a sinal (regra do #1726) |
| [escolha-de-modelo.md](escolha-de-modelo.md) | custo de token, 3º fator (`requests × contexto × **preço**`), medido em 48 dias: Fable = 34% do custo com 18% dos requests. Teste decisivo — 91% das sessões mistas COMEÇARAM em Fable e desceram → era **inércia, não escolha** (nenhum modelo estava fixado). Mas ~75% do uso se justifica (money-path/auditoria ampla/long-horizon) e só ~25% é exploratório, **53% dele em 3 sessões**; estimativa anterior de US$ 6.336/mês **refutada** (real ≈ US$ 800–990/mês, e é cota, não desembolso). Decisão: **não** virou regra no CLAUDE.md (não cabia; e regra escrita perde para default) → `"model": "opus"` no `.claude/settings.json` + degrau do alerta de contexto normalizado por **custo** (Fable avisa em 125k, não 250k). **Verificação pós-entrega (2026-08-04): INCONCLUSIVA** — rodou 6min após o merge (280 requests pós, 0,2%), mas devolveu 2 achados: (1) a asserção era **intestável** — `--dias N` filtra por mtime de ARQUIVO, não data de request (`--dias 30` trouxe requests desde 05-20, 5,1% fora da janela) → corrigido com `--desde`/`--ate` + `%reqs` + teste; (2) ❌ premissa "vale em todas as worktrees" **refutada** — `.claude/settings.json` é versionado, então só vale onde o branch tem o commit: **9/47 worktrees**, e **77% dos requests de 7d vinham das SEM a linha**. **Reavaliado 48h depois: implicação ERRADA** — o parque de worktrees rotaciona em DIAS (3 das 5 maiores recicladas, 1 ganhou a linha sozinha), e a cobertura ponderada por TRÁFEGO foi de 23% → **86,7%** sem intervenção; a contagem crua de worktrees (6/41) é a métrica errada, pois as sem-linha são as paradas. Conserto estrutural virou desnecessário; sobra só o diretório principal, que não rotaciona |
| [revisao-completa-2026-07-04.md](revisao-completa-2026-07-04.md) | varredura completa do app (11 subagentes por domínio + sweep transversal) sobre baseline mecânico verde — **todos os bugs achados passavam por CI verde**. Corrigido na sessão: #1162 (transcrição vazando entre chamadas/LGPD; fila offline apagando mutação no flush; preço 10× pela vírgula pt-BR; margem do farmer inflada por custo-zero), #1164 (`is_approved` auto-aprovável pelo próprio customer, PG17 12/12), #1172 (`.or()` em UPDATE derrubava toda efetivação de NF-e — 42703), #1174 (RPC SECURITY DEFINER exposta a anon/authenticated), #1176 (2 KPIs sempre-zero do cockpit). O resto virou **backlog priorizado** por domínio (vendas/farmer/financeiro/reposição/estoque/auth/tintométrico/edges) + temas sistêmicos (cap 1.000 do PostgREST, data UTC como "hoje", TTL 5min do lock de efetivação) — e a seção "saiu limpo", para não inflar |
| [auditoria-health-2026-07-06.md](auditoria-health-2026-07-06.md) | auditoria mecânica (health stack + sweeps de convenção UI e de padrão perigoso money-path), complementar à revisão de 07-04: knip 362→254 linhas (17 files e 72 deps fantasma eliminados — o que sobra é sinal real), flaky money-path do `SalesQuotes.priceGuard` (o teste tinha timeout de 15s, mas `findByRole` usava o default de 1s), dead code deletado e `knip.json` reescrito. Registra também os **falsos positivos descartados com razão provada** (`?? 0` que degrada fail-closed; gate de crédito que subestima e por isso dispara MAIS). Lote 2: 26 unused exports (74→48). Lote 3 (responsividade 375px): a escala tipográfica v3 recalibrou `text-base` para 14px e **neutralizou o anti-zoom do shadcn input** → todo campo do app zoomava ao focar no iOS Safari (fix `font-size: max(16px,1em)` sob `pointer: coarse`); as 29 quebras de grid nas analíticas ficaram para lote pós-merge (4 sessões paralelas na mesma superfície). Mais o lote 1 da migração react-query (5 telas, warnings 78→72) e o fecho dos 52 `<Loader2>` → `<PageSkeleton>` (70 páginas: #1210 + follow-up) |
| [melhorias-code-2026-07.md](melhorias-code-2026-07.md) | diagnóstico do setup Claude Code sobre **240 sessões-mãe (880MB) em 214 worktrees** (09/jun→06/jul): 4.633 mensagens do founder — **~450 são só "Retome"** e ~160 são "posso excluir a sessão?" —, 2.397 erros de ferramenta dos quais **65% (1.561, em 117 sessões e 21 dias) são uma classe só** (classificador de permissões indisponível), ~350 `codex exec` em foreground e claude-mem com 331 sessões, 5.000 prompts e **0 observações**. 12 candidatos ordenados por impacto (frequência × custo × alcance): 1-9 aprovados e executados (diário em [setup-agente.md](setup-agente.md)), 10-12 na 2ª leva — o 12 entregou o `verify-frontend.sh` paralelo (299s→61s, ~4,9×). Inclui o que **não** mexer (guard-rails com 0 falso-positivo, fábrica de chips, psql-ro) e as limitações do próprio diagnóstico |
| [setup-agente.md](setup-agente.md) | execução dos candidatos 1-9 acima (2026-07-06): allowlist na sintaxe canônica `:*` + `heavy-guard` que **reescreve** o comando em vez de negar (24 casos), `codex-async.sh` com preflight de auth antes de gastar quota, `pr-watch.sh`, skills `/fecho` e `/handoff-sessao`, pré-voo PROD via psql-ro no `lovable-db-operator` (≥25 SQLs já haviam quebrado na mão do founder), hooks `pos-compact-ptbr`/`vigia-worktree`, e o claude-mem consertado em 2 camadas (**0 → 214 observações na 1ª hora pós-login**). Mais o post-mortem do `pr-watch` (2026-07-18): **`exit 5` saía tanto de "consultei, sem desfecho" quanto de "não consegui consultar"** — o #1396 tinha MERGEADO e o watcher reportou timeout; fix = `exit 6` para estado desconhecido + cartada final com backoff + janela contando **vigília** (a máquina dormiu e o relógio saltou o deadline), provado com `gh`/`date`/`sleep` stubados e 4 sabotagens que derrubam só o que cada uma guarda |
| [pcp.md](pcp.md) | diário do programa **PCP** (Fases 1A → 2A, 2026-07-04→08): malha do Omie espelhada pela edge `omie-malha-sync` (sync real de 41 páginas, 2.011 produtos, 6.732 componentes, `shape_err=0`), parser dimensional e destilação paramétrica da BOM. Os dados reais refutaram o modelo da FITA (é proporcional, não aditivo — valida 96,1% das 1.378 fitas) e a 1ª destilação em prod deu 78,4% → modelo **híbrido** (cola tabelada por linha/largura) → **95,4%**, com 223 exceções em 174 de 1.398 cintas que são majoritariamente **erro de cadastro no Omie** (a destilação virou auditoria da fonte) + tela `/producao/bom-excecoes`. F1B-M1 núcleo event-sourced (PASS=26) e F1B-M2 corte múltiplo (PASS=35; 109 rotas e 0 violações de invariante em prod); F2A custo-padrão (PASS=62), onde a **sonda empírica achou o `custoProducao` do Omie 100% zerado (0/2011)** antes de o schema nascer errado. Dezenas de bugs pegos na bancada por painel tri-modelo + falsificação — inclusive um **teste-teatro do advisory lock** que passava mesmo sabotado (a 2ª conexão travava no row-lock, não no lock) e o FIX 7, achado só no psql-ro pós-deploy (38 SKUs de estrutura vazia fabricavam custo 0, R$ 1.309 de impacto falso no topo da fila) |
| [modularizacao.md](modularizacao.md) | programa de modularização F1→F4: **F1** (#1251) manifesto de 15 módulos com 1-dono-por-arquivo + gate `manifesto.gate` + boletim de saúde por módulo; **F2** (#1255) gate de fronteiras anti-vazamento com ratchet, baseline de **284 arestas** (219 runtime + 65 type, 58 pares), extrator = parser TS e não regex (o risco nº1 era falso-negativo); **F4** bundle por módulo — o entry carregava ~48 KB de fonte de NEGÓCIO, 3 chips do header viraram `React.lazy` (72,94→70,09 kB gzip). **F3 re-decidida** (founder + Codex): mover tudo para `src/modules/` foi DESCARTADO — *"não reduz uma única aresta, só troca conflito de arquitetura por conflito de Git"* — virou programa cirúrgico guiado pelo relatório de fronteiras: PR-1 #1262 (`routePlanner` era erro de corte, não acoplamento: 284→239) e PR-2 (análise SPIN é da chamada, não do farmer: 239→226). Anti-padrão nomeado: plataforma como "lixão de contratos" |
| [reposicao-embalagem-captura.md](reposicao-embalagem-captura.md) | captura mensal de preços de **embalagem econômica no portal Sayerlack** — Fase 1 em 7 PRs (#1339 edge + run-log + cron `0 9 10-12 * *` · #1348 · #1374 · #1383 · #1388 · #1392 · #1400), aplicada em prod com o kill-switch `embalagem_captura_automatica_habilitada` ainda em **false** (ligar é decisão do founder). O miolo é a saga dos 8 spikes, cada falha ensinando um guard: `confirm()` dismissado por default do puppeteer, placeholder do DataTables lido como linha de rascunho sujo, ✖ de edição ≠ remoção, budget de 340s matando o full órfão na persistência — e no v7 a **conferência de identidade reprovou os 2 preços** (o select2 trazia o item errado), **0 preços errados gravados**: precisão>recall funcionando. Padrões reusáveis: helper compartilhado interpolado no Browserless via `${fn.toString()}`, `trace_tail` no erro do run, guard fail-safe que relata o que viu |
| [sayerlack-pos-login-falso-positivo.md](sayerlack-pos-login-falso-positivo.md) | `login_success via url_changed` dava FALSO POSITIVO no portal Sayerlack — o redirect para a tela de **troca de senha obrigatória** virava `EXCEPTION` anônima 3s depois, e o `fornecedor_alerta` estava gateado por `LOGIN_FAILED`: o aviso certo atrás da condição errada (pedido 1939, 3 falhas, ~1h calado). Confirmação POSITIVA de dashboard nas 2 edges + `PASSWORD_CHANGE_REQUIRED`/`POS_LOGIN_NAO_DASHBOARD` + alerta decidido por função pura. **Achado colateral:** o `*/*` do header `Accept` cegava os gates textuais (1.041 de 1.226 linhas invisíveis) — a CLASSE foi fechada em paralelo pelo `limpeza-fonte`, e este PR quita os sítios que aquela revelação expôs. |
| [ci-testes-edge-deno.md](ci-testes-edge-deno.md) | **os 119 testes Deno das edges rodavam em lugar nenhum** (#1437, 2026-07-18) — o `include` do vitest é só `src/`+`scripts/`, então eram documentação executável e não guarda de regressão, com todo PR auto-mergeando no verde. Entraram como **step do `validate`** (a branch protection exige só ele — job paralelo reproduziria o próprio bug que o PR conserta) e com `--no-remote`, o que expôs a invariante: **teste de edge não pode ter import remoto** (o `jsr:` de 1 arquivo poria o jsr.io no caminho de entrega de todo PR do repo). Sequela (#1498): o `test:edges` **não type-checa** — só o grafo que os testes alcançam, e nenhum `index.ts` entra nele; um símbolo fora do import sobreviveu a 5.527 testes vitest, 181 deno, typecheck e lint. Fechado pelo `edges:typecheck` sobre as 93 edges, gate de **precisão** (a classe que quebra em runtime está em zero; os 141 erros de tipo em 25 edges são dívida tolerada). Inclui por que o pin do deno **não** ganhou o gate espelhado do bun e por que o `deno lint` ficou fora (198 problemas, 141 do padrão que a plataforma exige). Sequela (#1772, 2026-08-18) — a **terceira perna**: o **vitest** vigia a FORMA da edge, lendo `supabase/functions/` como TEXTO (20 arquivos, 70 das 95 edges), então refactor puramente sintático reprova o `validate` com os três comandos "de edge" (`test:edges`, `edges:typecheck`, `bun lint`) **exit 0**. |
| [teste-que-afirma-o-defeito.md](teste-que-afirma-o-defeito.md) | suíte verde pode significar "defeito PROTEGIDO" (6 testes afirmavam o vazamento como contrato) + "verificar o sink" é MEDIR: `this.cause=x` é enumerável e `JSON.stringify(err)` vazava o cru |
| [ci-typecheck-scripts-db.md](ci-typecheck-scripts-db.md) | **nenhum gate type-checava `scripts/` nem `db/`** (2026-08-13) — irmão do doc acima, mesma classe de lacuna em outro diretório: `tsconfig.app.json` inclui só `src`, knip só olha `src`+`supabase/functions`, vitest **executa** `scripts/**/*.test.ts` mas transpila sem type-checar, e eslint não type-checa. Erro de tipo passava verde nos 4 gates — inclusive nos arquivos que **são** os gates (authz/bun-pin/docs-indice/edges-typecheck). Não era hipotético: o #1201 deletou `src/lib/radar/types.ts` como "0 refs provadas" e quebrou `scripts/radar/carga.ts`, TS2307 **verde por 5 semanas** — as duas ferramentas erraram pelo mesmo motivo (a ref estava fora do `project` do knip **e** do `include` do tsc). Dos 19 erros achados, 9 eram só configuração (`import.meta.main` sem `bun-types`) e 10 dívida real, todos corrigidos → gate **sem baseline**, um erro novo em qualquer arquivo fica vermelho. Falsificado em 5 alvos (raiz, subdiretório, `db/`, `.test.ts` e **arquivo novo**) com controle verde antes e depois |
| [knip-scripts-causa-raiz.md](knip-scripts-causa-raiz.md) | **`scripts/` entra no `project` do knip** (2026-08-13) — follow-up imediato do #1720, que fechava o *sintoma* (TS2307 vermelho na hora) e não a *causa*: com `scripts/` fora do `project`, o knip não via as refs de `scripts/`→`src/` e seguiria propondo o delete do #1201. Medição confirmou a previsão: dos 15 achados, **10 eram falsos-positivos** (entrypoints de CLI — `city-norm-print.ts` é chamado por um `.sh`, `fronteiras-modulos.ts` gera a baseline que um teste cobra) e 5 reais, des-exportados. A decisão que dá valor: `scripts/**` vai para `entry` **menos `scripts/lib/**`** — pôr tudo em entry mataria a auditoria de exports e a ampliação viraria enfeite; deixando a lib de fora, é lá (authz-contract/authz-grants/migration-objects) que export órfão é detectado, e nasce a convenção de que lib compartilhada em `scripts/` vive sob `scripts/lib/`. Falsificado em 3 provas — morde onde deve, **não** morde em CLI novo, e a causa raiz fechou **por contraste** (o mesmo cenário rodado contra a config antiga de `git show HEAD:knip.json` acusa, reproduzindo o #1201; sem esse contraste o verde seria evidência vazia). Sem step novo de CI, sem baseline, sem `ignore` |
| [piso-de-contexto.md](piso-de-contexto.md) | registro central da linha de trabalho de **custo de contexto** (medido em 20.933 requests/7 dias; piso médio de 68.537 tokens). **Três hipóteses plausíveis foram medidas e refutadas**: encurtar as `description` das skills rendeu 57 tokens (ruído), tirar 14 skills não encolheu a lista, e esconder as 113 nunca usadas **PIOROU o piso em +141** — o orçamento se repreenche, e com descrições mais longas que as removidas. A saída foi achar a **chave**, não editar `SKILL.md`: `skillListingMaxDescChars 220` + `skillListingBudgetFraction 0.004` → **−7.179 tokens aplicados** (listing 30.446→12.148 chars) sem perder o gatilho de 8/8 das skills que o founder de fato invoca. E a virada de alvo: o piso inteiro é só **26%** do custo de entrada — os 74% são **ocupação** (Read custa ~55% com 75 chamadas contra ~40% de 769 do Bash; e `head -n` corta LINHAS enquanto o contexto se paga em BYTES) → guard de Read do #1647 + `bash-contexto-nudge`. Réguas versionadas: `scripts/piso-contexto.sh` (±6 tokens), `tokens-report.sh`, `ocupacao-contexto.sh` |
| [reposicao-auto-aprovacao-piloto.md](reposicao-auto-aprovacao-piloto.md) | post-mortem do piloto N3 de **auto-aprovação de compra Sayerlack** (em prod 2026-06-11, encerrado 2026-07-09): em ~4 semanas ele **nunca auto-aprovou nada** — `reposicao_auto_aprovacao_log` com **0 linhas *ever*** (nem v1 nem v2), com o fusível `reposicao_auto_aprovacao_ativa` em `false` desde o seed e o cron do tick ativo o tempo todo. Não foi falta de candidato: pedidos elegíveis (OBEN ≥ R$3k, máximos de 17k–22k) fluíram quase todo dia e **foram aprovados na mão**. Veredito **inconclusivo/inerte** — não pode ser promovido (zero dado para provar veto < 10%) e não falhou aberto (nenhuma compra errada); encerramento **leve**: tarefa agendada removida, código-máquina **dormente** em produção. Duas causas-raiz candidatas em aberto (fusível nunca ligado conscientemente · migration da v2 pode ter caído na falha SILENCIOSA do Lovable — conferir o corpo da função antes de religar) e um caveat de método: `company_config` **não tem trigger de `updated_at`**, então "`updated_at == created_at` ⇒ nunca tocado" é inferência fraca ali (o veredito se sustenta no log vazio, que é prova direta). ⚠️ **NÃO** ligar `ativa=true` achando que o piloto já validou o comportamento. Registra ainda a remoção da trava de R$3k no mesmo dia — era **uma régua com dois usos** (alerta "pronto pra aprovar" + gate de disparo), e `≤ 0` desliga os dois de uma vez |
| [faxina-knip-2026-07-07.md](faxina-knip-2026-07-07.md) | a sessão que zerou os **74 unused exports + 180 unused types** e promoveu `bunx knip` a **gate de regressão de dead code** (exit 1 com 254 linhas → **exit 0**) — #1212, follow-up previsto no backlog da auditoria de 07-06. Método: des-exportação em massa **ancorada no relatório do knip** (só a linha `arquivo:linha` exata; qualquer outra forma vira SKIP manual — não `sed` cego), com rede dupla no `tsc` strict, cujo `noUnusedLocals` é quem prova que nenhum símbolo des-exportado ficou órfão e converte o caso em delete. Deletes reais (0 consumidores) e a distinção que evita perda de contexto: **constante money-path documental** (`COBERTURA_MIN_EMPRESA`, `EPSILON_MONETARIO`, `PLAUSIBILIDADE_TETO_DIAS`…) foi des-exportada, **não deletada** — segue no arquivo documentando o threshold ao lado do uso. O valor durável está nas **fronteiras que ficaram de fora com razão**: os exports "sem consumidor" de `_shared/sayerlack-sku.ts` são o preço da **paridade byte-exata** com o canônico do `src/` (o parity test quebra dos dois lados), `cost-compute` só pôde ser des-exportado **identicamente nos dois lados**, e os needles dos `.mut` são trechos internos de corpo de função — des-exportar declaração não dessincroniza contrato |
| [programa-cabreuva-colacor.md](programa-cabreuva-colacor.md) | benchmark do **mega CD de Cabreúva da Renner** (Brazil Journal, 14/07/2026) rodado pela skill `benchmark-externo` → tabela de 11 gaps com evidência `arquivo:linha` + priorização Codex + programa em fases. O padrão do gap é a tese: *a Renner dos **diagnósticos** nós já somos* (motor de reposição por SKU, baixo-giro, venda-perdida, picking mobile, 3 empresas numa plataforma) — falta a das **ações** e da **ponta** (reservar/ATP, sugerir reabastecimento, desovar excesso, expedir, medir por canal). O achado de dado que reordenou tudo (psql-ro): **`sales_orders.origem` é ~100% NULL — 1 pedido classificado em 30.650**, e 12 meses com ZERO venda nascida no app → "margem por canal" nasce como **espelho de digitalização**, e a adoção do canal digital vira A alavanca. Pista A (quick-wins) entregue nos PRs #1659 (margem por canal), #1662 (desova acionável) e #1663 (giro executivo); pista B são os épicos em ordem de dependência — ATP/reserva, pedido sugerido, GMROI, expedição e consolidação do grupo (esta com risco fiscal dos aliases) |
| [prs-parados-2026-08-06.md](prs-parados-2026-08-06.md) | os 2 PRs não-draft do repo estavam `CONFLICTING` (o auto-merge nunca dispara nesse estado) e por isso dormiram 3 e 4 semanas: #1332 (WhatsApp PR-4) e #1212 ([faxina knip](faxina-knip-2026-07-07.md)), destravados e mergeados no mesmo dia. A tese é que **PR parado apodrece de duas formas opostas**: (1) a regra nasce DEPOIS do código — 2 gates de agosto reprovaram código de julho, e os dois achados eram reais, incluindo uma leitura que descartava o `error` e apagaria a trava `conversa_de_outro_cliente` (proposta com preços sairia no fio de outro cliente); **`validate` verde de 3 semanas atrás atesta o código contra as regras de então**; (2) a intenção INVERTE — num PR de dead code a premissa é sobre o resto do repo, e `EdgeFunctionError` saiu de morto para 6 consumidores + `status`/`retryAfterSeconds`, então aplicar a des-exportação original quebraria o build (mesmo arquivo, resolução dividida). Mais: artefato gerado se **regenera**, não se resolve (626 migrations = 624+2, provado com `comm -23`); e o `edges:typecheck` reprovando por `500` do esm.sh é **desenho** — o que autoriza o rerun é o passe vizinho (mesmo step verde no outro PR 20min antes), não fé |
| [medicao-trabalho-nao-entregue.md](medicao-trabalho-nao-entregue.md) | **as três armadilhas de medir "isto já foi entregue?"** num repo que faz **squash**-merge — e o `bun run wt:orfas` (irmão do `wt-status`/`wt-clean`/`wt-reap`/`wt-prune`) que cataloga as sessões Claude cujo worktree já sumiu. Em 2026-08-06 eram **104 sessões órfãs: 89 entregues, 15 com commit fora da main** (incluindo money-path), e o founder não tinha como ver isso sem abrir uma por uma. As três medidas ingênuas **inflaram** a apuração: `merge-base --is-ancestor` **nunca** é verdade após squash (**66 falsos pendentes** — o certo é `headRefOid` do PR MERGED == tip da branch); a linha `pr-link` do transcript grava PR **citado** na conversa, não produzido (uma sessão listava 17 PRs que não fez → **9 falsos "PR aberto"**; o certo é `headRefName == gitBranch`); e `git cherry` compara **patch-id**, que o squash de N commits destrói (**18 commits fantasma** numa branch de 1 — o certo é `git rev-list --count <headRefOid>..<branch> --not origin/main`). Uma **quarta** apareceu ao reimplementar: `IFS=$'\t' read` **colapsa** tabs consecutivos (tab é IFS-*whitespace*), então campo vazio no meio some e desloca os seguintes — **117 de 449 linhas** saíram com a branch trocada pelo peso; a correção é o separador (`US`, `$'\037'`), não o `read`. A tese é sobre MEDIÇÃO, não sobre worktrees: **teste que mede a coisa errada não devolve "não sei", devolve número plausível e falso** — e pendência fantasma custa mais que pendência desconhecida. O corolário: as 4 erram **por linha, não por relatório** — cada uma corrompe só o subconjunto que satisfaz uma condição e o resto sai certo, então **conferir amostra não detecta nenhuma**; o que detecta é sabotar a medida e exigir vermelho. Provado com squash de verdade em repo descartável + falsificação das 5 asserções em `LC_ALL=C` e `pt_BR.UTF-8`. **§ o limite que fica** (2026-08-07): consertar a contagem de COMMITS não fecha a distância até a pergunta, que é sobre CONTEÚDO — com squash o conteúdo chega por outro PR sem o commit virar ancestral, e na 1ª triagem real **as 2 candidatas money-path estavam entregues** (uma tinha 4/5 arquivos idênticos e o 5º diferia porque a MAIN estava à frente). Nenhum teste automático barato decide: blob-a-blob erra 39/40, `git apply -R` erra pelo contexto vizinho. Logo o `wt:orfas` é **filtro de triagem, não veredito** — tratar o proxy como medida recria pendência fantasma com o número já corrigido |
| [duplicata-por-objetivo.md](duplicata-por-objetivo.md) | a classe **"a tarefa inteira já foi ENTREGUE por outra sessão, sem conflito de arquivo nenhum"** — o eixo OBJETIVO, irmão do eixo ARQUIVO que a §Multi-sessão do CLAUDE.md já cobria. 3 ocorrências num só dia (2026-08-15): duas comidas pelo MESMO commit alheio (`c542210c`/#1744) e uma cujo conserto já estava na main com **solução melhor** (`a891ba9c`, `DENO_NO_PACKAGE_JSON=1`). O eixo já estava escrito desde 2026-07-23; o que falhou foi o **detector** — varrer TÍTULO de PR dá **0 hits** (o entregador mergeia sob o tema DELE, e `gh pr list` lista só aberto) contra **1 hit** do `git grep <símbolo> origin/main`. Regra certa com sensor cego é mais cara que regra ausente, porque ninguém suspeita dela. |
| [gate-indice-docs.md](gate-indice-docs.md) | a classe **"o `.md` existe, o índice não o lista, e nada quebra"** e as duas versões do gate que a fecham. Cinco PRs em duas semanas: #1658 e #1665 reconciliaram 20 arquivos invisíveis à mão, #1670 pôs o gate estrutural, #1674 destravou a main que ele deixou vermelha (o #1212 entrou com doc sem linha, CI rodado sobre base anterior ao gate). O **falso-negativo da 1ª versão**: casar links no README INTEIRO fazia um doc *apenas citado dentro do resumo de outra entrada* contar como indexado — provado contra o índice real (apagar a linha de `setup-agente.md`, citado no resumo de `melhorias-code-2026-07.md`, mantinha o gate VERDE). Fechado lendo a **1ª coluna da tabela**, preservando a âncora (`](x.md#secao)`), que é uso normal e cuja perda seria o falso-positivo mais provável. As **5 invariantes** (órfão · link quebrado · duplicata de duas worktrees que o merge aceita sem conflito · texto ≠ destino · piso de resumo de 30 chars, com a menor célula viva em 44) e o método: cada uma **sabotada uma a uma** exigindo vermelho só no teste que a guarda — a sabotagem que troca o órfão pela busca solta passou VERDE na 1ª tentativa e foi ela, não a leitura do código, que revelou o teste ausente |
| [gate-links-docs.md](gate-links-docs.md) | o gate `docs:links`, que RESOLVE link relativo de `.md` — a classe do #1863 (`../../` de `.claude/skills/fecho/` caindo em `.claude/docs/`), os **10 links quebrados vivos na main** que ele achou ao nascer, e por que `.claude/skills/` não tinha cobertura nenhuma. Inline code (não cerca) era o falso-positivo; autoridade é o índice do git, não o `existsSync` (APFS é case-insensitive). |
| [fase-sem-sinal.md](fase-sem-sinal.md) | a classe **"a fase N+1 construída sobre uma fase N que nunca provou estar viva"** e a regra que a fecha (§Armadilhas do CLAUDE.md): antes da fase N+1, exija ≥1 sinal POSITIVO de uso real em prod **com denominador**. Três precedentes, três disfarces do mesmo silêncio — o piloto Sayerlack de auto-aprovação (fusível `false` desde o seed, log com **0 linhas *ever***, recalibrado v1→v2 sem nunca ter auto-aprovado nada, e o silêncio lido como "calibragem errada"), a rota do Farmer `/rota/ligacoes` (#1717 — pipeline íntegro com 24 cidades e centenas de candidatos, `route_contact_log`/`route_queue_snapshot` zerados desde a origem, 4 saídas-vazias + "nunca aberta" indistinguíveis no mesmo pixel) e o plano tático (#1713/#1716 — "0 desfechos" lido como veredito sobre a tela, quando `employee` tinha 2 usuários e **0 com sessão viva**, e o único usuário ativo do sistema era o founder). Traz a query canônica do denominador (`user_roles ⋈ auth.sessions`, com `last_sign_in_at` como o segundo sinal que lhe dá dente), os 5 passos de aplicação e o recorte de onde a regra **não** vale |
| [sentinela-grants-tabelas-fechadas.md](sentinela-grants-tabelas-fechadas.md) | a **Parte C do `authz:check`**: vigia as tabelas fechadas por PRIVILÉGIO (`REVOKE ALL` + `GRANT SELECT`), onde um `GRANT` futuro reabre em silêncio. Três vetores com dente provado por falsificação no comando real — `REABERTURA`, `RECRIACAO` (o `DROP`+`CREATE` faz a tabela renascer com o default privilege aberto, sem `GRANT` no diff) e `RLS_OFF`. Resgate da branch órfã `claude/jolly-wescoff-d36818`, cuja allowlist passou 3 semanas declarando aberta uma `omie_products` já fechada em prod (#1558) — o `ANCORA_NAO_DECLARADA` que o próprio desenho existia para impedir. Lição: **artefato que declara pendência precisa de mecanismo que force sua atualização quando a pendência resolve** |
| [stub-gate-degenerado.md](stub-gate-degenerado.md) | a classe **"o stub do gate no harness é mais simples que a prod e apaga a distinção entre duas políticas"** — o bloco de authz passa a provar só "master vs resto" e fica verde enquanto a autorização regride. O discriminante (existe persona no seed que as 2 políticas classificam diferente?), as 2 instâncias (#1761 e `cockpit-preco`/`defasagem`) e como reauditar EXECUTANDO os stubs, não lendo-os. |
| [sentinela-authz-controle-nao-mencao.md](sentinela-authz-controle-nao-mencao.md) | a defesa de authz de `reposicao_pos_candidatos` era regex sobre `pg_get_functiondef` que rodou **uma vez** — e a revisão do #1718 mostrou 3 corpos que ela APROVA e que regridem a autorização (gate em ramo morto · chamada nova só em COMENTÁRIO com o gate velho real · guard em comentário). Trocada por 2 camadas: a RPC entra no `AUTHZ_MANIFEST` (Parte A, last-writer, vale para **toda** recriação futura) e o harness PG17 ganha N1/N2/N3, que medem o **par** — os regexes APROVAM e o assert comportamental fica vermelho. Registrar a função exigiu antes consertar duas cegueiras MEDIDAS do detector: a forma real do repo `(SELECT gate(…)) IS NOT TRUE` era lida como gate decorativo (por isso a RPC nunca entrou no contrato) e `IF 1=0 AND NOT gate() THEN RAISE` passava verde. Lições: **casar a CHAMADA ainda é casar texto — o que separa menção de controle é executar**; e **detector calibrado só na forma canônica não vigia o código que existe**. Prod estava correta o tempo todo |
| [verificar-sonda-versao.md](verificar-sonda-versao.md) | verificação da sonda `{"probe":true}` das 4 edges do #1766 sem pagar o efeito caro: a prova PASSIVA pelo `versao` que toda resposta carrega (um cron já prova a versão), a armadilha `jobname` ≠ edge do `command`, o `versao.ts` novo que quebra a edge se o deploy levar só o `index.ts`, e por que a ordem gate↔sonda muda a leitura de um 401 |
| [fallback-lista-vazia-troca-escopo.md](fallback-lista-vazia-troca-escopo.md) | a classe **"a condição não é o comentário"**: os 2 motores do Farmer tinham `if (!scores.length) recarregue SEM filtro de farmer_id` sob o rótulo *"fallback for super_admin"* — mas a condição só perguntava se a leitura veio VAZIA, então o desfecho mais destrutivo ("carregue a base inteira e grave sob este nome") virou o **default de uma falha**. **2.676** recomendações gravadas sob um `farmer_id` que não era o dono do cliente e, por isso, invisíveis ao recálculo do dono real (as RPCs expiram `WHERE farmer_id = p_farmer_id`). Abril/2026: 25,9% dos clientes do lote eram do gravador, que detém 18,8% da base — a assinatura do acaso. Corrigido nos DOIS lados (fallback removido + gate `FG009` na RPC, que o browser não contorna) — e `FG008` já era de outra defesa, SQLSTATE repetida engana até o próprio teste.
| [farmer-troca-dono-oferta-pendente.md](farmer-troca-dono-oferta-pendente.md) | fatia 2 do #escopo-orfao: a oferta pendente que **não seguia o cliente na troca de dono** — o UPDATE de expiração das RPCs filtra `WHERE farmer_id = p_farmer_id`, então o recálculo do dono NOVO não a alcança. Medição ANTES **com denominador** (0 fora de escopo / 1.083 pendentes ⇒ a fatia é **preventiva**, dito em vez de fabricar urgência). O achado que mudou o lugar: a edge `calculate-scores` **não** reatribui dono de cliente existente (o upsert só cobre `missing`) — quem reatribui é a trigger `trg_carteira_reconcile_score_owner`, logo o guard entra na fronteira `farmer_client_scores`, a mesma fonte que o `FG009` consulta. Challenge Codex confirmou o lugar e refutou a suficiência: a trigger não cobre a oferta que nasce **depois** da varredura (advisory lock é por farmer) ⇒ `FOR SHARE` nas RPCs — e `FOR KEY SHARE` não serviria (UPDATE comum toma `FOR NO KEY UPDATE`, que não conflita). Lição durável: **falsificar prova que o assert tem o dente CERTO** — o teste de concorrência passava dos dois lados por esbarrar no row lock das recomendações, não no lock de score; e a própria sabotagem começou sem sabotar (regex de extração que recriava a função verdadeira). Decisões explícitas: pendente do dono NOVO sobrevive (regra fraca, precisão>recall); `expired_reason` é coluna com CHECK, não assinatura inferida de `expired_by_run IS NULL`; policy de DELETE **não** mexida (autoridade indireta registrada, não corrigida às escondidas) |
| [tothrow-pelado.md](tothrow-pelado.md) | a classe **"o teste negativo verde que nao assere nada"**: `toThrow()` sem argumento e o `WHEN OTHERS THEN 'OK'` do TS. 7 blocos onde era a UNICA assercao (2 no FAIL-CLOSED de money-path), provados por mutation-check (1/3 -> 3/3). Inclui o meta-achado dos 3 bugs no proprio script de medicao |
| [gates-textuais-cegos.md](gates-textuais-cegos.md) | a classe **"o fiscal fica verde por CEGUEIRA"**: os 10 gates textuais limpavam comentário com regex, e um `/*` dentro de string (o `*/*` do header `Accept`) pareava com o `*/` seguinte apagando 1.041 de 1.226 linhas ANTES da medição — stripper com máquina de estados, a dívida revelada, e o sentinela do maior bloco contíguo descartado (88 legítimo vs 924 envenenado) |
| [roteirizador-corte-cidades.md](roteirizador-corte-cidades.md) | a classe **"o LIMIT não estava escondido, o CRITÉRIO estava"**: o seletor de cidades de "Visitas em campo" ranqueia por *volume de prospects* e corta em 500, mas a operação visita por **carteira e faturamento** — eixos ortogonais. Medido em prod: **52,7% do faturamento** (R$ 1,68M de R$ 3,3M em 10 meses de NF-e) vem de cidade que o seletor não oferece, a maior fonte de receita fora do seletor (Carmo do Cajuru, R$ 697k, 21% do total) está no rank **917**, MG mostra 59 das suas 848 cidades (7%) sendo 94% da carteira, e **19 das 24 cidades do roteiro semanal oficial** (`route_schedule`) ficam de fora. Por que **subir o limite não corrige** (no teto de 2.000 do SQL faltam 10,9% do faturamento; São Félix de Balsas/MA, R$ 334k, está no rank 3.763), a **negativa falsa** do `CommandInput` ("Nenhuma cidade encontrada" para cidade que fatura), e o veredito oposto no eixo dos 1.000 prospects/cidade (115.996 escondidos, mas só 4,9% da carteira; mediana 144 — com o viés do `ORDER BY` inerte: 523.180 de 523.183 são `a_contatar`, sobra `data_abertura DESC`, e SP entrega só CNPJ aberto há ≤6 meses) |
| [sonda-ausente-em-script-que-apaga.md](sonda-ausente-em-script-que-apaga.md) | a **assimetria** entre sensor e script destrutivo: no `wt:status` a sonda que falta degrada a seção; no `wt:clean`/`wt:prune` ela vira REMOÇÃO INDEVIDA calada ⇒ fail-CLOSED. Inclui por que `command -v` não basta (ferramenta presente-porém-quebrada) e por que o `set -e` é suspenso pelo CONTEXTO DE CHAMADA, escondendo `| head` latente. |
| [audit-migrations-falso-vermelho.md](audit-migrations-falso-vermelho.md) | **Quantas migrations custom faltam mesmo? ZERO** (medido em prod 2026-08-21 via `psql-ro`, Sections 1+2 rodadas inteiras, exit 0). Os "202 ❌ MISSING" eram do audit binário pré-#1105 — hoje são 479 custom em 5 estados: 363 🟡 (normal, não é dívida), 55 ⚪, 52 ✅, 8 ⚠️, 1 ❌. As 9 acionáveis se resolveram TODAS em falso-vermelho: 2 fantasmas `%I` de `EXECUTE format()`, 5 obsoletas (objeto superseded/tabela `omie_clientes` dropada/MV virou view) e 2 mudanças de schema (`public.carteira_visivel_para` → `private.`). Mapeamento slug↔version: **0 não mapeados** (o gerador deriva os dois do mesmo parse). Ponto cego do fence de `farmer_association_rules` (#1840): hipótese "não re-rodou o audit" FALSIFICADA (o mesmo commit regenerou) — a causa é a regex `[^\s"]+` que perde `CREATE POLICY "Nome Com Espaços"` (**50 de 450 policies, 11%, em 20 arquivos**), somada a `REVOKE`/`GRANT` sem objeto e ao `✅ registrado` que curto-circuita a checagem. Classe cega total: **64/479 (13,4%)**. Lição: fence de permissão não se prova por existência — prova-se por `pg_policies.cmd`/`qual` e `proacl`, que é o que o `/fecho` faz e o audit não |
| [paginacao-offset-janela.md](paginacao-offset-janela.md) | a classe **"o risco não segue o volume de escrita"**: paginação por OFFSET desloca sob escrita CONCORRENTE, mas só a escrita que atravessa a **fronteira do RECORTE** desloca — INSERT/DELETE sempre, UPDATE só na coluna do `.order()`/`WHERE`. Medido em prod: as 3 tabelas mais quentes do banco (8,0M / 7,2M / 6,2M updates) são SEGURAS sob offset, e `omie_products` (86 inserts, zero deletes — a mais fria) é a MAIS exposta, porque o `.eq('ativo',true)` do leitor é a coluna que um cron diário reescreve. Ranquear por `n_tup_upd` INVERTE o risco. Traz `fetchAllKeyset` + o Caminho B (Codex sem cota) que achou 3 defeitos no próprio helper. |
| [recommend-amostra-sem-denominador.md](recommend-amostra-sem-denominador.md) | a classe **"a amostra media a população errada"** (irmã do `roteirizador-corte-cidades.md`): o `sim_score` do motor de recomendação sorteava 100 clientes do `health_class` por UUID, mas **87% do cluster `critico` (5.406 de 6.185) é `sem_historico`** — linha sem venda válida, que entra no DENOMINADOR e nunca no numerador; dos 50 amostrados, 8 tinham compra. Somado ao denominador que contava 100 para um numerador de 50, o ramo `cluster_based` era **inalcançável por construção** nos três clusters (693 impressões no log, zero dele). Os dois consertos são ACOPLADOS — isolados não cruzam 0,10 em `critico`. E a correção do Codex que vale além do caso: **`minMaxNorm` apaga a escala**, então "o número é minúsculo" ≠ "não influencia o ranking" — o que estava morto era a APRESENTAÇÃO, não o sinal. |
| [mergeabilidade-assincrona.md](mergeabilidade-assincrona.md) | a classe **"a sonda cega quando o alvo esfria"**: `mergeable`/`mergeStateStatus` do GitHub é calculado SOB DEMANDA — a 1ª consulta a um PR frio enfileira o cálculo e devolve `UNKNOWN`. Medido 2026-08-21: **6 de 7 PRs abertos vieram `UNKNOWN`; a 2ª chamada devolveu 5 `CONFLICTING`** (o único que respondeu de primeira tinha 0 dia). A sonda é confiável onde não precisa e cega onde precisa — anti-correlacionada com o risco. Mordia na skill `/fecho` (one-shot, sem ramo para `UNKNOWN`, rodando justamente contra PR de sessão parada): liberava "pode excluir" em PR conflitante — 2ª reincidência da classe "APROVADO a partir de ausência de evidência" na mesma skill, após o #1677. Traz também a falsificação com **grupo de controle** que matou o enquadramento errado ("troque `pr list` por `pr view`" não conserta nada). |
| [recommend-teto-linhas-cluster.md](recommend-teto-linhas-cluster.md) | **a agregação no lugar errado ARRASTA o teto** (3ª da classe, com `roteirizador-corte-cidades.md` e o irmão acima): o `sim_score` lia `order_items` com `.order("id").limit(1000)` — e como o `id` é UUID aleatório isso é amostra de LINHAS, não janela temporal. O teto mordia e **apagava clientes que TINHAM compra** (5 em `atencao`, 2 em `estavel`), que o denominador então contava como "não comprou" — zero fabricado. Em `estavel` a edge decidia vendo **6% das linhas**. Agregar no TS obriga a ler linhas; ler linhas obriga a um teto: a correção é mover a agregação para o banco, não aumentar o teto. ⚠️ E a RPC **linha-por-produto reabriria o defeito** — o agregado tem 957/1.312/1.109 produtos e o PostgREST capa em 1.000 **inclusive em `.rpc()`**; por isso o retorno é UMA linha com `jsonb`. Janela temporal foi MEDIDA e rejeitada: derruba `critico` em 92%, porque o `health_class` já codifica recência. E a falsificação achou um assert **sem dente** — o teste do REVOKE passava sem o REVOKE, porque o harness não espelhava os GRANTs do Supabase real. |
| [evidencia-positiva-shell.md](evidencia-positiva-shell.md) | a doutrina da **evidência POSITIVA** ("ausência de sinal não é aprovação") + o catálogo das **7 armadilhas de shell que fabricam VEREDITO**: `| tail` engole o exit code, `$?` sobrescrito por um `echo` no meio, `echo|jq` corrompendo JSON no zsh, o `grep` shim que dobra acento, log em `/private/tmp` que morre no reboot, a flag homônima BSD×GNU que não falha — faz outra coisa —, e o wrapper (`heavy`/`timeout`) que aborta com exit≠0 sem o comando embrulhado ter rodado. As 6 primeiras fabricam **verde**; a 7ª, **vermelho**. Movido do CLAUDE.md (era a maior linha do arquivo). |
| [split-claude-md-sensor.md](split-claude-md-sensor.md) | por que dividir o CLAUDE.md por `@import` não economiza NADA (imports carregam no launch) e por que a maior seção (Armadilhas, 45%) é o **pior** candidato a lazy-load: ela falha ABERTO e o workflow de banco daqui nem toca `supabase/`. O que é seguro mover são as 413 palavras de convenção de código. Inclui o sensor `InstructionsLoaded` que mede se regra `paths:` chega no subagente — a pergunta que a doc não responde. |

> **As 3 camadas do refactor:** lição durável reutilizável → `docs/agent/<dominio>.md` (carregada sob demanda); **narrativa histórica** → aqui; detalhe profundo (spec/plano) → `docs/superpowers/{specs,plans}/`. Plano: `docs/superpowers/plans/2026-06-14-refactor-claude-md.md`.
