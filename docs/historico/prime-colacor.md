# Prime Colacor (assinatura B2B paga) — registro do programa

> Programa de assinatura mensal por CNPJ ("Amazon Prime" B2B do grupo): âncora = franquia de afiação (200 dentes/mês a R$1,20 de tabela) + previsibilidade operacional (coleta/devolução na rota, prioridade), com **extrato mensal honesto** (só entra em R$ o que tem contrafactual transacional real). Spec consolidada (founder + Codex + deep-research 104 agentes): `docs/superpowers/specs/2026-07-09-prime-colacor-design.md`. Fases: PR-1 fundação → PR-2 admin → PR-3 `/prime` extrato → PR-4 sync colacor_sc → PR-5 painel do piloto.

- ✅ **PR-1 fundação de dados (2026-07-11, [PR #1295](https://github.com/LucasSardenbergL/afiacao/pull/1295), migration manual `20260711090000_prime_fundacao` APLICADA em prod, validação `4|3|8|6`):** 3 tabelas (`prime_planos`, `prime_assinaturas`, `prime_beneficio_uso`) + view `v_prime_extrato_mensal` (`security_invoker`). Honestidade money-path ENFORÇADA no banco: monetizável exige `valor_tabela > 0` + `referencia` Omie; afiação amarra `valor = round(quantidade × preco_unitario_snapshot, 2)` (contrafactual auditável); registro monetário **append-only** (UPDATE só-estorno via trigger, DELETE sem policy — nem staff); uso só em assinatura ATIVA dentro da vigência (suspensa CONGELA franquia e extrato); ciclos não sobrepõem competência; status amarrado às datas (cancelada exige `data_fim`); bônus cross-sell 1/mês ≤50; grandfathering = preço/franquia congelados por trigger (campos contratuais imutáveis). View expõe `mensalidade_contratada` (**nunca "pagou"** — não há fato de pagamento na v1) e `dentes_excedentes` (estouro exposto, nunca escondido). Prova PG17 `db/test-prime-fundacao.sh` (73 asserts: RLS matriz staff/cliente/sem-role/anon, SQLSTATEs, estorno, overfranchise, sobreposição) + falsificações F1/F2 embutidas + 2 externas. Fold do challenge Codex xhigh: 14 dobrados, 1 refutado (UNIQUE de referência — um PV com N ferramentas gera N linhas legítimas), 2 deferidos documentados (chave por CNPJ multi-login; `status_registro` do matcher v2).
- ✅ **PR-2 admin mínimo `/admin/prime` (2026-07-11, [PR #1300](https://github.com/LucasSardenbergL/afiacao/pull/1300), frontend puro — deploy = só Publish):** tela staff em 3 abas. **Assinaturas:** criar (combobox de cliente aprovado + plano ativo → preço/franquia default editáveis ANTES de criar), suspender/reativar/cancelar; guards do banco (P0001/23505/23514) traduzidos em pt-BR (`src/lib/prime/erros.ts` — fallback repassa a mensagem do banco, nunca engole). **Uso:** gate de UI = só assinatura ATIVA no select (banco barra por trigger de todo jeito); afiação com valor amarrado `q × preço` em **centavos exatos** (espelho do CHECK — multiplicação em centavos inteiros para não divergir do numeric por float; preço limitado a 2 casas no form); franquia restante da competência exibida via view; estorno = único update, estornado fica com badge e imutável. **Planos:** criar + ativar/desativar; **v1 SEM edição de plano**. Competência SEMPRE derivada em `America/Sao_Paulo` (armadilha de virada de mês). Módulo `prime` no manifesto de módulos (`moneyPath: true`); 33 asserts de helpers puros (`src/lib/prime/prime.test.ts`). **Decisões herdadas resolvidas:** (minor 8) reativação v1 = simples, com rastro apensado em `observacao` ("Reativada em X — suspensa desde Y"); meses suspensos reaparecem no extrato como "sem uso registrado" (fato, não fabricação); histórico estruturado = v2 se o piloto mostrar suspensões longas. (minor 9, lição Rappi) `beneficios` jsonb sem snapshot → resolvido POR CONSTRUÇÃO: sem edição de plano; mudança de oferta = plano NOVO; **desativar plano com assinaturas vivas bloqueado** (policy de leitura do cliente exige `ativo` — desativar cegaria o assinante grandfathered no `/prime` do PR-3). **Lições:** (a) gate de fronteiras de módulos mordeu `prime→reposicao` (import de `formatBRL`) → formatadores locais no módulo (padrão do repo: grupos/financeiro têm os seus); registrar módulo novo no manifesto É parte de criar página nova. (b) tabelas fora do `types.ts` gerado → idioma canônico do repo é o cast com `eslint-disable` + interfaces manuais + nota de regen (`useClienteGrupos.ts`).

**Pendências vivas do programa:** fiscal (qual CNPJ emite, NFS-e/ISS — founder+contador ANTES do 1º boleto) · calibrar franquia/preço final (R$99–129 pela fórmula do §5) · seed do plano piloto via admin quando o preço cravar · aposentar `/loyalty`, `/gamification`, `/savings` (junto do PR-3) · nome definitivo do programa (marketing).
