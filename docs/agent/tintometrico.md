# Tintométrico — referência operacional (Afiação/Colacor, money-path)

> Balcão de tinta: cor + produto + base + embalagem → fórmula (receita de corantes) → preço. Princípios em `docs/agent/money-path.md`; casamento boletim↔SKU em `docs/agent/knowledge-base.md`. Módulo em `src/pages/Tint*.tsx`, `src/lib/tint/`, `src/hooks/useTint*`, edges `supabase/functions/tint-*`.

## Modelo de dados (a chave)

- **`tint_formulas`** = uma linha por combinação vendável `(account, cor_id, produto_id, base_id, subcolecao_id, embalagem_id)` — é a unique key `uq_tint_formulas_chave` (com `COALESCE(subcolecao_id, '000…')`). **`tint_formula_itens`** = a RECEITA (N linhas corante_id + qtd_ml). Fórmula SEM item = sem receita.
- **`sku_id`** liga a fórmula ao `omie_products` (base) via `tint_skus.omie_product_id`. É o `sku_id` que a busca do balcão filtra.
- **Base sem `tint_skus` pode ser INTENCIONAL, não gap de mapeamento** (founder, 2026-07-19, no QA da 2b-fix): produto-base com descrição **810ML nunca é vendido nessa descrição — vale para TODAS as bases** (ex.: WFOB.6554 810ML/PRD03527 fica sem mapear de propósito, enquanto 405ML/GL/QT da mesma família são mapeadas). O aviso "esta base não está configurada no módulo tintométrico" no balcão é o comportamento esperado nesses produtos. **Não abrir tarefa/chip de "mapear"** ao topar com base não-mapeada sem antes confirmar com o founder que ela é vendida.
- **`subcolecao_id` está na unique key.** ⚠️ Consequência cara (achado 2026-07-17): quando um writer muda o rótulo de subcoleção da MESMA fórmula (ex.: o sync passou a mandar `SL` onde o import de março mandava `1`/SAYERLACK), a chave muda → **INSERT em vez de UPDATE** → o catálogo inteiro DUPLICA. Em 2026-07-17 havia 2 gerações ativas: `1`/SAYERLACK (465k, congelada 03→04/2026, `preco_final_sayersystem` presente) e `SL` (493k, viva desde 06/2026, `preco_final_sayersystem` NULL em 100%). Ambas com `sku_id` → o catálogo aparece DUPLICADO na busca do balcão.
- **`volume_final_ml` tem convenção DIVERGENTE entre gerações:** na SAYERLACK, `vol_final = vol_embalagem + Σ(corantes)`; na SL, `vol_final = vol_embalagem` (não soma corante). Isso importa para detectar corrupção: a invariante `vol_final − vol_embalagem ≈ Σ(qtd_ml)` só vale na SAYERLACK.

## Preço = motor honesto, fail-closed (RPC `get_tint_price`/`get_tint_prices`)

- **A venda NUNCA lê `tint_formula_itens` direto** — passa pela RPC `SECURITY DEFINER` `get_tint_price` (single) / `get_tint_prices` (batch, alternativas+busca global). A RPC soma custo_base (Omie) + custo dos corantes e devolve `precoFinal` (+ custo gateado por staff — P1 `20260708234100`).
- **Fail-closed em receita VAZIA:** `v_corantes_completos := COALESCE(bool_and(custo_disponivel), false)`. `bool_and` sobre conjunto vazio = NULL → `COALESCE(…,false)` → false → `precoFinal = NULL`. `select-price.ts` então bloqueia TODAS as fontes (inclusive o CSV `preco_final_sayersystem`) → botão "Adicionar" desabilitado. Fórmula vazia **não vende**. (batch idêntico: fórmula sem item não entra no `GROUP BY` → `LEFT JOIN` → `COALESCE(corantes_completos,false)`.)
- ⚠️ **Fail-OPEN em receita PARCIAL** (achado 2026-07-17): se a fórmula tem ALGUNS corantes mas falta um (ex.: o import gravou 3 de 4), `bool_and` roda sobre os presentes → pode dar `true` → `precoFinal` calculado com receita INCOMPLETA → **preço baixo válido, subfaturamento SILENCIOSO**. Pior que a vazia (que é fail-closed). Medido: 240 na SAYERLACK (residuo de volume − Σqtd_ml > 1ml). Detecção só funciona na SAYERLACK (convenção de `vol_final`); a SL precisa de outro oráculo.

## Import de fórmulas — a fronteira de escrita (DOIS writers)

- **Fluxo INBOUND-only:** a máquina SayerSystem externa manda dados PRO app. O app NÃO manda receita a nenhuma máquina; NÃO há tela/impressão de receita pro operador. `tint_vendas`/`tint_vendas_itens` são tabelas MORTAS (0 escritores). A venda vira `sales_orders.items` (jsonb) → Omie.
- ⚠️ **O writer VIVO é o `tint_promote_sync_run`** (RPC SECURITY DEFINER), chamado pelo `tint-sync-agent` (`index.ts:397,542`) — é ele que alimenta o catálogo automático desde 18/06 (geração `SL`). **É o ÚNICO writer desde a Fase 1b**: o edge `tint-import` foi **APOSENTADO (410 `TINT_IMPORT_RETIRED`)** em 2026-07-17 — o frontend já saíra no #1314 (12/07), e prod confirmou 0 invocações desde então (última linha não-`sync_agent` em `tint_importacoes` é de 17/04), 0 crons, 0 imports em andamento. O bloco `MIRROR parseDecimalBR` segue verbatim no arquivo — **`src/lib/tint/__tests__/edge-parse-parity.test.ts` (vitest) LÊ este arquivo Deno via `readFileSync` e compara a função textualmente: removê-lo por "parecer órfão" quebra o CI, e nenhum lint avisa** (o acoplamento cruza a fronteira Node↔Deno). O corpo do writer foi **REMOVIDO em 18/07** (610→114 linhas): o #1401 deixou ~473 linhas mortas abaixo do 410, a um `return` de distância de serem ressuscitadas por quem "limpasse" o guard — mesmo desfecho do `syncOrdersIncremental` (omie-analytics-sync, 24/06). Recuperável em `git show b0092d88:supabase/functions/tint-import/index.ts`; é a versão **fail-open**, não reative direto. **Se o import manual voltar a ser requisito: implemente o fail-closed por-linha ANTES de remover o 410** — não reative o writer antigo direto.
- ⚠️ **Invariantes do Guard 4 que NÃO podem regredir** (5 bypasses P1 achados pelo Codex xhigh na v1 e fechados na `20260718100000`; cada um tem cenário em `db/test-tint-promote.sh`): **(1)** `_formulas_latest` só lê staging de run `status='complete'` — o edge commita headers ANTES dos itens e o advisory lock serializa a PROMOÇÃO, não a ingestão; sem isso um run lia header de outro em voo, via "0 corantes", tratava como base pura e APAGAVA a receita (C21). **(2)** 0 item válido no staging **nunca** apaga receita existente — o `DELETE` de itens exige `EXISTS(dose válida)`; trocar receita exige receita nova (C20). **(3)** dose positiva com `id_corante` vazio (órfão) **corrompe** — o edge converte ID ausente em `''` PRESERVANDO a dose, então ignorá-la perdia um componente (C17). **(4)** o guard é aplicado **DEPOIS** do `DISTINCT ON` que escolhe o vencedor da chave oficial (`DELETE` sobre `_expand_uniq`) — filtrar antes fazia o PERDEDOR da colisão virar vencedor e substituir header/preço/receita (C19). **(5)** "dose válida" = positiva **E FINITA**, num predicado ÚNICO reusado em guard/preço/stub/INSERT — em `numeric` o `NaN` ordena ACIMA de tudo, então `NaN <= 0` é FALSO e `NaN > 0` é VERDADEIRO, e o critério ingênuo deixava NaN/Infinity entrarem na receita (C18). ⚠️ **Armadilha plpgsql:** o JOIN novo usou alias `r`, que é uma **variável `record` declarada na função** — o identificador da variável VENCE o alias, a função COMPILA e só explode em runtime (`record "r" has no field "id"`). Só o PG17 pega; use `sr`.
- ✅ **Fronteira de escrita fail-closed por-linha (Guard 4 — Fase 1a, migrations `20260717163000` + `20260718100000`).** O padrão era delete-incondicional + insert-filtrado: `DELETE FROM tint_formula_itens ... ; INSERT ... WHERE id_corante<>'' AND COALESCE(qtd_ml,0)>0` — corante com qtd inválida era FILTRADO → receita **parcial** (fail-OPEN, subfaturamento silencioso) ou **zero**. Agora a promoção é **all-or-nothing por fórmula**: se a fórmula LATEST do staging tem ≥1 corante presente cujo conjunto de linhas não tem NENHUMA dose válida (`bool_and(COALESCE(qtd_ml,0)<=0)` por `(formula,corante)`), ela **não é promovida** — fica fora do `_expand` → fora de `_promoted` → o `DELETE ... USING _promoted` **não a toca** → a receita anterior é 100% preservada, e o erro vai a `tint_sync_errors` (`entity_type='formula_promote'`, msg `receita corrompida`). ⚠️ O critério é `bool_and` **POR corante** (não "≥1 linha ruim") de propósito: a **dosagem em 2 etapas** (mesmo corante em 2 ordens, ambas >0 — ver `20260622210000`) é legítima e NÃO pode ser barrada. Fórmula legitimamente **sem corante** (base pura) também não dispara. Fórmula NOVA toda-quebrada **não cria header** (não vira fórmula vazia ativa — o mal das 28.609). Provado em `db/test-tint-promote.sh` C14-C16 + falsificação (sabota o filtro → receita vira parcial). ⚠️ Enquanto o staging latest seguir corrompido, o erro **re-loga a cada run** (a latest-por-chave só melhora quando a FONTE manda uma versão boa) — é sinal honesto, não ruído a silenciar.
- ✅ **Protocolo de staging como UNIDADE (Fase 1c, migration `20260718170000` + edge).** O header declara `expected_item_count` = nº de linhas de item que a EDGE recebeu (`Array.isArray(f.itens) ? f.itens.length : null` — ausência NUNCA vira 0); a promoção (gate `_eu_incompleta`, pós-vencedor da colisão, antes do guard (c)) só aceita a fórmula quando o `COUNT(*)` BRUTO ingerido bate — fecha o subconjunto que atravessa fronteira de chunk (cleanup falho) e a corrida ingestão×promoção em chave nova. `NULL` = protocolo legado/simulação → caminho v3 exato. Id do header é PRÉ-GERADO na edge (associação header→itens não depende da ordem do retorno). `expected_item_count` sozinho NÃO autoriza limpeza de receita — o sinal semântico é o `is_base_pura` da Fase 1d (abaixo). ⚠️ Ordem de deploy: migration ANTES da edge (o edge INSERTa a coluna; sem ela, PGRST204 e o sync de fórmulas PARA).
- ✅ **Fronteira do CONECTOR + base pura explícita (Fase 1d, migration `20260722113000` + edge + binário).** O furo que a 1c documentou FECHOU: o conector (`connector/sayersync`) deixou de OMITIR slot inválido antes do POST — fonte manda `{AX=10, VM=0}` → o VM chega ao staging cru → **Guard 4 barra e loga** (antes o payload chegava "íntegro" e promovia parcial). Invariantes que NÃO podem regredir (prova: `fase1d_contrato_test.go` + `staging-rows_test.ts` + C30-C33/F1d do `db/test-tint-promote.sh`): **(1)** o conector é TRANSPORTE FIEL — corante presente com qtd inválida → item emitido com qtd crua (≤0) ou `null` (ilegível; NUNCA 0); órfão (corante vazio + dose legível ≠0) emitido; slot flat LIVRE (vazio+qtd nil/0) segue omitido (emiti-lo barraria ~100% do catálogo via C29). **(2)** `is_base_pura=true` SÓ quando a FONTE confirma: flat = 6 slots livres E 12 flat cols resolvidos; child = `formula_pk` resolvida E 0 linhas na filha; child com pk NÃO-resolvida → chave `itens` AUSENTE do payload (ambíguo → barra) — antes injetava `itens=[]` mentiroso em TUDO, e um is_base_pura derivado disso limparia o catálogo inteiro. **(3)** hash-cache: o campo novo entra no hash por APPEND CONDICIONAL (só quando true) — payload sem o campo preserva o stream byte a byte (goldens `TestFormulaContentHash1d_GoldenCompat`; mudá-los = re-envio dos ~485k). **(4)** edge repassa APENAS literal `true` (`staging-rows.ts`, builders puros testáveis — não reimplementar inline no index). **(5)** banco v5: a TRÍADE declarada (`is_base_pura=true AND expected=0 AND COUNT=0`) libera a transição legítima pigmentada→pura (limpa receita; `receitas_limpas` no metadata do run) e base pura NOVA; sinal CONTRADITÓRIO (true com itens/expected NULL) barra; vazio SEM declaração barra AGORA TAMBÉM em chave nova (endurecimento do C15a — fatos: 0 bases puras no fluxo vivo em 1 mês, 0 headers expected=0). **(6)** Pós-challenge Codex xhigh (2026-07-20, 1 P0 + 4 P1 no diff v1 — parecer cru no PR #1474): **cap anti-limpeza-em-massa é ACUMULADO em 24h por (account,store)** — soma `tint_sync_runs.metadata->>'receitas_limpas'` da janela + candidatas do run, contado por RECEITA (embalagem); um cap por-promote era contornável em CATRACA (run A limpa 50, run B limpa as 2 restantes) e célula a célula (C34 + falsificação F1d-4). **(7)** shape child extrai pai+filha na **MESMA tx REPEATABLE READ** (`ExtractFormulasComItens`) — sob READ COMMITTED, um delete+commit/reinsert na origem entre as 2 queries fabricava "0 linhas na filha" → is_base_pura falso → limpeza indevida por baixo de qualquer cap. **(8)** ramo **[1d-E]** do Guard 4: no protocolo novo (`expected` declarado), QUALQUER linha emitida que não seja {corante presente + dose válida} barra a fórmula INTEIRA — sem isso, dose válida mascarava linha inválida do MESMO corante (fallback silencioso de dosagem), placeholder real passava com irmão válido e slot flat ilegível sumia; headers legados preservam bool_and/2-etapas (C16 intacto; C29/C35/C36 provam). **(9)** todo POST do conector carrega `x-agent-version` → `tint_sync_runs.metadata.agent_version` (auditoria do ESTADO MISTO: binário velho segue FILTRANDO inválidos antes do POST — o furo original só fecha DE FATO quando o binário novo sobe; até lá o banco não vê o que não chega, e depois que sobe, toda parcial da janela muda de hash, re-envia e fica DENUNCIADA em `tint_sync_errors` a cada run). Base pura promovida fica ativa com 0 itens → preço RPC NULL (fail-closed) → NÃO vende; destravar venda de base pura é decisão futura explícita. ⚠️ Deploy em 3 peças NA MESMA JANELA (mesmo dia): migration ANTES da edge (PGRST204); binário on-prem por último (founder aplica). ⚠️ O 410 do `tint-import` agora está FIXADO em teste (`retired_test.ts`) — remover exige revisão money-path. Follow-ups registrados: `header_ready` por fórmula (pura promovível enquanto o request criador está aberto — run-status não é fronteira, 59% error legítimo); property test do hash; `source_item_index`.
- 🔎 **As "receitas corrompidas" que o Guard 4 loga desde 2026-07-21 são DADO DA FONTE — conector e guard estão CERTOS (diagnóstico completo, psql-ro; não refaça).** O 1º ciclo do conector 0.2.0 acendeu 2 assinaturas, e a leitura correta de ambas é *não mexer em código nosso*. Baseline: **195.046 fórmulas** na staging, **17** problemáticas (0,009%) — o transporte fiel está saudável. **(A) `303U - BS`, 16 fórmulas (todos os 16 produtos da cor), slot 4 com dose 2,4645 e `id_corante` VAZIO.** A coluna `corante4` é lida corretamente em **108.519** outras fórmulas → não é falha de mapeamento; a fonte tem mesmo dose sem ID. **Não é decidível daqui** se é 4º componente REAL (→ as 124 fórmulas ativas vendem receita PARCIAL: R$1,84–2,71 de corante por lata da formulação base, escalando na embalagem) ou lixo. ⚠️ A "corroboração" da gêmea de março é FRACA e quase me enganou: a geração `1` tem os mesmos 3 corantes, mas veio do import CSV cujo `processFormulas` **também** pulava corante inválido — as duas fontes podem ter filtrado o MESMO componente, não são independentes. Pista para a fonte: existe o corante `4` (WP06.3900 CONCENTRADO V, o mais caro dos 4) e a fórmula usa 2/3/5 — sedutor, mas slot≠id, não confunda. Exposição ATUAL ≈ 0 (a cor nunca foi vendida; o tintométrico tem 7 pedidos no app, último 2026-04-22 — o balcão físico dosa pela máquina, o app só precifica). **(B) `CAFELATTE ARAUCO ZAMBALD`, 1 fórmula, slot 6 com `id_corante='0'` e dose 0.** O corante `'0'` NÃO existe no cadastro (ids: 1..5, 8..16) e **21/21 ocorrências têm dose exatamente 0, todas no slot 6** → é a fonte marcando slot livre como `0/0`. O conector lê `"0"` como corante presente (string não-vazia) e emite; o guard barra. **Tentador "consertar" no conector — NÃO faça:** a Fase 1d fixou que *o conector é transporte fiel e o banco é a fronteira*, e a única exceção (omitir slot vazio) se justifica por VOLUME (~100% do catálogo, C29), não por semântica. Abrir exceção de interpretação para **1** fórmula inverte o princípio e assume que `'0'` nunca será corante real. Corrija na FONTE. Em ambos os casos a receita anterior fica preservada e a fórmula segue vendendo; o erro re-loga a cada run (~13min) até a origem mudar — sinal honesto.
- **O promote está inocentado para as 28.609 vazias de março** (vieram do CSV, `importacao_id` NULL, o promote nem existia). A limpeza delas é a Fase 5 (decisão do founder) — o Guard 4 fecha o dado NOVO, não retroage.
- ⚠️ **O parser conta FÓRMULA, não RECEITA** (achado 2026-07-17, `processFormulas`): `imported++` sobe por fórmula, independente dos itens; itens só são inseridos `if (coranteIds.length>0)`; `errors++` só em exceção/cor_id-vazio. → uma linha sem corante legível vira fórmula gravada, contada como "importada", **0 erros**. Foi assim que `FL.6344 PU FUNDO.CSV` reportou 29.548/29.548/0-erros gravando 0 receita em 28.592 fórmulas (célula SAYERLACK×FL.6344, 100% vazia). O import SÓ reporta erro de header/upsert (ex.: duplicate key), **nunca** erro de insert de receita (que vai a `console.error`).
- **Lição que sobrevive ao aposento: preflight de CLIENTE não é fronteira de escrita.** O `preflight-formulas.ts` reprovava "(linha sem corante)" e qtd inválida, mas era fronteira CLIENTE — o writer server-side (`processFormulas`) gravava receita PARCIAL (pulava o corante ruim) em vez de rejeitar a linha, e o delete+insert de itens não era transacional. "O buraco foi fechado" valia só para a UI. É por isso que a Fase 1 pôs o guard **no writer** (Guard 4, SQL) e não numa validação de UI. Os defeitos residuais do edge (`processFormulas` parcial; `:304` `.is("subcolecao_id", subcolecaoId ? undefined : null)` passando `undefined` ao `.is()` quando não-nula, que deveria ser `.eq`, com erro da consulta ignorado) morreram com o **410** — ficam registrados aqui como a dívida a NÃO recriar se alguém reabrir o endpoint.

## Busca do balcão (`useTintColorSelect.ts`)

- ✅ **Fase 2 (migration `20260718213000`): o picker lê a CANÔNICA `v_tint_formula_canonica`** — 1 fórmula por `(account, sku_id, cor_id)`, `ORDER BY cor_id, id` determinístico, nas 3 consultas (por-SKU, global, alternativas). Regra: rank 0 = SL com receita válida (espelho de `corantes_completos` da RPC — comentário `[MIRROR]` nos 2 lados; paridade singular+batch provada em `db/test-tint-canonica.sh`, 13 asserts + 5 falsificações dirigidas) · rank 1 = outra geração válida (fallback SAYERLACK/personalizada) · rank 2 = SL inválida · rank 3 resto · desempate `id ASC`. **`base_disponivel` fica FORA do rank** (gêmeas compartilham o `sku_id` — não discrimina) e **"sync recente" também** (updated_at é re-tocado em massa; janela eliminatória fliparia o catálogo pra geração congelada a cada pane do conector). A view NÃO desativa nada (Fase 5 = gate humano). `security_invoker=on` (repetir no replace!); grants: `authenticated`+`service_role`, anon revogado. ⚠️ **Delta medido ao cutover (2026-07-18):** 167.961 pares precificáveis, 15.096 divergentes; em 14.484 a SL cobra MENOS que o max(RPC, CSV-março) da SAYERLACK (p95 R$8,70; 6.604 >R$10).
- ✅ **Fase 2b-fix (review retroativo Codex xhigh, 2026-07-19): a escolha vale TAMBÉM nas alternativas/busca global, e o rótulo da fonte CSV é NEUTRO.** (a) `selectAltPrice` ganhou override `calculado|tabela` por formulaId (estado em `useTintColorSelect`; reset ao fechar/trocar cor/mudar busca; default segue a regra do maior; fonte sem valor → default; sem preço confiável → o override **não fura** o fail-closed — testes em `select-price.test.ts`); mini-seletor `AltPriceSourcePicker` nos 2 cards quando calc E CSV existem. Antes, confirmar direto uma alternativa vendia o preço do `selectAltPrice` sem escolha. (b) Rótulo **"Tabela importada"** (era `is_sl ? "Tabela (versão anterior)"`): o max de `preco_csv_legado` inclui a PRÓPRIA linha — se o sync um dia popular `preco_final_sayersystem` na geração SL (hoje NULL em 100%, medido 2026-07-18), o rótulo condicional afirmaria proveniência não provada. ✅ **A alternativa semântica foi ENTREGUE** (migration `20260722100002`, decisão do founder 2026-07-20 — opção 2 do P1 B): quando a canônica é SL, o max lê **só linhas não-SL** ativas da chave (o CSV próprio da SL — e de qualquer outra SL — nunca entra); canônica não-SL segue com o max de TODAS as ativas (comportamento da 2b verbatim, via curto-circuito `NOT rf.is_sl OR NOT EXISTS(sub SL)`). No-op no dado atual (baseline psql-ro 2026-07-20: 0 linhas SL ativas com CSV). Prova: C14 (future-proof — SL com CSV próprio 999 devolve o 260 da gêmea) + C15 (ramo não-SL segue incluindo SL no max) + falsificações dirigidas F6 (expressão da 2b de volta → só C14 cai) e F7 (exclui SL sempre → só C15 cai) em `db/test-tint-canonica.sh`. Com a proveniência garantida, o founder decidiu (2026-07-20) **restaurar o rótulo condicional** — PR #1479: `is_sl=true` → "Tabela (versão anterior)", ausente/false → "Tabela" genérico (nunca afirma sem prova), no card principal E no `AltPriceSourcePicker` (alternativas/busca global), com `is_sl` fluindo pelos 3 selects da canônica no hook.
- ✅ **Allowlist da geração '1' (migration `20260724130000`) — desfecho do challenge RETROATIVO do Codex xhigh (2026-07-20) sobre o par #1469/#1479, veredito REPROVADO com 2 achados procedentes.** **Achado 1 [P1 latente, semântica]:** o conjunto "não-SL" da blocklist da `20260722100002` inclui **personalizadas** (`subcolecao_id` NULL) e qualquer subcoleção futura ≠'SL' — mas o rótulo restaurado promete proveniência da geração CONGELADA '1', não "qualquer não-SL". Latente e não mentindo hoje (psql-ro 2026-07-21: 0 linhas SL ativas com CSV · 756 personalizadas ativas com CSV, mas **interseção com chaves que têm SL ativa = 0** · só '1' e 'SL' existem em `tint_subcolecoes`, 0 ativas fora dessas duas). Decisão do founder (2026-07-21): **allowlist estrita** — canônica SL → o max lê SÓ linhas ativas da subcoleção '1'; canônica não-SL segue com o max de todas (2b verbatim). NO-OP no dado atual; fail-closed para gerações futuras e para fórmula manual (precisão > recall: nada afirma "versão anterior" sem ser a versão anterior). **Achado 2 [P1 de harness, o mais caro]:** o mutante `NOT rf.is_sl OR g2.id <> f.id` — excluir só a PRÓPRIA linha em vez de toda SL — passava **todos** os asserts C13/C14/C15 e as falsificações F6/F7, porque nenhum seed K1-K16 tinha uma **2ª linha SL com CSV na mesma chave**. Fechado com 3 seeds e 3 falsificações novas em `db/test-tint-canonica.sh`: **K17/C16/F8** (2ª SL com CSV 800 → o max segue no 280 da '1'; mata o mutante), **K18/C17/F9** (personalizada com CSV 500 em chave de canônica SL → 290 da '1'; é o assert que FIXA a decisão de produto, e a falsificação restaura a blocklist para provar que ele tem dente), **K19/C18/F10** (linha desativada com CSV 900 → 300; antes nenhum seed exercia o `desativada_em IS NULL` do max com um CSV que mudasse o valor esperado). ⚠️ **Lição que generaliza:** o par de falsificações F6/F7 parecia cobrir a expressão inteira porque ataca os dois RAMOS do condicional (`is_sl` e não-`is_sl`) — mas ramo coberto ≠ **predicado** coberto: dentro do ramo SL, "exclui toda SL" e "exclui só a própria" são indistinguíveis enquanto a chave tiver no máximo UMA linha SL, e todo seed tinha. Falsificação prova que o assert tem dente contra a sabotagem que você imaginou; o seed é que decide **quais sabotagens são sequer distinguíveis**. Ao endurecer expressão que agrega sobre um CONJUNTO, semeie ≥2 linhas de cada classe que o filtro nomeia.
- ✅ **Challenge retroativo do próprio #1505 (Codex xhigh, 2026-07-21) — REPROVADO, 4 P1. A view está certa; a PROVA é que não estava.** O #1505 fechou dois achados mas nunca passou por revisão independente, sendo money-path. Fechado em `db/test-tint-canonica.sh` (21 ✅ · 0 ❌), **sem migration nova** — a expressão em prod não muda. **(1) Guard de conta sem cobertura:** `tint_formulas.subcolecao_id` referencia `tint_subcolecoes(id)` **sem `account`** na FK, então nada no banco impede uma fórmula de `colacor` apontar para a subcoleção de `oben` — quem barra é o `s2.account = g2.account` do EXISTS, e o harness era MONOCONTA (todo seed `oben`), então removê-lo não produzia vermelho nenhum. Fechado por K20/C19/F11. **(2) Exclusividade do literal `'1'` não provada:** com só `'SL'` e `'1'` semeados, `<> 'SL'`, `IN ('1','2')`, `LIKE '1%'` e `COALESCE(id_subcolecao_sayersystem,'1')='1'` (o campo é NULLABLE) passavam TODOS os asserts — e o 1º deles é justamente "qualquer não-SL", o que a allowlist existe para proibir. A afirmação "subcoleção futura NUNCA alimenta o rótulo" era a alegação central da migration e não tinha assert. Fechado por K21/C20/F12/F13. **(3) "Fail-closed" é impreciso na fronteira do pedido:** encolher o conjunto do `max()` reduz `preco_csv_legado`, que reduz `v_tab`, que reduz o piso `LEAST(v_calc, v_tab)` de `tint_gate_revalida` para as fontes `manual`/legado — ou seja, a mudança pode AMPLIAR aceitação, não restringir (cenário: calc 500, '1' 300, personalizada 400 → piso cai de 400 p/ 300 e um preço 350 que era barrado passa). Inerte hoje (delta 0 linhas), e coerente com o desenho (o piso deve seguir a tabela legítima), mas **a palavra correta é "precisão de proveniência", não "fail-closed"** — a coluna serve a DOIS consumidores com necessidades opostas (rótulo quer precisão, piso quer conservadorismo). Separar em `preco_csv_legado` + `preco_piso_legado` é follow-up de produto, não fechado aqui. **(4) ⚠️ O maior: "vermelho certo, E SÓ ELE" era ESTRUTURALMENTE inverificável** — todos os asserts num único `DO`, o 1º `RAISE` abortava, e o `case` da falsificação só via o PRIMEIRO nome. Ao instrumentar (acumular num array e emitir `FALHAS[n]: …`), a medição mostrou que **7 das 10 falsificações derrubavam MAIS de um assert** (F3 derruba 9, F5 derruba 8, F6 derruba 5): a alegação era falsa desde sempre e ninguém podia saber. Agora cada F declara o CONJUNTO EXATO (nomes + contagem) via `confere_falsificacao`. **Lição que generaliza:** falsificação prova que o assert tem dente; só **instrumentação do resultado** prova que ele é ESPECÍFICO. "E só ele" é uma afirmação sobre o conjunto — se o harness não sabe contar, não a escreva. Efeito colateral revelado pela mesma instrumentação: F1-F4 sabotavam rank **e** shape (view de 12 colunas) ao mesmo tempo; sabotagem que muda 2 coisas não isola o que prova. F11-F13 nascem por `sed` sobre a migration real, garantindo diferença de exatamente 1 ponto.
- ⚠️ **O re-dump do snapshot (#1509) MATOU este harness em silêncio, e nada acusou.** O snapshot passou a CONTER `v_tint_formula_canonica` (antes era de junho, anterior à view), então a cadeia começava com a view de 13 colunas já criada e a Fase 2 (12 colunas) morria em `cannot drop columns from view` — **antes do primeiro assert**, com o script inteiro parando. Ficou assim entre o #1509 e 2026-07-21 porque **`db/test-*.sh` não roda no CI** (são manuais). Fechado com `DROP VIEW IF EXISTS` antes da cadeia: o harness prova as MIGRATIONS, não o snapshot. **Regra:** todo `db/test-*.sh` que aplica migration de objeto que o snapshot já traz precisa dropar o objeto antes — e re-dump de snapshot é mudança que pode quebrar harness manual sem nenhum sinal vermelho.
- ✅ **Fase 2b (migration `20260718233000`): a ESCOLHA da vendedora — `preco_csv_legado`.** Decisão do founder pós-apply da Fase 2: em vez de o preço novo entrar sozinho, a vendedora escolhe no balcão entre o CÁLCULO da receita viva e o preço da VERSÃO ANTERIOR da tinta. A view expõe `preco_csv_legado` = max CSV das linhas ATIVAS da chave (SL canônica → CSV da gêmea antiga; fallback → o próprio; sem CSV → NULL); o hook alimenta `selectTintPrice` com ele → a fonte "Tabela (versão anterior)" volta ao seletor do `SelectedFormulaCard`. Default segue a regra 3 do select-price (usa o MAIOR — não baixa silenciosamente); BAIXAR pro cálculo novo é escolha ativa. ⚠️ Duas RPCs de prod (`get_tint_price`×`get_tint_prices`) divergem em ~2e-20 por acúmulo de precisão `numeric` — paridade de valor testa com `round(…,6)` (achado do C10b endurecido). Quando a Fase 5 desativar a geração antiga, o destino desta fonte é decisão explícita de lá (o max só lê ativas).
- Estado pré-Fase 2 (para arqueologia): a query era na tabela crua, sem filtro de geração, `LIMIT 20` sem `ORDER BY` → as 2 gerações da mesma cor voltavam como linhas visualmente idênticas e a escolha da vendedora decidia o preço silenciosamente.
- ✅ **Fase 3 (PR #1460, migration `20260722100001`): o "último preço do cliente" deixou de ser default e virou opt-in endurecido.** A regra 2 do `selectTintPrice` (cliente vence auto — inferência que BAIXAVA preço silenciosamente) foi removida; o chip "Cliente" segue no seletor como escolha ATIVA. A inferência crua (50 pedidos sem filtro) virou a RPC `tint_ultimo_preco_cliente`: só pedido REAL no Omie (`omie_pedido_id NOT NULL`), não-cancelado, janela 180d, com `p_exclude_sales_order_id` (anti-autovalidação na edição) — a MESMA função que o gate do submit usa (paridade estrutural picker×fronteira, zero espelho). INVOKER: sob o picker respeita a RLS de sales_orders; trocá-la p/ SECURITY DEFINER vazaria histórico entre clientes (falsificação F6 do harness prova).
- ✅ **Fase 4: `TintPricing.tsx` (simulador admin) usa o motor do balcão** — o motor paralelo (`precoBase×imposto×margem + corantes crus`, que fabricava número com receita vazia/parcial) foi substituído por busca na canônica + `get_tint_prices` batch + `precoSimulador` (`src/lib/tint/simulador-preco.ts`, wrapper de `selectTintPrice` com `motorFalhou` e motivo textual). Paridade provada por teste contra `selectAltPrice` (`__tests__/simulador-preco.test.ts`) — divergir de lá = motor paralelo de volta. Sem preço → motivo honesto (distingue receita vazia de corante sem custo via `tem_receita` da view). ⚠️ `tint_skus.imposto_pct`/`margem_pct` (seção 1 da tela) são ÓRFÃOS de motor: nenhum cálculo os lê — o preço do balcão é custo base + corantes, e o sync usa imposto/margem do STAGING SayerSystem (`tint_staging_precos_base`), não de `tint_skus`. A coluna "Referência" da tela diz isso na UI; não reintroduzir esses campos em cálculo de preço sem decisão explícita.

## Fronteira do SUBMIT (Fase 3 — gate `tint_gate_revalida`)

- ✅ **Fase 3 (PR #1460, migration `20260722100001` + edge): TODA via de pedido revalida o preço tint na fronteira `omie-vendas-sync`** (`criar_pedido`/`alterar_pedido`, imediatamente antes da mutação Omie). Vias: submitOrder · conversão de orçamento (`SalesQuotes`) · edição (`useSalesOrderEdit`) · retry idempotente (repetir-pedido já resolvia pelo picker; submitQuote não vai ao Omie — a conversão revalida). O item carrega a decisão da vendedora (`tint_price_source` calculado|tabela|cliente + `tint_discount_pct` + `tint_formula_id`) e o gate recomputa a MESMA fonte AGORA — **a escolha de fonte (2b) sobrevive; o que barra é preço obsoleto/fórmula morta**. Bloqueio = 200 estruturado `{blocked:'tint_preco', bloqueios:[…]}` (padrão gateCredito); o pedido local fica intacto.
- **Invariantes que NÃO podem regredir** (2 challenges Codex xhigh — design e diff — no PR #1460; prova `db/test-tint-gate-revalida.sh`, 33 asserts + 9 falsificações): **(1)** a classificação de item tint é SERVER-side pelo produto (`omie_products.is_tintometric AND tint_type='base'`) — omitir `tint_cor_id` ou mandar o código como STRING não desliga o gate; código ilegível = `payload_invalido`, nunca "não-tint". Base sem cor: bloqueia na criação (push nunca produziu — 0 na história); na edição só passa IDÊNTICA ao baseline (os 5.167 itens inbound do balcão físico ficam livres). **(2)** fonte declarada exige `tint_formula_id` coerente de (conta, cor); célula multi-SKU sem fórmula que desambigue bloqueia (nunca desempatar preço por UUID). **(3)** o fallback legado (piso `min(calc, tabela)`) exige LASTRO no jsonb persistido sem fonte — não é controlável pelo caller; item novo sem metadados bloqueia. Edição manual de preço = fonte `'manual'` explícita (≥ piso; subir livre). **(4)** motor sem preço bloqueia TODAS as fontes (paridade selectTintPrice regra 1). **(5)** a EDIÇÃO aguarda o edge e SÓ persiste no sucesso — o jsonb persistido é o BASELINE do gate (intocado × alterado, proveniência, anti-autovalidação); write-back do edge é checado (falha = erro explícito "Omie novo × local velho", nunca success). **(6)** paridade numérica com o JS por CONSTRUÇÃO: aritmética float8 (`ceil(v*10)/10` ≡ `Math.ceil`; `floor(v*100+0.5)/100` ≡ `Math.round` — `round(dp)` do PG é half-to-even!), provada diferencial SQL×node (G18). **(7)** classificação em BATCH no edge ANTES da RPC: migration ausente/RPC fora só afeta pedido com cara de tint (fail-closed deliberado ≠ gateCredito fail-open: o risco real é migration custom não aplicada — gate fantasma seria invisível).
- **Follow-ups conhecidos (calibrados, não furos novos):** drift local×Omie na edição (o `alterar_pedido` SEMPRE re-enviou o local por cima do Omie, para qualquer item — reconciliação é fase própria); janela TOCTOU residual de ~1-2s entre o gate e o `IncluirPedido` (era 5min de cache); carimbo de validação server-side no jsonb; cap comercial de desconto (0–99,99% livre é comportamento pré-existente da UI).

## Regra de ouro

Fórmula ativa com preço mas **0 corantes NÃO é catálogo legítimo** (base pura) — é receita perdida. Prova barata: `volume_final_ml − vol_embalagem` da fórmula vazia bate com a soma dos corantes da gêmea com receita. Cores como PRETA/AMARELO/PANTONE com 0 corante são fisicamente impossíveis. Precisão > recall: **não apagar/desativar em massa sem o founder** — 465k linhas de catálogo, irreversível na prática; a remediação é o programa em `docs/superpowers/plans/2026-07-17-tint-receita-perdida-remediacao.md`.
