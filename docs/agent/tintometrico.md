# Tintométrico — referência operacional (Afiação/Colacor, money-path)

> Balcão de tinta: cor + produto + base + embalagem → fórmula (receita de corantes) → preço. Princípios em `docs/agent/money-path.md`; casamento boletim↔SKU em `docs/agent/knowledge-base.md`. Módulo em `src/pages/Tint*.tsx`, `src/lib/tint/`, `src/hooks/useTint*`, edges `supabase/functions/tint-*`.

## Modelo de dados (a chave)

- **`tint_formulas`** = uma linha por combinação vendável `(account, cor_id, produto_id, base_id, subcolecao_id, embalagem_id)` — é a unique key `uq_tint_formulas_chave` (com `COALESCE(subcolecao_id, '000…')`). **`tint_formula_itens`** = a RECEITA (N linhas corante_id + qtd_ml). Fórmula SEM item = sem receita.
- **`sku_id`** liga a fórmula ao `omie_products` (base) via `tint_skus.omie_product_id`. É o `sku_id` que a busca do balcão filtra.
- **`subcolecao_id` está na unique key.** ⚠️ Consequência cara (achado 2026-07-17): quando um writer muda o rótulo de subcoleção da MESMA fórmula (ex.: o sync passou a mandar `SL` onde o import de março mandava `1`/SAYERLACK), a chave muda → **INSERT em vez de UPDATE** → o catálogo inteiro DUPLICA. Em 2026-07-17 havia 2 gerações ativas: `1`/SAYERLACK (465k, congelada 03→04/2026, `preco_final_sayersystem` presente) e `SL` (493k, viva desde 06/2026, `preco_final_sayersystem` NULL em 100%). Ambas com `sku_id` → o catálogo aparece DUPLICADO na busca do balcão.
- **`volume_final_ml` tem convenção DIVERGENTE entre gerações:** na SAYERLACK, `vol_final = vol_embalagem + Σ(corantes)`; na SL, `vol_final = vol_embalagem` (não soma corante). Isso importa para detectar corrupção: a invariante `vol_final − vol_embalagem ≈ Σ(qtd_ml)` só vale na SAYERLACK.

## Preço = motor honesto, fail-closed (RPC `get_tint_price`/`get_tint_prices`)

- **A venda NUNCA lê `tint_formula_itens` direto** — passa pela RPC `SECURITY DEFINER` `get_tint_price` (single) / `get_tint_prices` (batch, alternativas+busca global). A RPC soma custo_base (Omie) + custo dos corantes e devolve `precoFinal` (+ custo gateado por staff — P1 `20260708234100`).
- **Fail-closed em receita VAZIA:** `v_corantes_completos := COALESCE(bool_and(custo_disponivel), false)`. `bool_and` sobre conjunto vazio = NULL → `COALESCE(…,false)` → false → `precoFinal = NULL`. `select-price.ts` então bloqueia TODAS as fontes (inclusive o CSV `preco_final_sayersystem`) → botão "Adicionar" desabilitado. Fórmula vazia **não vende**. (batch idêntico: fórmula sem item não entra no `GROUP BY` → `LEFT JOIN` → `COALESCE(corantes_completos,false)`.)
- ⚠️ **Fail-OPEN em receita PARCIAL** (achado 2026-07-17): se a fórmula tem ALGUNS corantes mas falta um (ex.: o import gravou 3 de 4), `bool_and` roda sobre os presentes → pode dar `true` → `precoFinal` calculado com receita INCOMPLETA → **preço baixo válido, subfaturamento SILENCIOSO**. Pior que a vazia (que é fail-closed). Medido: 240 na SAYERLACK (residuo de volume − Σqtd_ml > 1ml). Detecção só funciona na SAYERLACK (convenção de `vol_final`); a SL precisa de outro oráculo.

## Import de fórmulas — a fronteira de escrita (DOIS writers)

- **Fluxo INBOUND-only:** a máquina SayerSystem externa manda dados PRO app. O app NÃO manda receita a nenhuma máquina; NÃO há tela/impressão de receita pro operador. `tint_vendas`/`tint_vendas_itens` são tabelas MORTAS (0 escritores). A venda vira `sales_orders.items` (jsonb) → Omie.
- ⚠️ **O writer VIVO é o `tint_promote_sync_run`** (RPC SECURITY DEFINER), chamado pelo `tint-sync-agent` (`index.ts:397,542`) — é ele que alimenta o catálogo automático desde 18/06 (geração `SL`). **É o ÚNICO writer desde a Fase 1b**: o edge `tint-import` foi **APOSENTADO (410 `TINT_IMPORT_RETIRED`)** em 2026-07-17 — o frontend já saíra no #1314 (12/07), e prod confirmou 0 invocações desde então (última linha não-`sync_agent` em `tint_importacoes` é de 17/04), 0 crons, 0 imports em andamento. O bloco `MIRROR parseDecimalBR` segue verbatim no arquivo (o teste de paridade depende dele); o resto do código é inalcançável de propósito. **Se o import manual voltar a ser requisito: implemente o fail-closed por-linha ANTES de remover o 410** — não reative o writer antigo direto.
- ⚠️ **Invariantes do Guard 4 que NÃO podem regredir** (5 bypasses P1 achados pelo Codex xhigh na v1 e fechados na `20260718100000`; cada um tem cenário em `db/test-tint-promote.sh`): **(1)** `_formulas_latest` só lê staging de run `status='complete'` — o edge commita headers ANTES dos itens e o advisory lock serializa a PROMOÇÃO, não a ingestão; sem isso um run lia header de outro em voo, via "0 corantes", tratava como base pura e APAGAVA a receita (C21). **(2)** 0 item válido no staging **nunca** apaga receita existente — o `DELETE` de itens exige `EXISTS(dose válida)`; trocar receita exige receita nova (C20). **(3)** dose positiva com `id_corante` vazio (órfão) **corrompe** — o edge converte ID ausente em `''` PRESERVANDO a dose, então ignorá-la perdia um componente (C17). **(4)** o guard é aplicado **DEPOIS** do `DISTINCT ON` que escolhe o vencedor da chave oficial (`DELETE` sobre `_expand_uniq`) — filtrar antes fazia o PERDEDOR da colisão virar vencedor e substituir header/preço/receita (C19). **(5)** "dose válida" = positiva **E FINITA**, num predicado ÚNICO reusado em guard/preço/stub/INSERT — em `numeric` o `NaN` ordena ACIMA de tudo, então `NaN <= 0` é FALSO e `NaN > 0` é VERDADEIRO, e o critério ingênuo deixava NaN/Infinity entrarem na receita (C18). ⚠️ **Armadilha plpgsql:** o JOIN novo usou alias `r`, que é uma **variável `record` declarada na função** — o identificador da variável VENCE o alias, a função COMPILA e só explode em runtime (`record "r" has no field "id"`). Só o PG17 pega; use `sr`.
- ✅ **Fronteira de escrita fail-closed por-linha (Guard 4 — Fase 1a, migrations `20260717163000` + `20260718100000`).** O padrão era delete-incondicional + insert-filtrado: `DELETE FROM tint_formula_itens ... ; INSERT ... WHERE id_corante<>'' AND COALESCE(qtd_ml,0)>0` — corante com qtd inválida era FILTRADO → receita **parcial** (fail-OPEN, subfaturamento silencioso) ou **zero**. Agora a promoção é **all-or-nothing por fórmula**: se a fórmula LATEST do staging tem ≥1 corante presente cujo conjunto de linhas não tem NENHUMA dose válida (`bool_and(COALESCE(qtd_ml,0)<=0)` por `(formula,corante)`), ela **não é promovida** — fica fora do `_expand` → fora de `_promoted` → o `DELETE ... USING _promoted` **não a toca** → a receita anterior é 100% preservada, e o erro vai a `tint_sync_errors` (`entity_type='formula_promote'`, msg `receita corrompida`). ⚠️ O critério é `bool_and` **POR corante** (não "≥1 linha ruim") de propósito: a **dosagem em 2 etapas** (mesmo corante em 2 ordens, ambas >0 — ver `20260622210000`) é legítima e NÃO pode ser barrada. Fórmula legitimamente **sem corante** (base pura) também não dispara. Fórmula NOVA toda-quebrada **não cria header** (não vira fórmula vazia ativa — o mal das 28.609). Provado em `db/test-tint-promote.sh` C14-C16 + falsificação (sabota o filtro → receita vira parcial). ⚠️ Enquanto o staging latest seguir corrompido, o erro **re-loga a cada run** (a latest-por-chave só melhora quando a FONTE manda uma versão boa) — é sinal honesto, não ruído a silenciar.
- ✅ **Protocolo de staging como UNIDADE (Fase 1c, migration `20260718170000` + edge).** O header declara `expected_item_count` = nº de linhas de item que a EDGE recebeu (`Array.isArray(f.itens) ? f.itens.length : null` — ausência NUNCA vira 0); a promoção (gate `_eu_incompleta`, pós-vencedor da colisão, antes do guard (c)) só aceita a fórmula quando o `COUNT(*)` BRUTO ingerido bate — fecha o subconjunto que atravessa fronteira de chunk (cleanup falho) e a corrida ingestão×promoção em chave nova. `NULL` = protocolo legado/simulação → caminho v3 exato. Id do header é PRÉ-GERADO na edge (associação header→itens não depende da ordem do retorno). ⚠️ **`expected_item_count` NÃO autoriza limpeza de receita** (Codex 2026-07-18): o CONECTOR (`connector/sayersync/pg.go`, flat `:262`/child `:317`) OMITE slot inválido ANTES do POST — 0/ausente não distingue base pura de corrupção filtrada; o guard (c) segue barrando TODO vazio. ⚠️ **Furo pré-existente da fronteira do conector** (não é do banco): fonte manda `{AX=10, VM=0}` → conector filtra o VM → payload "íntegro" → guards nunca veem → promove parcial. Fecha na **Fase 1d** (conector preserva/rejeita + `is_base_pura` explícito + contrato Go→edge). ⚠️ Ordem de deploy: migration ANTES da edge (o edge INSERTa a coluna; sem ela, PGRST204 e o sync de fórmulas PARA).
- **O promote está inocentado para as 28.609 vazias de março** (vieram do CSV, `importacao_id` NULL, o promote nem existia). A limpeza delas é a Fase 5 (decisão do founder) — o Guard 4 fecha o dado NOVO, não retroage.
- ⚠️ **O parser conta FÓRMULA, não RECEITA** (achado 2026-07-17, `processFormulas`): `imported++` sobe por fórmula, independente dos itens; itens só são inseridos `if (coranteIds.length>0)`; `errors++` só em exceção/cor_id-vazio. → uma linha sem corante legível vira fórmula gravada, contada como "importada", **0 erros**. Foi assim que `FL.6344 PU FUNDO.CSV` reportou 29.548/29.548/0-erros gravando 0 receita em 28.592 fórmulas (célula SAYERLACK×FL.6344, 100% vazia). O import SÓ reporta erro de header/upsert (ex.: duplicate key), **nunca** erro de insert de receita (que vai a `console.error`).
- **Lição que sobrevive ao aposento: preflight de CLIENTE não é fronteira de escrita.** O `preflight-formulas.ts` reprovava "(linha sem corante)" e qtd inválida, mas era fronteira CLIENTE — o writer server-side (`processFormulas`) gravava receita PARCIAL (pulava o corante ruim) em vez de rejeitar a linha, e o delete+insert de itens não era transacional. "O buraco foi fechado" valia só para a UI. É por isso que a Fase 1 pôs o guard **no writer** (Guard 4, SQL) e não numa validação de UI. Os defeitos residuais do edge (`processFormulas` parcial; `:304` `.is("subcolecao_id", subcolecaoId ? undefined : null)` passando `undefined` ao `.is()` quando não-nula, que deveria ser `.eq`, com erro da consulta ignorado) morreram com o **410** — ficam registrados aqui como a dívida a NÃO recriar se alguém reabrir o endpoint.

## Busca do balcão (`useTintColorSelect.ts`)

- ✅ **Fase 2 (migration `20260718213000`): o picker lê a CANÔNICA `v_tint_formula_canonica`** — 1 fórmula por `(account, sku_id, cor_id)`, `ORDER BY cor_id, id` determinístico, nas 3 consultas (por-SKU, global, alternativas). Regra: rank 0 = SL com receita válida (espelho de `corantes_completos` da RPC — comentário `[MIRROR]` nos 2 lados; paridade singular+batch provada em `db/test-tint-canonica.sh`, 13 asserts + 5 falsificações dirigidas) · rank 1 = outra geração válida (fallback SAYERLACK/personalizada) · rank 2 = SL inválida · rank 3 resto · desempate `id ASC`. **`base_disponivel` fica FORA do rank** (gêmeas compartilham o `sku_id` — não discrimina) e **"sync recente" também** (updated_at é re-tocado em massa; janela eliminatória fliparia o catálogo pra geração congelada a cada pane do conector). A view NÃO desativa nada (Fase 5 = gate humano). `security_invoker=on` (repetir no replace!); grants: `authenticated`+`service_role`, anon revogado. ⚠️ **Delta medido ao cutover (2026-07-18):** 167.961 pares precificáveis, 15.096 divergentes; em 14.484 a SL cobra MENOS que o max(RPC, CSV-março) da SAYERLACK (p95 R$8,70; 6.604 >R$10).
- ✅ **Fase 2b (migration `20260718233000`): a ESCOLHA da vendedora — `preco_csv_legado`.** Decisão do founder pós-apply da Fase 2: em vez de o preço novo entrar sozinho, a vendedora escolhe no balcão entre o CÁLCULO da receita viva e o preço da VERSÃO ANTERIOR da tinta. A view expõe `preco_csv_legado` = max CSV das linhas ATIVAS da chave (SL canônica → CSV da gêmea antiga; fallback → o próprio; sem CSV → NULL); o hook alimenta `selectTintPrice` com ele → a fonte "Tabela (versão anterior)" volta ao seletor do `SelectedFormulaCard`. Default segue a regra 3 do select-price (usa o MAIOR — não baixa silenciosamente); BAIXAR pro cálculo novo é escolha ativa. ⚠️ Duas RPCs de prod (`get_tint_price`×`get_tint_prices`) divergem em ~2e-20 por acúmulo de precisão `numeric` — paridade de valor testa com `round(…,6)` (achado do C10b endurecido). Quando a Fase 5 desativar a geração antiga, o destino desta fonte é decisão explícita de lá (o max só lê ativas).
- Estado pré-Fase 2 (para arqueologia): a query era na tabela crua, sem filtro de geração, `LIMIT 20` sem `ORDER BY` → as 2 gerações da mesma cor voltavam como linhas visualmente idênticas e a escolha da vendedora decidia o preço silenciosamente.
- `lastPracticedPrice` (`:220`): pega qualquer item coincidente nos 50 pedidos mais recentes, SEM filtro de status/cancelamento/idade, e **vence** cálculo/CSV mesmo sendo menor. Para fórmula vazia é bloqueado; para gêmea válida, perpetua preço velho. Não é "acordo explícito" — é inferência frágil.
- ✅ **Fase 4: `TintPricing.tsx` (simulador admin) usa o motor do balcão** — o motor paralelo (`precoBase×imposto×margem + corantes crus`, que fabricava número com receita vazia/parcial) foi substituído por busca na canônica + `get_tint_prices` batch + `precoSimulador` (`src/lib/tint/simulador-preco.ts`, wrapper de `selectTintPrice` com `motorFalhou` e motivo textual). Paridade provada por teste contra `selectAltPrice` (`__tests__/simulador-preco.test.ts`) — divergir de lá = motor paralelo de volta. Sem preço → motivo honesto (distingue receita vazia de corante sem custo via `tem_receita` da view). ⚠️ `tint_skus.imposto_pct`/`margem_pct` (seção 1 da tela) são ÓRFÃOS de motor: nenhum cálculo os lê — o preço do balcão é custo base + corantes, e o sync usa imposto/margem do STAGING SayerSystem (`tint_staging_precos_base`), não de `tint_skus`. A coluna "Referência" da tela diz isso na UI; não reintroduzir esses campos em cálculo de preço sem decisão explícita.

## Regra de ouro

Fórmula ativa com preço mas **0 corantes NÃO é catálogo legítimo** (base pura) — é receita perdida. Prova barata: `volume_final_ml − vol_embalagem` da fórmula vazia bate com a soma dos corantes da gêmea com receita. Cores como PRETA/AMARELO/PANTONE com 0 corante são fisicamente impossíveis. Precisão > recall: **não apagar/desativar em massa sem o founder** — 465k linhas de catálogo, irreversível na prática; a remediação é o programa em `docs/superpowers/plans/2026-07-17-tint-receita-perdida-remediacao.md`.
